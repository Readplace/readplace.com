{
  "name": "@packages/hutch-infra-components",
  "targets": {
    "check": {
      "command": "pnpm lint && pnpm test",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["compile"]
    }
  }
}
