{
  "name": "@packages/onboarding-extension-signal",
  "version": "1.0.0",
  "description": "Shared read/write interface for the localStorage signal that the browser extension sets on Readplace pages to indicate it is installed",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "lint": "concurrently --kill-others-on-fail \"tsc --project tsconfig.lint.json\" \"biome lint src\" \"knip\"",
    "test": "echo 'No testable logic — trivial localStorage wrappers'",
    "check": "nx run @packages/onboarding-extension-signal:check",
    "check-infra": "echo 'No infrastructure — shared library'"
  },
  "devDependencies": {
    "concurrently": "10.0.0",
    "typescript": "5.9.3"
  }
}
