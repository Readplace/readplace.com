{
  "$schema": "https://json-schema.org/schema",
  "$id": "hutch-workspace-project",
  "title": "Scaffold a project under projects/ from the workspace's standard template",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Project directory name (kebab-case); also becomes the unscoped package name",
      "$default": { "$source": "argv", "index": 0 }
    },
    "description": {
      "type": "string",
      "description": "One-sentence description recorded in the generated package.json"
    }
  },
  "required": ["name", "description"],
  "additionalProperties": false
}
