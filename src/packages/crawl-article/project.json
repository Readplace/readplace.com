{
  "name": "@packages/crawl-article",
  "targets": {
    "check": {
      "command": "pnpm lint && pnpm test-with-coverage",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["compile"]
    },
    "tier-1-plus-pipeline-health": {
      "command": "node --test --test-reporter=spec dist/scripts/tier-1-plus-pipeline-health.js",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["compile"]
    }
  }
}
