{
  "name": "@packages/check-failed-articles",
  "targets": {
    "check": {
      "command": "pnpm lint && pnpm test",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["compile"]
    },
    "check-failed-articles": {
      "command": "node --test --test-reporter=spec dist/scripts/check-failed-articles.js",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["compile"]
    }
  }
}
