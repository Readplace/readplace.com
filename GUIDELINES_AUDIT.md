# Guidelines & Skills Audit — Readplace monorepo

Generated by a 196-agent adversarially-verified audit across **114 slices** (30 npm projects + 84 hutch leaf folders). Each finding was produced by one auditor and independently confirmed by a second agent told to refute by default; **32 false positives were dropped**.

**Report only — no code was changed.**

| Severity | Count |
|---|---|
| 🔴 High | 39 |
| 🟡 Medium | 122 |
| ⚪ Low | 86 |
| **Total** | **247** |

## 🔴 High severity (39)

### projects/browser-extension-core  _(2)_

#### `projects/browser-extension-core/src/reading-list/siren-reading-list.ts`:273

- **Rule:** Raw console.* instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Logging' forbids raw console.* in application code: 'Use HutchLogger from @packages/hutch-logger for all logging. Never use raw console.log/console.error in application code.' This library module logs the server's fallback message via console.warn instead of an injected HutchLogger.
- **Verifier:** Snippet exists verbatim at line 273 in initSaveHtmlUnderstanding; this is browser application code exported via index.ts, the package depends on @packages/hutch-logger, and no logger is injected — raw console.* violates the Logging rule.

```
console.warn(errorParsed.data.properties.message);
```

#### `projects/browser-extension-core/src/reading-list/siren-reading-list.ts`:343

- **Rule:** Raw console.* instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** Same as line 273 — raw console.warn in the save-content understanding violates CLAUDE.md 'Logging' ('Never use raw console.log/console.error in application code'). The understanding has access to no logger; one should be injected.
- **Verifier:** Snippet exists verbatim at line 343 in initSaveContentUnderstanding, same application-code context as line 273 — confirmed.

```
console.warn(errorParsed.data.properties.message);
```

### projects/extensions/chrome-extension  _(2)_

#### `projects/extensions/chrome-extension/src/runtime/background/background.browser.ts`:2

- **Rule:** Time/plan-bound comment (task/PR reference) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound comments and references to the current task/PR ("leftover notes from an implementation plan", task references). This comment is a leftover test/probe note that assumes a particular moment and will rot; it documents neither a why nor a code constraint.
- **Verifier:** Line 2 contains the exact 'test 3/4 probe' comment — a leftover plan/probe note documenting neither a why nor a constraint, squarely banned by the plan-bound comment rule.

```
// (test 3/4 probe: chrome-only edit must publish chrome but skip firefox)
```

#### `projects/extensions/chrome-extension/src/runtime/background/background.browser.ts`:34

- **Rule:** Avoid TypeScript type assertions (`as`) at a boundary where a Zod schema belongs _(source: CLAUDE.md)_
- **Why:** `raw` is read from browser.storage.local (an external/persisted system boundary) and cast straight to OAuthTokens. CLAUDE.md 'Avoid TypeScript Type Assertions' mandates a Zod schema with .parse() at system boundaries (DynamoDB, HTTP, etc.) instead of `as`; the cast silently hides any shape mismatch of persisted token data. Not one of the allowed `as` exceptions (as const / isolated Node wrapper).
- **Verifier:** Line 34 reads value from browser.storage.local (line 31-32) and casts to OAuthTokens with `as` — a persisted-storage boundary; not an allowed exception, so the rule is violated.

```
return raw as OAuthTokens;
```

### projects/extensions/firefox-extension  _(1)_

#### `projects/extensions/firefox-extension/src/runtime/background/background.browser.ts`:2

- **Rule:** Time/plan/task-bound comment _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids leftover plan/task notes and references to the current task/PR ('this is a test to do X', task references). This is a CI publish-probe note that will rot; it documents a transient testing experiment, not a why about the code.
- **Verifier:** Line 2 matches exactly; it is a transient CI-probe note ('test 4/4 probe') with no bearing on the code's behaviour — a textbook plan/task-bound comment the rule forbids.

```
// (test 4/4 probe: firefox-only edit must publish firefox but skip chrome)
```

### projects/hutch/src/e2e  _(2)_

#### `projects/hutch/src/e2e/e2e-server.main.ts`:34

- **Rule:** Raw console instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Logging) states: "Never use raw console.log/console.error in application code." A HutchLogger is already constructed on line 32 (const logger = HutchLogger.from(consoleLogger)). This logError helper, which is wired into crawlArticle, the parser, and the shared deps, should route through that logger (e.g. logger.error) rather than calling console.error directly.
- **Verifier:** Line 34 contains the exact console.error call; logError is wired into crawlArticle (l.63), parseArticle (l.64), and shared.logError (l.145), and a HutchLogger already exists on l.32 and is used on l.222, so the raw console.error is a genuine violation with no e2e/playwright-config exception carved out.

```
const logError = (message: string, error?: Error) => console.error(JSON.stringify({ level: "ERROR", ... }))
```

#### `projects/hutch/src/e2e/e2e-server.main.ts`:93

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Environment Variable Access) states: "Never use process.env directly" and the only documented exception is playwright.config.*.ts files. This is e2e-server.main.ts (an Express composition root), not a playwright config, and the file already imports and uses requireEnv (line 26). The CI flag should be read via getEnv/requireEnv rather than process.env directly.
- **Verifier:** Line 93 reads process.env.CI directly; the file already imports requireEnv (l.15) and uses it on l.26, and the sole documented exception (playwright.config.*.ts) does not cover e2e-server.main.ts, so the violation holds.

```
const eventLogger = process.env.CI === 'true' ? noopLogger : logger
```

### projects/hutch/src/runtime  _(2)_

#### `projects/hutch/src/runtime/server.main.ts`:7

- **Rule:** Raw console.log/console.error instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Logging) says: "Never use raw console.log/console.error in application code. In composition roots, create the logger with HutchLogger.from(consoleLogger)." This composition root uses raw console.log instead of a HutchLogger; it is not marked c8-ignore and other .main.ts roots in this same folder already wire HutchLogger.from(consoleLogger).
- **Verifier:** Verified server.main.ts:6-8 uses raw console.log in a composition root (createHutchApp + app.listen); CLAUDE.md Logging rule explicitly forbids this and mandates HutchLogger.from(consoleLogger).

```
app.listen(PORT, () => {
  console.log(`Server is running on http://localhost:${PORT}`);
});
```

#### `projects/hutch/src/runtime/app.ts`:120, 525

- **Rule:** Raw console.error instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Logging) forbids raw console.error in application code and requires composition roots to use HutchLogger.from(consoleLogger). The file already imports HutchLogger/consoleLogger (line 89) and uses HutchLogger elsewhere, but the logError closures (lines 120 and 525) emit via raw console.error. The c8-ignore on the file does not exempt it from the logging rule.
- **Verifier:** Line 120 matches the snippet exactly and line 525 repeats the same console.error closure; HutchLogger/consoleLogger are imported (line 89) and used elsewhere, and the c8-ignore is a coverage exemption only, not a logging-rule exemption.

```
const logError = (message, error) => console.error(JSON.stringify({ level: "ERROR", timestamp: new Date().toISOString(), message, stack: error?.stack }));
```

### projects/hutch/src/runtime/domain  _(2)_

#### `projects/hutch/src/runtime/domain/logger.ts`:8,17

- **Rule:** Raw console.log/console.error instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Logging) states: "Use HutchLogger from @packages/hutch-logger for all logging. Never use raw console.log/console.error in application code." This is application code that builds its own logger directly on raw console.log/console.error instead of using HutchLogger.from(consoleLogger).
- **Verifier:** Confirmed: logger.ts line 8 uses console.log and line 17 uses console.error in a custom application-code logger, exactly the raw-console pattern the Logging rule forbids in favor of HutchLogger.

```
console.log(
				JSON.stringify({ level: "INFO", ... console.error(JSON.stringify({ level: "ERROR", ...
```

#### `projects/hutch/src/runtime/domain/require-env.ts`:4-8

- **Rule:** Never default missing environment variables (getEnv/requireEnv with a fallback) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Environment Variable Access) states: "Never default missing environment variables. Always use requireEnv and let the process fail if a variable is not set. Do not use getEnv with a fallback ... to work around CI environments missing secrets. Silent defaults complicate debugging." The documented requireEnv "throws if not set" with no defaultValue parameter; this implementation adds a defaultValue escape hatch that silently returns a default instead of failing, directly enabling the forbidden silent-default behavior.
- **Verifier:** Confirmed: lines 4-8 add a defaultValue option and return it when the env var is undefined, a silent-default path the 'Never default missing environment variables' rule explicitly forbids.

```
export const requireEnv = <T extends string = string>(name: string, options?: { defaultValue: T }): T => { const defaultValue = options?.defaultValue; ... if (value === undefined && defaultValue !== undefined) return defaultValue;
```

### projects/hutch/src/runtime/providers/article-store  _(1)_

#### `projects/hutch/src/runtime/providers/article-store/dynamodb-article-store.ts`:1, 574

- **Rule:** Whole-file c8 ignore on a non-thin, branching file (Allowed c8 ignore Cases / Thin AWS SDK Wrappers) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md restricts whole-file `/* c8 ignore start/stop */` to thin AWS SDK wrappers (the test-driven-design skill defines 'truly trivial' as 3-5 lines, no branching). This is a ~574-line file containing real logic: pagination loops (findArticlesByUser, countArticlesByUser), conditional FilterExpression assembly, branching update logic in updateArticleStatus, and ConditionalCheckFailedException handling. It also has a co-located dynamodb-article-store.test.ts exercising it, so it is testable rather than integration-only. The whole-file ignore hides untested branches that the coverage rules require to be exercised, not excluded.
- **Verifier:** Verified: 574-line file, c8 ignore spans line 1 to 574, with if/for/while/ternary branching and ConditionalCheckFailedException handling; skill line 321 limits whole-file ignore to 3-5 lines no branching, and a co-located test file exists.

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */ ... /* c8 ignore stop */
```

### projects/hutch/src/runtime/providers/article-summary  _(1)_

#### `projects/hutch/src/runtime/providers/article-summary/dynamodb-generated-summary.ts`:1,133

- **Rule:** c8 ignore without an approved/valid reason (whole-file ignore over branching, unit-tested logic) _(source: CLAUDE.md (Allowed `c8 ignore` Cases) + test-driven-design/SKILL.md (Thin AWS SDK Wrappers))_
- **Why:** The whole-file `c8 ignore` claims 'thin AWS SDK wrapper, tested via integration', but the file is not a thin 3-5 line, no-branching wrapper: `rowToGeneratedSummary` (lines 47-79) and `readyFromRow` contain substantial branching mapping logic. CLAUDE.md only permits whole-file ignore for 'thin AWS SDK wrappers (3-5 lines, no branching)'. The SKILL explicitly says to extract mapping/parsing logic and unit-test the helper directly rather than relying on integration to plug coverage gaps — and `dynamodb-generated-summary.test.ts` already unit-tests this mapping with a fake client (asserting ready/pending/failed/skipped/legacy branches and the two asserts). Ignoring covered, branching logic both hides real coverage and contradicts the 'tested via integration' justification.
- **Verifier:** Line 1 ignore-start and line 133 ignore-stop wrap rowToGeneratedSummary's 5-way branching mapper (lines 47-79), and dynamodb-generated-summary.test.ts unit-tests every branch with a Partial fake client; SKILL.md (line 320-321) reserves whole-file ignore for trivial 3-5 line no-branching wrappers and says to extract+unit-test mapping logic. Reason string also mismatches the documented 'tested via production canaries'.

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */
```

### projects/hutch/src/runtime/providers/email  _(1)_

#### `projects/hutch/src/runtime/providers/email/log-email.ts`:5

- **Rule:** Raw console.log instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Logging) states: "Use HutchLogger from @packages/hutch-logger for all logging. Never use raw console.log/console.error in application code." This is a production provider (under runtime/providers, not a test), so the raw console.log is a direct violation. The logger should be injected (the init* function takes no deps and constructs no logger), and HutchLogger.from(consoleLogger) used at the composition root.
- **Verifier:** Snippet exact at line 5; log-email.ts is runtime application code (not a .test.ts and not under @packages/test-fixtures), uses raw console.log with no DI logger, while sibling in-memory providers in app.ts all take an injected logger — genuine Logging-rule violation.

```
console.log("[Email]", {
```

### projects/hutch/src/runtime/providers/google-auth  _(2)_

#### `projects/hutch/src/runtime/providers/google-auth/google-token.ts`:1,48

- **Rule:** c8 ignore without an approved reason _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Allowed c8 ignore Cases' enumerates the only sanctioned uses: thin AWS SDK wrappers, CI-only retry callbacks, V8 async-function artifacts, and V8 block-coverage phantoms. A Google OAuth token-exchange that also decodes a JWT id_token (split on '.', base64url decode, JSON.parse, two zod .parse calls) is none of these. It is not an AWS SDK wrapper, and it is not trivial/branch-free. The whole-file ignore suppresses coverage of real, testable logic, so it is a forbidden coverage-ignore comment used without an approved reason.
- **Verifier:** Snippet present at line 1 (stop at 48). The TDD skill only sanctions the whole-file ignore for 'truly trivial (3-5 lines, no branching)' wrappers; this file does JWT split/base64url-decode/JSON.parse + two zod .parse (lines 35-45), and 'thin Google API wrapper' is not an AWS-SDK wrapper, so it falls outside every sanctioned case.

```
/* c8 ignore start -- thin Google API wrapper, tested via integration */
```

#### `projects/hutch/src/runtime/providers/google-auth/google-token.ts`:16-47

- **Rule:** Thin wrapper must be unit-tested with an injected fake, not coverage-ignored _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The 'Thin AWS SDK Wrappers Are Unit-Tested With Fake Clients' guidance requires reaching 100% coverage with a fake client and extracting mapping/parsing logic into a helper to unit-test directly, rather than masking coverage gaps. Here fetch is already dependency-injected, so the JWT-claims parsing/mapping (lines 35-45) is fully unit-testable with a fake fetch. The code instead wraps everything in a c8 ignore claiming 'tested via integration', which is the masking pattern the skill prohibits.
- **Verifier:** fetch is injected via deps (lines 20,23) and the parse/map at 35-45 is exercisable with a fake fetch; no *.test.ts or *.integration.ts exists for this file. SKILL.md line 320 explicitly forbids plugging coverage gaps via integration and mandates extracting/unit-testing the parsing logic, which is exactly what the c8 ignore avoids.

```
fetch: typeof globalThis.fetch; ... const response = await deps.fetch("https://oauth2.googleapis.com/token", {
```

### projects/hutch/src/runtime/providers/oauth  _(2)_

#### `projects/hutch/src/runtime/providers/oauth/dynamodb-oauth-model.ts`:1-282

- **Rule:** c8 ignore restricted to trivial thin AWS SDK wrappers (no branching) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Allowed `c8 ignore` Cases) and test-driven-design SKILL.md allow a whole-file `c8 ignore` only for a 'truly trivial (3-5 lines, no branching)' thin AWS SDK wrapper. This file is not thin and contains substantial branching logic: a PKCE `assert`, expiry-check-and-delete paths in getAuthorizationCode/getAccessToken/getRefreshToken, the conditional emailVerified re-resolution on refresh (lines 224-229), the `if (refreshToken)` write (line 171), and the paginated do/while delete loop in revokeAllUserTokens (lines 257-274). Suppressing coverage over all of this hides untested branches rather than restructuring to remove them. The rule also requires the trivial-wrapper case to rely on production canaries, not the 'tested via integration' justification given here.
- **Verifier:** Whole-file ignore wraps lines 1-282 with PKCE assert (89), expiry+delete (115-118,188-189,212-213), emailVerified re-resolution (224-229) and a do/while pagination loop (257-274) — far from the 3-5 lines/no-branching the rule permits.

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */
```

#### `projects/hutch/src/runtime/providers/oauth/dynamodb-oauth-model.ts`:18,21,277-279

- **Rule:** No Default In-Memory Implementations / test-only code in production _(source: CLAUDE.md (No Default In-Memory Implementations) and test-driven-design SKILL.md)_
- **Why:** Production code (this DynamoDB OAuth model returned by the composition `init*` and wired in non-test code) imports `getClient` and `generateToken` from `@packages/test-fixtures`. `getClient` is invoked on every real `getClient`/`getAccessToken`/`getRefreshToken`/`getAuthorizationCode` call via rebuildClient, and `generateToken` backs the production token/code generators (lines 277-279). Test fixtures and in-memory implementations are for tests only; production must receive these from real injected dependencies at the composition root, not from a test-fixtures package.
- **Verifier:** Imports at lines 18 and 21 resolve to `@packages/test-fixtures` (package name confirmed); getClient backs rebuildClient (59) and generateToken backs the production generators (277-279), and initDynamoDbOAuthModel is wired live at app.ts:163.

```
import { getClient } from "@packages/test-fixtures/providers/oauth";
import { generateToken } from "@packages/test-fixtures/providers/oauth";
```

### projects/hutch/src/runtime/providers/password-reset  _(1)_

#### `projects/hutch/src/runtime/providers/password-reset/dynamodb-password-reset.ts`:13

- **Rule:** In-memory/test-fixture implementations are for tests only (production must not depend on test-fixtures) _(source: test-driven-design/SKILL.md)_
- **Why:** This is production runtime code (under src/runtime/, no Pulumi import) yet it pulls a Zod schema from the test-fixtures package. The test-driven-design skill states in-memory/fixture code is 'for tests only'; a production module importing from @packages/test-fixtures inverts the dependency direction and couples production to test-only code. The branded-token schema belongs in provider-contracts (where PasswordResetToken is already defined), not in test-fixtures.
- **Verifier:** Line 13 imports from @packages/test-fixtures (confirmed pkg name) where the schema sits beside in-memory-password-reset; production depending on test-fixtures violates 'in-memory implementations are for tests only' and the branded type already lives in provider-contracts.

```
import { PasswordResetTokenSchema } from "@packages/test-fixtures/providers/password-reset";
```

### projects/hutch/src/runtime/web/auth/test-helpers  _(1)_

#### `projects/hutch/src/runtime/web/auth/test-helpers/complete-stripe-signup.ts`:23-25

- **Rule:** No time/plan-bound comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound/time-bound comments — leftover implementation-plan notes and wording that assumes a moment in time ('new', 'recently', 'for now', 'currently'). This comment references migration phases and will rot when the code evolves; if the condition needs revisiting it should be encoded in code, not a comment.
- **Verifier:** Snippet matches lines 22-25 exactly; 'Phase 1 removed', 'now', 'Phase 2 will create' are textbook plan-bound/time-bound wording the rule explicitly prohibits.

```
Phase 1 removed Stripe checkout from `POST /signup` (it's a no-card trial
 * now), so this helper no longer drives through the signup form. The
 * `/auth/checkout/success` endpoint remains live — Phase 2 will create
 * pending signups via `POST /account/subscribe` to feed it again.
```

### projects/hutch/src/runtime/web/onboarding  _(1)_

#### `projects/hutch/src/runtime/web/onboarding/onboarding.template.html`:8

- **Rule:** No emojis in UI _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES Tone Rules and Do's/Don'ts state "No emojis in UI. Emojis are fine in social posts or community replies, never in the product interface." The check-mark emoji (✅) is rendered in a product success message, which the rule forbids without exception.
- **Verifier:** Line 8 verbatim contains the ✅ emoji in a product success heading; BRAND_GUIDELINES lines 271/333 forbid emojis in UI with no exception.

```
<h2 class="onboarding__success-title">You did it! ✅</h2>
```

### projects/hutch/src/runtime/web/pages/account  _(2)_

#### `projects/hutch/src/runtime/web/pages/account/account.template.html`:20-32

- **Rule:** Forms Everywhere — Don't Split Items Into <a> vs <form> (no isLink discriminator, no template branch on it) _(source: web/SKILL.md)_
- **Why:** The web skill explicitly forbids adding an `isLink: method === "GET"` discriminator and branching the action template on it (section 'Forms Everywhere — Don't Split Items Into <a> vs <form>'). Every item must render as a `<form><button>`; a method=GET form behaves like a link. The template branches on `isLink` to choose `<a>` vs `<form>`, the exact anti-pattern the rule names.
- **Verifier:** Template lines 21-31 contain the exact {{#if isLink}}/<a>/{{else}}/<form> branch that SKILL.md line 85 forbids verbatim ('No {{#if isLink}} / {{else}} branch').

```
{{#each actions}}
{{#if isLink}}
<a class="account-card__action ..." href="{{href}}" ...>{{name}}</a>
{{else}}
<form method="POST" action="{{href}}" ...>
```

#### `projects/hutch/src/runtime/web/pages/account/account.view-model.ts`:28,73-79

- **Rule:** Forms Everywhere — Don't Split Items Into <a> vs <form> (no isLink discriminator) _(source: web/SKILL.md)_
- **Why:** The web skill names `isLink: method === "GET"` as the discriminator that must not be introduced. `AccountAction.isLink` is computed as `input.method === "GET"` and consumed only to branch the template between `<a>` and `<form>`. The field and the GET branch should be deleted in favour of a single `<form>` shape.
- **Verifier:** Line 28 declares isLink: boolean and line 77 computes isLink: input.method === "GET" — exactly the discriminator SKILL.md line 81 names as forbidden.

```
isLink: boolean; ... function action(input: Omit<AccountAction, "isLink">): AccountAction {
	return { ...input, href: ..., isLink: input.method === "GET" };
}
```

### projects/hutch/src/runtime/web/pages/admin  _(1)_

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.page.ts`:136-137

- **Rule:** No Side Effects on GET _(source: .claude/skills/web/SKILL.md)_
- **Why:** web SKILL 'No Side Effects on GET': 'Never mutate state on a GET — proxies cache them, prefetchers fire them, crawlers hit them.' This handler is wired as router.get('/*splat', ...) yet forces the crawl row back to pending and publishes a recrawl event — two state mutations on a GET. The skill prescribes an auto-submitting POST form or POST-Redirect-GET for mutation-triggering URLs.
- **Verifier:** Lines 136-137 match; handler is registered via router.get('/*splat', handleRecrawlArticle) at lines 234-237 and performs two state mutations — exactly the pattern web SKILL line 143 forbids.

```
await deps.forceMarkCrawlPending({ url: articleUrl });
			await deps.publishRecrawlLinkInitiated({ url: articleUrl });
```

### projects/save-link  _(1)_

#### `projects/save-link/src/runtime/providers/pdf-page-ocr/init-tesseract-ocr.ts`:17-18

- **Rule:** Direct process.env access + defaulted missing env var _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Environment Variable Access' forbids reading process.env directly (use requireEnv/getEnv) and forbids defaulting a missing env var ('Never default missing environment variables. Always use requireEnv and let the process fail'). This provider reads process.env.TESSDATA_PREFIX directly and silently falls back to a hardcoded path, exactly the 'silent default' the rule prohibits. It is provider code, not a composition root, and is not a Playwright config (the only documented exception).
- **Verifier:** Snippet matches lines 17-18 exactly; reads process.env directly and defaults via ?? to a hardcoded path. Provider code, not the Playwright-config exception.

```
export function resolveTessdataDir(env: NodeJS.ProcessEnv = process.env): string {
	return env.TESSDATA_PREFIX ?? "/opt/tesseract/tessdata";
```

### projects/web-embed  _(1)_

#### `projects/web-embed/src/runtime/embed/embed.component.ts`:12-52

- **Rule:** Browser JS must be bundled and served same-origin via <script src>, not inlined _(source: web/SKILL.md)_
- **Why:** The web skill's 'Browser JS Is Bundled and Served Same-Origin' section requires client behaviour to be compiled to a *.client.js IIFE bundle referenced via a same-origin `<script src="/...">`, and the pre-commit checklist states browser JS must be 'bundled and referenced via a same-origin <script src>, not inlined'. Here the copy-to-clipboard / URL-substitution behaviour is built as an inline `<script>` string and injected via the template ({{{scripts}}}), exactly the inline pattern the skill forbids.
- **Verifier:** COPY_SCRIPT exists at lines 12-52 verbatim, is real browser behaviour (clipboard + URL substitution) inlined as a <script> string and passed as `scripts: COPY_SCRIPT` (line 94) into EmbedBase, never bundled to a same-origin script src as the SKILL section mandates.

```
const COPY_SCRIPT = `<script>
(function() {
  var buttons = document.querySelectorAll('[data-copy]');
```

### src/packages/check-failed-articles  _(3)_

#### `src/packages/check-failed-articles/scripts/check-failed-articles.ts`:39-43

- **Rule:** Direct process.env access / local re-implementation instead of shared requireEnv _(source: CLAUDE.md)_
- **Why:** CLAUDE.md ("Environment Variable Access") mandates: "Use requireEnv and getEnv from projects/hutch/src/require-env.ts. Never use process.env directly." This file re-implements requireEnv with a raw process.env[name] lookup instead of using the shared helper, accessing process.env directly. The documented playwright.config exception does not apply (this is a node:test canary script).
- **Verifier:** Snippet exists verbatim at lines 39-43; rule absolutely forbids direct process.env and mandates the shared helper, and the only documented exception (playwright.config) does not apply to this node:test canary.

```
function requireEnv(name: string): string {
	const value = process.env[name];
	assert(value, `${name} env var is required`);
	return value;
}
```

#### `src/packages/check-failed-articles/scripts/check-failed-articles.ts`:46

- **Rule:** Direct process.env access + defaulting a missing environment variable _(source: CLAUDE.md)_
- **Why:** Two violations of CLAUDE.md "Environment Variable Access": (1) reads process.env directly rather than via getEnv/requireEnv; (2) supplies a silent default (?? "0") for a missing env var, which the rule forbids: "Never default missing environment variables... Do not use getEnv with a fallback to work around CI environments missing secrets."
- **Verifier:** Snippet matches line 46 exactly; the direct process.env access is a clear literal violation. The ?? "0" default is a deliberate optional-config knob (not a missing-secret workaround), so that half is weaker, but the direct-access half alone confirms the finding.

```
const raw = process.env.FAILED_ARTICLES_LOOKBACK_DAYS ?? "0";
```

#### `src/packages/check-failed-articles/scripts/check-failed-articles.ts`:56

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Environment Variable Access" forbids reading process.env directly; an optional variable must be read via getEnv from projects/hutch/src/require-env.ts. The playwright.config exception does not apply here.
- **Verifier:** Snippet matches line 56 exactly; optional var read via raw process.env instead of the mandated getEnv helper, and no documented exception applies to this canary script.

```
const reportPath = process.env.FAILED_ARTICLES_REPORT_PATH;
```

### src/packages/check-stuck-articles  _(2)_

#### `src/packages/check-stuck-articles/scripts/check-stuck-articles.ts`:32-36

- **Rule:** Direct process.env access (must use requireEnv/getEnv) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Environment Variable Access' states: 'Use requireEnv and getEnv from projects/hutch/src/require-env.ts. Never use process.env directly.' This file re-implements its own requireEnv reading process.env[name] directly rather than importing the canonical helper. The only documented exception is playwright.config.*.ts files, which this is not.
- **Verifier:** Snippet matches lines 32-36 exactly; reads process.env[name] directly, not the Playwright exception, so the rule's letter is violated.

```
function requireEnv(name: string): string {
	const value = process.env[name];
	assert(value, `${name} env var is required`);
	return value;
}
```

#### `src/packages/check-stuck-articles/scripts/check-stuck-articles.ts`:60

- **Rule:** Direct process.env access (must use requireEnv/getEnv) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md forbids reading process.env directly ('Never use process.env directly'); STUCK_ARTICLES_REPORT_PATH should be read via getEnv. This is an optional var (used as a CI-only toggle), so the documented pattern is getEnv, not a direct process.env read.
- **Verifier:** Line 60 matches verbatim; optional env read straight off process.env, the exact pattern getEnv exists to replace, no applicable exception.

```
const reportPath = process.env.STUCK_ARTICLES_REPORT_PATH;
```

### src/packages/domain  _(3)_

#### `src/packages/domain/src/oauth/oauth.schema.ts`:4-10

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** All four branded-ID schemas cast with `as` inside a `.transform()`. CLAUDE.md 'Avoid TypeScript Type Assertions' forbids `as` for branding and gives the exact GOOD pattern `z.string().brand<'UserId'>()` + `z.infer`. This is not an allowed exception (not `as const`, not an isolated Node API wrapper) and the same package already does it correctly: import-session.schema.ts uses `.brand<"ImportSessionId">()` and saveable-url.ts uses `z.string().brand<"SaveableUrl">()`. The `as` silently hides any future mismatch between the schema output and the brand type.
- **Verifier:** Snippet exact at lines 4,6,8,10; rule forbids `as` for branding; not an exception; same package uses `.brand<>()` correctly.

```
export const OAuthClientIdSchema = z.string().transform((s): OAuthClientId => s as OAuthClientId);
export const AccessTokenSchema = z.string().transform((s): AccessToken => s as AccessToken);
export const RefreshTokenSchema = z.string().transform((s): RefreshToken => s as RefreshToken);
export const AuthorizationCodeSchema = z.string().transform((s): AuthorizationCode => s as AuthorizationCode);
```

#### `src/packages/domain/src/user/user.schema.ts`:4

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `UserId` is the canonical example used in CLAUDE.md's GOOD pattern (`z.string().brand<'UserId'>()`). Here it is branded with `as UserId` instead, which is the BAD pattern the doc explicitly contrasts. Should be `z.string().brand<"UserId">()` with the type derived via `z.infer`.
- **Verifier:** Line 4 matches exactly; UserId is the literal example in CLAUDE.md's BAD/GOOD contrast; uses `as` instead of `.brand<>()`.

```
export const UserIdSchema = z.string().transform((s: string): UserId => s as UserId);
```

#### `src/packages/domain/src/user/user-id-prefix.ts`:13

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** Branded-ID schema cast with `as UserIdPrefix` inside `.transform()`. CLAUDE.md mandates `z.string()...brand<"UserIdPrefix">()` instead of an `as` assertion; this is not an allowed exception.
- **Verifier:** Line 13 matches; `.brand<>()` is the sanctioned mechanism here, not an inline `as` in transform.

```
.transform((s): UserIdPrefix => s as UserIdPrefix);
```

### src/packages/test-fixtures  _(1)_

#### `src/packages/test-fixtures/src/providers/oauth/oauth-model.ts`:274-276

- **Rule:** Time/plan-bound comment (TODO, "Currently", plan-bound "when scopes are added") _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids time-bound/plan-bound comments. This comment is a plan-bound TODO that 'assumes a particular moment in time' ("Currently", "when scopes are added", "Once scopes are added") and describes WHAT the function does rather than WHY. The doc says such notes rot when code changes and that a condition needing revisiting should be 'encoded in code (a failing test, a feature flag with an owner, a tracked issue) rather than in a comment'.
- **Verifier:** Snippet exact at lines 274-276; uses TODO + 'Currently'/'Once scopes are added' which CLAUDE.md explicitly bans (matches its 'POST coming soon' / 'Temporary: remove after migration' examples).

```
// TODO: Update this function when scopes are added. Currently returns true
// because no scopes are implemented. Once scopes are added, this must
// validate that the token has the requested scope(s).
```

### src/packages/test-phase-runner  _(4)_

#### `src/packages/test-phase-runner/src/run-test-phases.ts`:157

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Environment Variable Access" states "Never use process.env directly" and to use requireEnv/getEnv. The only documented exception is playwright.config.*.ts files; this is run-test-phases.ts. CLAUDE_CODE_REMOTE should be read via getEnv.
- **Verifier:** Snippet exact at line 157; this is not a playwright.config file so the sole documented process.env exception does not apply.

```
shouldSkipE2E: () => process.env.CLAUDE_CODE_REMOTE === "true",
```

#### `src/packages/test-phase-runner/src/run-test-phases.ts`:166

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Environment Variable Access" forbids direct process.env access outside playwright.config.*.ts files. This module reads the full process.env directly rather than through the require-env helpers.
- **Verifier:** Snippet exact at line 166; reads full process.env directly outside the playwright.config exception.

```
env: { ...process.env, ...options.extraEnv },
```

#### `src/packages/test-phase-runner/src/run-test-phases.ts`:191

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Environment Variable Access" forbids reading process.env directly; CI should be read via getEnv. Not a playwright.config file, so the documented exception does not apply.
- **Verifier:** Snippet exact at line 191; direct process.env.CI read, not within the playwright.config exception.

```
const isCI = process.env.CI === "true";
```

#### `src/packages/test-phase-runner/src/run-test-phases.ts`:203

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Environment Variable Access" forbids direct process.env access outside playwright.config.*.ts files. The Playwright phase spreads process.env directly instead of using the require-env helpers.
- **Verifier:** Snippet exact at line 203; spreads process.env directly in the playwright phase runner (a .ts module, not a playwright.config file).

```
env: { ...process.env, ...phase.env },
```

## 🟡 Medium severity (122)

### projects/blog-site  _(2)_

#### `projects/blog-site/src/runtime/lambda.main.ts`:31

- **Rule:** Raw console.log instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Logging" forbids raw console.log/console.error and states that even composition roots must build a logger with `HutchLogger.from(consoleLogger)` and log through it. This entry point (a composition root) logs startup with raw console.log instead of HutchLogger.
- **Verifier:** Line 31 is exactly the cited raw console.log in a composition-root entry point; Logging rule explicitly requires HutchLogger.from(consoleLogger) even in composition roots.

```
console.log(`blog-site is running on http://localhost:${PORT}`);
```

#### `projects/blog-site/src/runtime/server.main.ts`:10

- **Rule:** Raw console.log instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Logging" forbids raw console in application code and requires composition roots to use `HutchLogger.from(consoleLogger)`. The local-server composition root logs with raw console.log instead of HutchLogger.
- **Verifier:** Line 10 matches the cited snippet verbatim in a composition-root entry point; same Logging-rule violation as lambda.main.ts.

```
console.log(`blog-site is running on http://localhost:${PORT}`);
```

### projects/browser-extension-core  _(5)_

#### `projects/browser-extension-core/src/reading-list/siren-reading-list.ts`:183

- **Rule:** Avoid TypeScript type assertions (as) / Branded types via Zod _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' and 'Branded Types' require constructing a branded ID via a Zod .brand() parse at the boundary, not a bare 'as'. props is parsed by SirenPropertiesSchema (id: z.string()), so the id should be branded in the schema (z.string().brand<'ReadingListItemId'>()) rather than cast with 'as ReadingListItemId'.
- **Verifier:** Confirmed at line 183: SirenPropertiesSchema.id is z.string() and the result is cast with 'as ReadingListItemId' at a Siren-response boundary — exactly the case CLAUDE.md says to handle with z.string().brand<>() instead of 'as'.

```
id: props.id as ReadingListItemId,
```

#### `projects/browser-extension-core/src/core.ts`:78

- **Rule:** Avoid TypeScript type assertions (as) — faking objects instead of satisfies/typed value _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' forbids 'as' to coerce an object literal into a type. Building a CoreError via 'as' bypasses the compiler; a typed variable or 'satisfies CoreError' would catch shape mismatches. Same pattern repeats on lines 87, 91 and 143.
- **Verifier:** Confirmed at line 78: after the !ok guard, guardedResult.reason is 'not-logged-in'|'error', so { reason } is NOT assignable to CoreError (the 'error' variant requires an error field) — the 'as' masks exactly the object-literal shape mismatch the rule warns about.

```
eventBus.emit(event, "failure", { reason: guardedResult.reason } as CoreError);
```

#### `projects/browser-extension-core/src/core.ts`:202-220

- **Rule:** Avoid TypeScript type assertions (as) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)': handler is cast to ResultCallbacks<unknown> and the event-bus 'value' is cast to CoreError (lines 202, 207, 215, 220). These assertions bypass the compiler on values coming from the untyped event bus; the rule lists no exception for this case.
- **Verifier:** Confirmed: 'handler as ResultCallbacks<unknown>' (lines 202, 215) and 'value as CoreError' (lines 207, 220) exist as quoted; both cast untyped (any / event-bus unknown) values, matching no allowed 'as' exception in CLAUDE.md.

```
const resultHandler = handler as ResultCallbacks<unknown>; ... resultHandler.failure(value as CoreError);
```

#### `projects/browser-extension-core/src/zod-config.ts`:4

- **Rule:** Time/plan-bound comment _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' bans plan/task-bound comments — 'leftover notes from an implementation plan' and 'references to the current task or PR'. This 'test 2/4 probe' note is a task/plan artifact that will rot and has no bearing on the module's behaviour.
- **Verifier:** Confirmed verbatim at line 4 — a 'test 2/4 probe' plan-artifact comment unrelated to the module's jitless purpose, exactly the leftover-plan-note category CLAUDE.md forbids.

```
// (test 2/4 probe: extension-core edit must publish BOTH chrome and firefox)
```

#### `projects/browser-extension-core/enforce-coverage.config.js`:14-19

- **Rule:** No individual files in coverage extraExcludePatterns _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Allowed c8 ignore Cases' / 'Never Add Excludes ... Without Approval' states: 'do NOT add individual files to enforce-coverage.config.js extraExcludePatterns. Config excludes are for whole directories (e.g., src/infra/**).' 'src/core.ts' is an individual file added to the coverage excludes; a wiring module should reach 100% via integration tests or be restructured, not file-excluded.
- **Verifier:** Confirmed: extraExcludePatterns contains the individual file 'src/core.ts' (lines 14-18), which CLAUDE.md explicitly forbids — config excludes must be whole directories.

```
extraExcludePatterns: [ 'src/core.ts', 'src/e2e-actions/**' ]
```

### projects/extensions/chrome-extension  _(3)_

#### `projects/extensions/chrome-extension/src/runtime/popup/popup.browser.ts`:449

- **Rule:** Avoid TypeScript type assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (`as`)' bans casts outside the allowed exceptions (as const, isolated Node API wrappers). Widening `navigator` via `as` to bolt on `userAgentData` bypasses the compiler; a `declare global` interface augmentation would type the field without an assertion.
- **Verifier:** Line 449 matches exactly; widening navigator via `as` is not as-const nor an isolated Node wrapper, so it falls under the general `as` ban.

```
const nav = navigator as Navigator & { userAgentData?: { platform: string } };
```

#### `projects/extensions/chrome-extension/src/runtime/background/tinted-icon.browser.ts`:53-56

- **Rule:** Avoid TypeScript type assertions (`as`) at a boundary where a Zod schema belongs _(source: CLAUDE.md)_
- **Why:** The response of browser.runtime.sendMessage is `unknown` crossing a message boundary; casting it with `as` (instead of validating the SerializedImageData shape with Zod, per CLAUDE.md 'Avoid TypeScript Type Assertions') hides any malformed payload. The same untrusted-`as` pattern recurs at line 72/74 (`as Theme`).
- **Verifier:** Lines 53-56 match and `as Theme` confirmed at line 75; sendMessage returns unknown across a runtime-messaging boundary cast without validation, not an allowed exception.

```
const rawData = (await browser.runtime.sendMessage({ target: "offscreen", type: "get-saved-icon-data" })) as Record<number, SerializedImageData>;
```

#### `projects/extensions/chrome-extension/src/runtime/popup/popup.browser.ts`:49

- **Rule:** Avoid TypeScript type assertions (`as`) at a boundary where a Zod schema belongs _(source: CLAUDE.md)_
- **Why:** The onMessage listener receives `raw` typed `unknown` from the runtime messaging boundary and casts it to PopupMessage with `as` before reading `message.type`/`message.phase`. CLAUDE.md prescribes a Zod schema (.safeParse for user-facing, .parse when invalid means a bug) at system boundaries instead of an assertion. The same boundary-`as` pattern appears throughout background.browser.ts (e.g. `raw as PopupMessage`, `raw as { type: string }`).
- **Verifier:** Line 49 matches; raw arrives unknown from runtime.onMessage and is cast to PopupMessage before reading .type/.phase. Recurrence confirmed in background.browser.ts at lines 194, 199, 203.

```
const message = raw as PopupMessage;
```

### projects/extensions/firefox-extension  _(5)_

#### `projects/extensions/firefox-extension/src/runtime/background/background.browser.ts`:30-34

- **Rule:** Avoid type assertions (`as`) at a boundary where Zod belongs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' requires validating external/system-boundary data with a Zod schema (`z...parse`) rather than `as`. browser.storage.local.get returns untyped data; casting it to the domain type OAuthTokens with `as` silently hides any shape mismatch.
- **Verifier:** Lines 30-34 match; browser.storage.local.get returns untyped data and is force-cast to the domain OAuthTokens via `as`, exactly the boundary-cast pattern (cf. the `dbResult.Item.url as string` BAD example) the rule says to replace with a Zod parse.

```
const raw = result[STORAGE_KEY];
		if (!raw) return null;
		return raw as OAuthTokens;
```

#### `projects/extensions/firefox-extension/src/runtime/background/background.browser.ts`:205-210

- **Rule:** Avoid type assertions (`as`) at a boundary where Zod belongs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' — runtime.onMessage delivers `unknown` from an external sender; casting it to `{ type: string }` and `PopupMessage` with `as` bypasses validation. The rule directs using a Zod schema at the HTTP/message boundary instead of `as`.
- **Verifier:** Lines 205 and 210 match; `raw` arrives as untyped onMessage payload and is force-cast twice via `as` with no runtime validation, the cast pattern the rule prohibits.

```
if ((raw as { type: string }).type === "shortcut-pressed") {
...
	const message = raw as PopupMessage;
```

#### `projects/extensions/firefox-extension/src/runtime/background/background.browser.ts`:192-194

- **Rule:** Avoid type assertions (`as`) at a boundary where Zod belongs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' — the content-script sendMessage response is untyped external input; it is narrowed via `as { rawHtml: unknown }` rather than parsed with a Zod schema at the boundary.
- **Verifier:** Lines 192-194 match; the sendMessage response is runtime-guarded with typeof/'in' but then force-cast via `as { rawHtml: unknown }` to read the field — the rule treats such boundary casts as violations even though a guard precedes them.

```
if (captured && typeof captured === "object" && "rawHtml" in captured) {
			const rawHtml = (captured as { rawHtml: unknown }).rawHtml;
```

#### `projects/extensions/firefox-extension/src/runtime/popup/popup.browser.ts`:48

- **Rule:** Avoid type assertions (`as`) at a boundary where Zod belongs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' — runtime.onMessage payload is `unknown` from an external sender; casting to PopupMessage with `as` skips boundary validation that should be a Zod parse.
- **Verifier:** Line 48 matches; `raw` from runtime.onMessage is force-cast to PopupMessage with no validation before reading message.type/message.phase.

```
const message = raw as PopupMessage;
```

#### `projects/extensions/firefox-extension/src/runtime/popup/popup.browser.ts`:221-224

- **Rule:** Avoid type assertions (`as`) at a boundary where Zod belongs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' — send() returns Promise<unknown> (an IPC boundary response); casting the untyped reply to a domain result type with `as` (also at lines 256, 346, 367, 405) bypasses validation the rule says belongs in a Zod schema at the boundary.
- **Verifier:** Line 221-224 matches; send() is typed Promise<unknown> (line 70) and the reply is force-cast to GuardedResult<RemoveUrlResult>; the same pattern recurs at 255-257, 343-346, 362-367 and 405-409, all casting untyped IPC replies.

```
const result = (await send({
					type: "remove-item",
					id: item.id,
				})) as GuardedResult<RemoveUrlResult>;
```

### projects/hutch/src/e2e/queue-flow  _(2)_

#### `projects/hutch/src/e2e/queue-flow/auth-actions.ts`:43-44

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `(el as HTMLInputElement)` is a type assertion used to reach `.value`. CLAUDE.md forbids `as` outside the allowed exceptions (as const; isolated Node API wrappers); a DOM cast in a Playwright `evaluate` callback is neither. The compiler-safe alternative is to type the callback parameter via the generic, e.g. `.evaluate((el: HTMLInputElement) => { el.value = ... })`, so an upstream type change surfaces as a compile error instead of being silently hidden.
- **Verifier:** Snippet matches lines 43-44 exactly; it is a real `as` cast, not `as const` nor an isolated Node-API wrapper, so it falls under the prohibition.

```
await page.locator('input[name="loadedAt"]').evaluate(
  (el) => { (el as HTMLInputElement).value = String(Date.now() - 5000) },
)
```

#### `projects/hutch/src/e2e/queue-flow/queue-actions.ts`:144-148

- **Rule:** Allowed `c8 ignore` Cases / No Coverage Ignore Comments without an approved reason _(source: CLAUDE.md)_
- **Why:** This inline `/* c8 ignore next */` carries no reason and does not map to any approved category (thin AWS SDK wrapper, CI-only retry callback, V8 async artifact, or V8 block-coverage phantom with a bcoe/c8#319 / v8.dev reference). CLAUDE.md states coverage-ignore comments are forbidden unless explicitly approved, and when necessary must carry an inline reason. Contrast with the `beforeRetry` ignore at line 337 in the same file, which does document its CI-only reason.
- **Verifier:** Confirmed at line 148: bare `/* c8 ignore next */` with no reason text and no mapping to an approved category, violating the inline-reason requirement.

```
.getAttribute('href')
.catch(/* c8 ignore next */ () => null)
```

### projects/hutch/src/infra  _(1)_

#### `projects/hutch/src/infra/index.ts`:278

- **Rule:** Data-Driven Over Environment Conditionals _(source: .claude/skills/infrastructure-design/SKILL.md)_
- **Why:** Infrastructure code branches on the environment name (stage === "production") to derive a Lambda env var. The infrastructure-design rule states infrastructure code must not branch on environment names (e.g., `if (stage === "production")`); environment differences must be expressed as config values in each environment's YAML and read via Pulumi's config API. NODE_ENV should be a config value (e.g. config.require("nodeEnv")) per-environment rather than computed from the stage name.
- **Verifier:** Snippet verified verbatim at line 278; stage = config.require("stage") is the env name, and the ternary on stage==="production" is the exact anti-pattern the rule names.

```
NODE_ENV: stage === "production" ? "production" : "development",
```

### projects/hutch/src/runtime  _(2)_

#### `projects/hutch/src/runtime/app.ts`:316, 545

- **Rule:** getEnv with a fallback default _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Environment Variable Access) states "Do not use getEnv with a fallback (e.g., getEnv(\"KEY\") ?? \"\") ... Never default missing environment variables." Lines 316 and 545 use `getEnv("PORT") || "3000"`, which is the forbidden getEnv-with-fallback pattern.
- **Verifier:** Line 545 matches the cited snippet exactly and line 316 uses `getEnv("PORT") || "3000"` in a redirectUri template; both are the forbidden getEnv-with-fallback pattern per CLAUDE.md Environment Variable Access.

```
const port = getEnv("PORT") || "3000";
```

#### `projects/hutch/src/runtime/conversions.test.ts`:50, 71, 190-199, 235

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The test-driven-design SKILL (Avoid Negative Test Assertions) says "Negative assertions (.not.toContain()) become stale when code is refactored" and to assert the actual expected value instead. Multiple assertions here use `.not.toContain(...)` against the serialized event rather than asserting the concrete emitted object shape (e.g. expect(captured[0]).toEqual({...}) without the omitted keys).
- **Verifier:** SKILL.md:277-287 names `.not.toContain()` as the exact anti-pattern; lines 50, 71, 190-198, and 235 all use `.not.toContain(...)` on the serialized event instead of asserting the concrete object shape (line 41's toEqual shows the positive form is available).

```
expect(JSON.stringify(captured[0])).not.toContain("stripe_checkout_session_id");
```

### projects/hutch/src/runtime/cancel-subscription  _(1)_

#### `projects/hutch/src/runtime/cancel-subscription/cancel-subscription-handler.test.ts`:90

- **Rule:** Comments must document why, not what; no time/plan-bound or task/PR-referencing comments _(source: CLAUDE.md)_
- **Why:** "that's the regression we're fixing" is a plan/task-bound comment that assumes the current moment and references the in-flight change. CLAUDE.md forbids wording tied to the current task/PR or a particular moment in time because it rots once the fix lands; the assertion itself already encodes the expected behaviour.
- **Verifier:** Confirmed: line 90 matches the snippet exactly; 'that's the regression we're fixing' is task/moment-bound wording that CLAUDE.md explicitly forbids, and the deepEqual assert on the next line already encodes the behaviour.

```
// No immediate Stripe DELETE — that's the regression we're fixing.
```

### projects/hutch/src/runtime/checkout-recovery  _(1)_

#### `projects/hutch/src/runtime/checkout-recovery/send-checkout-recovery-emails.cli.main.ts`:93

- **Rule:** Raw console.error instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** The Logging rule states: 'Never use raw console.log/console.error in application code... In composition roots, create the logger with HutchLogger.from(consoleLogger).' The file already builds a HutchLogger inside main() (line 22); the top-level catch handler instead uses raw console.error. A module-scope HutchLogger.from(consoleLogger) should be used here too.
- **Verifier:** Line 93 is verbatim raw console.error in a composition root; the file itself builds HutchLogger at line 22, so the rule applies and no exception covers an entry-point catch.

```
console.error("[recovery] Fatal:", err);
```

### projects/hutch/src/runtime/export-user-data  _(2)_

#### `projects/hutch/src/runtime/export-user-data/export-user-data-handler.test.ts`:86

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids `as` casts outside the allowed exceptions (`as const`, isolated Node wrappers). Faking the Lambda context object with `{} as never` bypasses the compiler; the doc directs using `Partial<T>` to make a subset explicit instead.
- **Verifier:** Snippet verified at line 86 verbatim (also repeats at 207, 266); `as never` is a type assertion, not `as const` nor an isolated Node wrapper, and the rule's own createFakeResponse example prescribes Partial<T> for exactly this fake-object case.

```
harness.handler(createSqsEvent(detail), {} as never, () => {})
```

#### `projects/hutch/src/runtime/export-user-data/export-user-data-handler.test.ts`:95, 105

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' and 'Branded Types for Domain IDs' call for a validated factory / zod brand parse (e.g. UserIdSchema.parse) rather than asserting a raw literal into a branded type with `as`. The same `as UserId` / `as Minutes` pattern repeats at lines 159, 176, 219.
- **Verifier:** Verified `"user-1" as UserId` at line 95 and `1 as Minutes` at line 105; the rule explicitly lists `rawValue as UserId` as the BAD pattern and prescribes a validated brand parse, with no test-code exemption. Repeats confirmed at 146/159, 176, 219.

```
const userId = "user-1" as UserId; ... estimatedReadTime: 1 as Minutes
```

### projects/hutch/src/runtime/providers/article-store  _(2)_

#### `projects/hutch/src/runtime/providers/article-store/batch-get-from-table.test.ts`:61, 63

- **Rule:** Avoid TypeScript type assertions (`as`) / faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** The `as unknown as SendFn` double-cast and `fake as DynamoDBDocumentClient` bypass the compiler entirely. CLAUDE.md forbids using `as` and specifically calls out faking a partial object with `as` (the GOOD example uses `Partial<T>`). The file already builds `fake: Partial<DynamoDBDocumentClient>`, then discards that safety with a force cast to the full type instead of injecting the Partial directly.
- **Verifier:** Verified: line 39 declares `fake: Partial<DynamoDBDocumentClient>`, line 61 `as unknown as SendFn`, line 63 `fake as DynamoDBDocumentClient`; matches CLAUDE.md's faking-with-as anti-pattern and no exception covers it.

```
}) as unknown as SendFn, ... return { client: fake as DynamoDBDocumentClient, calls };
```

#### `projects/hutch/src/runtime/providers/article-store/dynamodb-article-store.test.ts`:5

- **Rule:** Avoid type assertions (`as`) — use a validated factory (Zod brand parse) for branded domain IDs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md's 'Avoid TypeScript Type Assertions' shows the exact anti-pattern `const userId = rawValue as UserId` and prescribes a validated factory (`UserIdSchema.parse(rawValue)`). The production code (dynamodb-article-store.ts) imports `UserIdSchema`, so a parse-based construction is available; the `as UserId` cast bypasses the brand's validation.
- **Verifier:** Verified: line 5 reads `const USER = "abc123" as UserId;` matching the exact CLAUDE.md anti-pattern, and UserIdSchema is exported from @packages/domain/user (imported by the production store at line 15).

```
const USER = "abc123" as UserId;
```

### projects/hutch/src/runtime/providers/auth  _(4)_

#### `projects/hutch/src/runtime/providers/auth/backfill-registered-at.cli.main.ts`:22

- **Rule:** Raw console instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md Logging: "Never use raw console.log/console.error in application code." This CLI main is application code; it should construct a HutchLogger via HutchLogger.from(consoleLogger) and log through it.
- **Verifier:** Snippet exact at line 22; raw console.log in a main() entry point. CLAUDE.md explicitly tells composition roots/entry points to use HutchLogger.from(consoleLogger); the c8-ignore wrapper is for coverage, not a logging exception.

```
console.log(`Backfilling registeredAt = ${now} on table ${tableName}`);
```

#### `projects/hutch/src/runtime/providers/auth/backfill-registered-at.cli.main.ts`:53-55

- **Rule:** Raw console instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md Logging rule forbids raw console.log in application code; use HutchLogger.
- **Verifier:** Snippet exact at lines 53-55; same raw console.log violation in the same CLI main as finding 1.

```
console.log(
		`Done. Scanned ${items.length} rows, set registeredAt on ${updatedTotal}, ${alreadySetTotal} already had a value.`,
	);
```

#### `projects/hutch/src/runtime/providers/auth/backfill-registered-at.cli.main.ts`:59

- **Rule:** Raw console instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md Logging rule: raw console.error is forbidden in application code; route through HutchLogger.
- **Verifier:** Snippet exact at line 59; raw console.error in the entry point's catch handler. Rule explicitly names console.error as forbidden.

```
console.error("Backfill failed:", err);
```

#### `projects/hutch/src/runtime/providers/auth/dynamodb-auth.test.ts`:51

- **Rule:** as cast to branded type instead of schema.parse _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions': casting a raw string to a branded id with `as` is the exact anti-pattern shown (`rawValue as UserId`). Use `UserIdSchema.parse("abc123")`. This is not one of the allowed exceptions (as const / isolated Node wrapper).
- **Verifier:** Snippet exact at line 51; `"abc123" as UserId` mirrors the rule's BAD example verbatim. Not as-const nor an isolated Node wrapper; the rule lists no test-file exception.

```
const USER = "abc123" as UserId;
```

### projects/hutch/src/runtime/providers/email-verification  _(1)_

#### `projects/hutch/src/runtime/providers/email-verification/dynamodb-email-verification.ts`:1-82

- **Rule:** Whole-file c8 ignore reserved for trivial (3-5 lines, no branching) AWS wrappers; branching/mapping logic must be extracted and unit-tested with a fake client _(source: .claude/skills/test-driven-design/SKILL.md (Thin AWS SDK Wrappers Are Unit-Tested With Fake Clients) + CLAUDE.md (Allowed c8 ignore Cases))_
- **Why:** The entire file is c8-ignored as a 'thin AWS SDK wrapper', but verifyEmailToken contains real branching/domain logic: the missing-Attributes check (line 59), the token-expiry comparison (line 63), and the ConditionalCheckFailedException catch (lines 73-76). The SKILL states whole-file c8 ignore is only for wrappers that are 'truly trivial (3-5 lines, no branching)' and that mapping/branching logic must instead be extracted into a helper and unit-tested with the Partial<DynamoDBDocumentClient> fake-client pattern to reach 100% coverage ('Do NOT add a .integration.ts to plug coverage gaps'). Blanket-ignoring this branching hides untested logic.
- **Verifier:** Snippets verified verbatim: whole-file c8 ignore (line 1) wraps verifyEmailToken which has three real branches (lines 59, 63, 73) plus a re-throw; not trivial 3-5 line no-branch wrapper, production code, no exception applies.

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */ ... if (!Attributes) { return { ok: false, reason: "invalid-token" }; } if (Attributes.expiresAt < Math.floor(Date.now() / 1000)) { ... } catch (error) { if (error instanceof ConditionalCheckFailedException) { ... } }
```

### projects/hutch/src/runtime/providers/google-auth  _(1)_

#### `projects/hutch/src/runtime/providers/google-auth/google-token.ts`:3

- **Rule:** Production code depends on a test-fixtures package _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** GoogleIdSchema (the branded domain schema used to validate claims.sub at runtime in production) is imported from @packages/test-fixtures, a package whose role is test fixtures (it ships google-auth.schema.test.ts and fixture exports). The TDD skill scopes in-memory/test artifacts to tests only and warns against shipping test-only code into production paths; a production runtime branded-ID schema belongs in a domain/contracts package (e.g. provider-contracts), not in test-fixtures, otherwise production now hard-depends on a test package.
- **Verifier:** Import verified at line 3; GoogleIdSchema (a runtime zod schema, schema.ts line 6) is consumed by production google-token.ts. Its sibling GoogleId *type* already lives in @packages/provider-contracts/google-auth, confirming the runtime schema's natural home is the contracts package, not @packages/test-fixtures whose stated role includes the test-app fixture builder and which ships google-auth.schema.test.ts.

```
import { GoogleIdSchema } from "@packages/test-fixtures/providers/google-auth";
```

### projects/hutch/src/runtime/providers/import-session  _(1)_

#### `projects/hutch/src/runtime/providers/import-session/dynamodb-import-session.ts`:59

- **Rule:** Named Parameters Over Positional When Types Repeat _(source: CLAUDE.md)_
- **Why:** loadOwned has two consecutive parameters of the same type (string, string). The rule requires a named parameter object when 2+ consecutive params share a type, because positional same-typed args are easy to swap by accident (connascence of position). The two values are also transposable id/userId pairs, exactly the case the rule targets. Every call site (e.g. loadOwned(id, userId) at lines 98, 103, 109, 113, 134) passes them positionally.
- **Verifier:** Line 59 matches exactly; (string, string) signature, positional call sites at 98/102/109/113/134; rule explicitly covers (string,string) with no exception for internal closures.

```
async function loadOwned(id: string, userId: string) {
```

### projects/hutch/src/runtime/providers/oauth  _(1)_

#### `projects/hutch/src/runtime/providers/oauth/dynamodb-oauth-model.ts`:36,48

- **Rule:** Internal Contracts Are Not Versioned — Prefer Required Fields _(source: test-driven-design/SKILL.md)_
- **Why:** AuthCodeRow and TokenRow are internal DynamoDB row schemas consumed only inside this monorepo. Every in-repo producer always writes the field as a concrete boolean (`emailVerified: user.emailVerified === true` at lines 101 and 167), so `.optional()` weakens an internal contract 'for backward compatibility' and lets a producer silently omit it, deferring failure to runtime. Per the rule it should be required (`z.boolean()`) so the compiler forces every producer to supply it.
- **Verifier:** Snippet present at both lines 36 and 48; both in-repo producers (101, 167) write a concrete boolean, and the rule explicitly names DynamoDB row schemas as internal contracts that must use required fields, not optional-for-back-compat.

```
emailVerified: z.boolean().optional(),
```

### projects/hutch/src/runtime/providers/password-reset  _(1)_

#### `projects/hutch/src/runtime/providers/password-reset/dynamodb-password-reset.ts`:1-73

- **Rule:** Allowed c8 ignore is reserved for truly trivial wrappers (3-5 lines, no branching); extract branching/parsing logic and unit-test it instead _(source: test-driven-design/SKILL.md)_
- **Why:** The whole-file c8 ignore is justified as a 'thin AWS SDK wrapper', but this module is not trivial/branch-free: it generates a random token, computes a TTL, and has real branching (Attributes null-check line 54, expiry comparison line 58, ConditionalCheckFailedException handling line 64). The test-driven-design skill says to reserve whole-file c8 ignore for truly trivial 3-5 line wrappers with no branching, and to extract mapping/expiry logic into a helper that is unit-tested, rather than blanket-ignoring code with logical branches.
- **Verifier:** Whole-file ignore spans lines 1-73 over code with three real branches (54, 58, 64) plus token/TTL logic; SKILL.md line 321 reserves this for 3-5 line no-branch wrappers and line 320 says to extract and unit-test branching logic.

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */
```

### projects/hutch/src/runtime/providers/pending-signup  _(2)_

#### `projects/hutch/src/runtime/providers/pending-signup/dynamodb-pending-signup.ts`:1-157

- **Rule:** Whole-file c8 ignore reserved for truly trivial wrappers; extract and unit-test non-trivial mapping/branching logic instead of ignoring it _(source: test-driven-design/SKILL.md (Thin AWS SDK Wrappers Are Unit-Tested With Fake Clients))_
- **Why:** The skill permits whole-file c8 ignore only for wrappers that are 'truly trivial (3-5 lines, no branching)' and says non-trivial mapping/parsing logic must be extracted into a helper and unit-tested directly, NOT covered by an ignore. consumePendingSignup (lines 72-113) contains real method-discrimination and null-handling branches that are hidden under the file-wide ignore instead of being extracted and unit-tested with the Partial<DynamoDBDocumentClient> fake-client pattern the skill mandates.
- **Verifier:** Confirmed: line 1 opens '/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */' wrapping the whole file through line 157; consumePendingSignup (72-113) has three method branches plus null-handling, well beyond the skill's 3-5-line/no-branching trivial threshold (SKILL.md:319-321).

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */ ... consumePendingSignup ... if (Attributes.method === "email") { ... if (!passwordHash) return null; ... } /* c8 ignore stop */
```

#### `projects/hutch/src/runtime/providers/pending-signup/dynamodb-pending-signup.ts`:9

- **Rule:** In-memory implementations / test fixtures are for tests only; production code must not depend on the test-fixtures package _(source: test-driven-design/SKILL.md (No Default In-Memory Implementations / fixtures are for tests only) and CLAUDE.md (No Default In-Memory Implementations))_
- **Why:** This production DynamoDB wrapper imports CheckoutSessionIdSchema from @packages/test-fixtures, whose own package.json describes it as the package for 'in-memory implementations and the default test-app fixture builder.' The schema is defined only inside test-fixtures (src/packages/test-fixtures/src/providers/stripe-checkout/stripe-checkout.schema.ts), so runtime production code now has a hard dependency on a test-only package. Per the docs, in-memory/test fixture artifacts are for tests only; the schema should live in a production package (e.g. domain or provider-contracts) that both production and tests import.
- **Verifier:** Confirmed: line 9 imports from '@packages/test-fixtures/providers/stripe-checkout'; package.json describes test-fixtures as 'in-memory implementations, and the default test-app fixture builder', and CheckoutSessionIdSchema is defined only at src/packages/test-fixtures/src/providers/stripe-checkout/stripe-checkout.schema.ts (absent from provider-contracts/domain), so production runtime code hard-depends on the test-only package.

```
import { CheckoutSessionIdSchema } from "@packages/test-fixtures/providers/stripe-checkout";
```

### projects/hutch/src/runtime/providers/reader-ready-state  _(1)_

#### `projects/hutch/src/runtime/providers/reader-ready-state/dynamodb-reader-ready-state.test.ts`:27

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md's 'Avoid TypeScript Type Assertions' rule explicitly forbids faking a partial object with `as` and requires Partial<T> to make the subset explicit. The fake DynamoDB client is built with a single `send` method and cast onto the full `DynamoDBDocumentClient`; the test-driven-design skill's documented fake-client pattern is `Partial<DynamoDBDocumentClient>`, not an `as` cast.
- **Verifier:** Snippet exists verbatim at line 27; the test-driven-design skill line 319 mandates the `Partial<DynamoDBDocumentClient>` pattern, which this file declines in favour of an `as` cast on a fully-inline fake.

```
return { client: client as typeof client & DynamoDBDocumentClient, commands };
```

### projects/hutch/src/runtime/providers/stripe-checkout  _(2)_

#### `projects/hutch/src/runtime/providers/stripe-checkout/stripe-checkout.ts`:109-110

- **Rule:** No Silent Fallbacks for Missing Values / No Defensive Checks Without Valid Tests _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The trailing `?? ""` provides a silent empty-string default when neither customer_details.email nor customer_email is present, letting the system continue in an invalid state. SKILL.md 'No Silent Fallbacks for Missing Values' forbids conditionals/defaults that hide a missing dependency ('const targets = toCustomerEmail ? [toCustomerEmail] : []' is the BAD example), and 'No Defensive Checks Without Valid Tests' forbids `?? defaultValue` unless a test exercises the fallback. The code should fail fast (or model the absent email explicitly) rather than coerce to "".
- **Verifier:** Snippet verified at lines 109-110; SKILL.md line 224 literally forbids '?? defaultValue' without a test, and no test file exists for this directory, so the fallback is both silent and untested.

```
const customerEmail = parsed.customer_details?.email ?? parsed.customer_email ?? "";
```

#### `projects/hutch/src/runtime/providers/stripe-checkout/stripe-checkout.ts`:1, 124

- **Rule:** No Coverage Ignore Comments / Allowed c8 ignore Cases (thin AWS SDK wrappers only) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md forbids `c8 ignore` unless approved; its only sanctioned whole-file case is 'Thin AWS SDK wrappers tested via integration tests', and SKILL.md only allows the trivial-wrapper variant when it is '3-5 lines, no branching'. This is a Stripe (non-AWS) wrapper with substantial branching — error parsing via safeParse, response.ok checks, 404/resource_missing mapping, payment_status/status mapping, and conditional subscription/customer spreads. Blanket-ignoring this branching logic falls outside the approved exceptions; the branches should be covered (e.g. fake fetch client) rather than excluded.
- **Verifier:** Snippet verified at lines 1 and 124 wrapping the whole ~85-line file; it is a non-AWS Stripe wrapper with multiple branches (lines 77,79,95,100,103,113,117,118) and no test file, so it matches neither CLAUDE.md's AWS-only exception nor SKILL.md's '3-5 lines, no branching' trivial-wrapper allowance.

```
/* c8 ignore start -- thin Stripe API wrapper, tested via integration */ ... /* c8 ignore stop */
```

### projects/hutch/src/runtime/reader-ready-fanout  _(2)_

#### `projects/hutch/src/runtime/reader-ready-fanout/reader-ready-fanout-handler.test.ts`:58-59

- **Rule:** Avoid TypeScript Type Assertions (`as`) — branded domain IDs must use a validated factory _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' forbids using `as` to cast a raw value to a branded domain ID and prescribes the validated-factory pattern (`UserIdSchema.parse(rawValue)` from a Zod brand). The `as UserId` cast bypasses the compiler so a wrong-shaped value would be silently accepted. The rule lists no test-file exception for branded-ID casts.
- **Verifier:** Snippet and lines 58-59 match exactly; `"viewer" as UserId` / `"never" as UserId` cast raw strings to branded UserId, which the rule forbids in favour of a Zod `.parse()` factory; neither allowed exception (`as const` / isolated Node wrapper) applies and there is no test-file carve-out.

```
const viewedSaver = { userId: "viewer" as UserId, viewedAt: new Date("2026-05-30T11:50:00.000Z") };
const neverViewedSaver = { userId: "never" as UserId, viewedAt: undefined };
```

#### `projects/hutch/src/runtime/reader-ready-fanout/reader-ready-fanout-handler.test.ts`:79

- **Rule:** Avoid TypeScript Type Assertions (`as`) — branded domain IDs must use a validated factory _(source: CLAUDE.md)_
- **Why:** Same as above: `"viewer" as UserId` casts a raw string to the branded `UserId` type via `as`, which CLAUDE.md forbids in favour of a Zod `.parse()` factory. `as` is not one of the allowed exceptions (as const / isolated Node wrapper).
- **Verifier:** Line 79 matches the cited snippet exactly; the `as UserId` cast on a raw string literal violates the branded-ID rule and falls under none of the documented exceptions.

```
const viewedSaver = { userId: "viewer" as UserId, viewedAt: new Date("2026-05-30T11:50:00.000Z") };
```

### projects/hutch/src/runtime/web/api  _(1)_

#### `projects/hutch/src/runtime/web/api/api.routes.test.ts`:28-34

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md ("Avoid TypeScript Type Assertions") forbids using `as` to fake a partial object and shows `Partial<T>` as the correct alternative. The fake `Client` only sets three fields; `as Client` asserts a full Client, hiding any future required field added upstream.
- **Verifier:** Snippet exact at lines 28-32; only three of Client's fields set then cast `as Client` — the exact faked-partial-object anti-pattern CLAUDE.md shows as BAD with Partial<T> as the fix.

```
client: {
			id: "hutch-firefox-extension",
			grants: ["authorization_code", "refresh_token"],
			redirectUris: ["http://127.0.0.1:3000/oauth/callback"],
		} as Client,
```

### projects/hutch/src/runtime/web/auth  _(3)_

#### `projects/hutch/src/runtime/web/auth/account-locked.template.html`:4

- **Rule:** Use "I" not "we" (solo-built voice) _(source: BRAND_GUIDELINES.md)_
- **Why:** Brand voice rule: "Use 'I' not 'we.' Readplace is solo-built." User-facing UI copy uses "we'll" instead of "I'll".
- **Verifier:** Snippet exists verbatim at line 4; BRAND_GUIDELINES.md Voice section and Don'ts both forbid "we" in product UI; this is user-facing copy.

```
Email <a class="auth-card__link" href="mailto:{{contactEmail}}">{{contactEmail}}</a> and we'll restore your access.
```

#### `projects/hutch/src/runtime/web/auth/forgot-password.template.html`:9

- **Rule:** Use "I" not "we" (solo-built voice) _(source: BRAND_GUIDELINES.md)_
- **Why:** Brand voice rule: "Use 'I' not 'we.' Readplace is solo-built." User-facing UI copy uses "we'll" instead of first-person/product voice.
- **Verifier:** Snippet exists verbatim at line 9 of the forgot-password subtitle; same documented "use 'I' not 'we'" rule; user-facing UI.

```
Enter your email and we'll send you a link to reset your password
```

#### `projects/hutch/src/runtime/web/auth/verification-lockout.route.test.ts`:50-54

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Avoid TypeScript Type Assertions (`as`)" forbids faking a partial object with `as` and shows the exact `as Response` BAD example; the fix is to type the subset with Partial<Client>. `as Client` is not an allowed exception (not `as const`, not an isolated Node API wrapper).
- **Verifier:** `as Client` on a partial object literal exists at lines 50-54; matches the "faking a partial object with `as`" BAD pattern; not `as const` nor an isolated wrapper exception; the rule applies to test code too.

```
client: {
			id: "hutch-firefox-extension",
			grants: [...],
			redirectUris: [...],
		} as Client,
```

### projects/hutch/src/runtime/web/auth/test-helpers  _(1)_

#### `projects/hutch/src/runtime/web/auth/test-helpers/complete-stripe-signup.ts`:17-21

- **Rule:** Comments Document Why, Not What _(source: CLAUDE.md)_
- **Why:** This doc comment narrates WHAT the function body does step-by-step (creates session, stores pending signup, marks paid, GETs success URL). CLAUDE.md 'Comments Document Why, Not What' says an approved comment explains why, never what; the sequence is already visible in the code below it.
- **Verifier:** Snippet matches lines 17-21 exactly; the comment mirrors the body's createCheckoutSession/storePendingSignup/markPaid/get sequence — a 'what' narration the rule forbids.

```
/** Drives `GET /auth/checkout/success` directly: creates a Stripe checkout
 * session via the in-memory fake, stores a pending signup keyed by that
 * session id, marks the session paid, then GETs the success URL using a shared
 * agent so the resulting session cookie persists.
```

### projects/hutch/src/runtime/web/oauth  _(1)_

#### `projects/hutch/src/runtime/web/oauth/oauth.component.ts`:54

- **Rule:** Hardcoded colour value instead of CSS custom property _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES 'Do's and Don'ts' states 'Hardcode hex values — always use the CSS custom properties' and 'Use the CSS custom properties defined in base.styles.ts — never hardcode colour values.' The Buttons table mandates white primary-button text be expressed as --primary-foreground. The approve button hardcodes 'white' instead of var(--primary-foreground).
- **Verifier:** Line 54 has 'color: white;' inside .oauth-authorize__btn--approve which uses 'background: var(--primary)' (a primary button); BRAND_GUIDELINES line 181 mandates --primary-foreground and lines 328/339 forbid hardcoded colours.

```
color: white;
```

### projects/hutch/src/runtime/web/pages/account  _(1)_

#### `projects/hutch/src/runtime/web/pages/account/account.view-model.ts`:26,29,79

- **Rule:** Never Add Defensive Checks Without Valid Tests / dead unreachable path _(source: test-driven-design/SKILL.md)_
- **Why:** Every constructed action (SUBSCRIBE_ACTION, CANCEL_FORM_ACTION, REACTIVATE_FORM_ACTION) is `method: "POST"`, so `isLink` is always false and the `{{#if isLink}}` <a> branch in the template is never exercised by any test. The `"GET"` arm of the union and the link branch are dead/untestable code that should be removed (related to 'No Defensive Checks Without Valid Tests' and the Forms-Everywhere rule).
- **Verifier:** Verified all three actions (lines 85, 93, 101) use method: "POST", so isLink is always false and the GET arm / <a> branch are dead, unexercised paths.

```
method: "GET" | "POST";
...
isLink: input.method === "GET"
```

### projects/hutch/src/runtime/web/pages/admin  _(6)_

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.styles.css`:18-20

- **Rule:** Hardcode hex values — always use CSS custom properties from base.styles.ts _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES.md 'Don't' list says 'Hardcode hex values — always use the CSS custom properties' and 'Use the CSS custom properties defined in base.styles.ts — never hardcode colour values.' These raw hexes also do not correspond to any brand token (the brand Info colour is --color-info / #4A7FB5).
- **Verifier:** Lines 18-20 match exactly; raw hex literals, BRAND_GUIDELINES lines 328/339 forbid hardcoded hex and require base.styles.ts custom properties.

```
background: #eef3fb;
  color: #1e6fb8;
  border: 1px solid #c4d6ee;
```

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.styles.css`:24-26

- **Rule:** Hardcode hex values — always use CSS custom properties from base.styles.ts _(source: BRAND_GUIDELINES.md)_
- **Why:** Raw hex literals for the legacy badge instead of the documented neutral tokens (--color-surface, --color-text-muted, --color-border). BRAND_GUIDELINES.md forbids hardcoded hex and requires the CSS custom properties.
- **Verifier:** Lines 24-26 match exactly; raw hex literals, same forbidden-hex rule (BRAND_GUIDELINES lines 328/339).

```
background: #f5f5f5;
  color: #666;
  border-color: #ddd;
```

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.route.test.ts`:151-152

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** test-driven-design 'Never Rely on querySelector(...).toBeNull()': a selector typo makes .not.toBeNull() fail for the wrong reason. The doc requires asserting existence then checking element state/metadata, not bare existence via toBeNull().
- **Verifier:** Lines 151-152 match exactly; bare .not.toBeNull() existence checks, precisely the BAD pattern at SKILL line 260.

```
expect(doc.querySelector("[data-test-admin-recrawl-form]")).not.toBeNull();
			expect(doc.querySelector("[data-test-admin-recrawl-input]")).not.toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.route.test.ts`:282-284

- **Rule:** Never Rely on querySelector(...).toBeNull() / Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** test-driven-design forbids proving absence/presence via toBeNull()/.not.toBeNull() — a selector typo passes the toBeNull() assertions trivially. The doc requires rendering elements unconditionally and asserting on a state class/data-* attribute instead.
- **Verifier:** Lines 282-284 match exactly; both .not.toBeNull() and .toBeNull() forms used — the BAD patterns at SKILL lines 257/260.

```
expect(doc.querySelector("[data-test-admin-recrawl]")).not.toBeNull();
			expect(doc.querySelector("[data-share-balloon]")).toBeNull();
			expect(doc.querySelector("[data-test-view-cta]")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.route.test.ts`:424

- **Rule:** Structured Parsing Over Regex _(source: .claude/skills/web/SKILL.md)_
- **Why:** web SKILL 'Structured Parsing Over Regex': 'Never use regex to extract data from structured markup.' The test already parses with JSDOM elsewhere; it should read the hx-get attribute via the DOM (querySelector(...).getAttribute('hx-get')) rather than a regex over HTML.
- **Verifier:** Line 424 matches exactly; regex extracts an hx-get attribute from HTML, violating web SKILL lines 242-244 (JSDOM is already used in the same file).

```
const pollUrl = response.text.match(/hx-get="([^"]+)"/)?.[1];
```

#### `projects/hutch/src/runtime/web/pages/admin/recrawl.route.test.ts`:452

- **Rule:** Structured Parsing Over Regex _(source: .claude/skills/web/SKILL.md)_
- **Why:** Second regex extraction of an attribute from structured HTML, contrary to web SKILL's 'use linkedom/parseHTML and standard DOM APIs' requirement; regex over markup is forbidden without human approval.
- **Verifier:** Line 452 matches exactly; second regex extraction of hx-get from HTML markup, same web SKILL lines 242-244 violation.

```
const pollUrl = response.text.match(/hx-get="([^"]+)"/)?.[1];
```

### projects/hutch/src/runtime/web/pages/export  _(4)_

#### `projects/hutch/src/runtime/web/pages/export/export.template.html`:24

- **Rule:** Use "I" not "we" — Readplace is solo-built _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES Voice & Copy states "Use 'I' not 'we.' Readplace is solo-built" and the Don't list forbids using "we". This UI copy uses "We'll".
- **Verifier:** Snippet matches export.template.html line 24 exactly; BRAND_GUIDELINES lines 254 and 334 forbid "we" in product UI copy. No exception applies.

```
We'll prepare your export in the background and email you a download link when it's ready.
```

#### `projects/hutch/src/runtime/web/pages/export/export-preparing.template.html`:3

- **Rule:** Use "I" not "we" — Readplace is solo-built _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES Voice & Copy requires "I" not "we" because Readplace is solo-built; the heading uses "We're".
- **Verifier:** Exact match at line 3; user-facing heading uses "We're", violating the "I" not "we" voice rule (BRAND_GUIDELINES lines 254/334).

```
<h1 class="export__title">We're preparing your export</h1>
```

#### `projects/hutch/src/runtime/web/pages/export/export-preparing.template.html`:8

- **Rule:** Use "I" not "we" — Readplace is solo-built _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES Voice & Copy requires "I" not "we"; this body copy uses "We'll".
- **Verifier:** Line 8 contains the cited "We'll email you a link as soon as it's ready." Body copy uses "we", violating the voice rule. No exception.

```
We'll email you a link as soon as it's ready.
```

#### `projects/hutch/src/runtime/web/pages/export/user-data-export-email.ts`:24

- **Rule:** Use "I" not "we" — Readplace is solo-built _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES Voice & Copy requires "I" not "we" for product/email copy; this email body uses "We've".
- **Verifier:** Exact match at line 24; email body copy uses "We've". The Email/Newsletters voice ("Fayner from Readplace", personal) reinforces the "I" not "we" rule. Confirmed.

```
We've packaged ${articleCount} article${articleCount === 1 ? "" : "s"} as a single JSON file.
```

### projects/hutch/src/runtime/web/pages/home  _(7)_

#### `projects/hutch/src/runtime/web/pages/home/home.static.route.test.ts`:27

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: test-driven-design/SKILL.md)_
- **Why:** The doc forbids proving a hidden/absent state with querySelector(...).toBeNull(): a selector typo also returns null, so the assertion passes for the wrong reason. The element should be rendered unconditionally with a state class/data-* attribute that the test looks up and asserts on.
- **Verifier:** Line 27 matches exactly; SKILL.md line 257 names this exact anti-pattern, and sibling tests (lines 41-49) already use the assert-then-check-class alternative.

```
expect(doc.querySelector("[data-test-founding-progress]")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/home/home.static.route.test.ts`:68

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: test-driven-design/SKILL.md)_
- **Why:** Same anti-pattern: visibility is proven via .toBeNull(), which a selector typo would satisfy spuriously. Render the title unconditionally and toggle a state class, then assert the class.
- **Verifier:** Line 68 matches verbatim; proves hidden state of the pricing title via .toBeNull(), exactly what SKILL.md line 257 forbids.

```
expect(doc.querySelector("[data-test-pricing-title]")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/home/home.static.route.test.ts`:54

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: test-driven-design/SKILL.md)_
- **Why:** .not.toBeNull() fails for the wrong reason on a selector typo and only checks existence, not behaviour. Assert the element exists then check a load-bearing attribute (e.g. its href).
- **Verifier:** Line 54 matches verbatim; SKILL.md lines 251/260 explicitly call out .not.toBeNull() as the subtler form of the same bug.

```
expect(fallback.querySelector('[data-test-cta="become-member"]')).not.toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/home/home.route.test.ts`:196

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: test-driven-design/SKILL.md)_
- **Why:** Existence-only assertion via .not.toBeNull(); a typo makes it fail even when production is correct. The doc requires asserting existence then checking state/behaviour, not merely that the element is non-null.
- **Verifier:** Lines 195-196 match; existence-only .not.toBeNull() with no follow-up state/behaviour check, violating both 'Test Behavior, Not Element Existence' and the querySelector rule.

```
const backstory = doc.querySelector('[data-test-section="backstory"]'); expect(backstory).not.toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/home/home.route.test.ts`:398

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: test-driven-design/SKILL.md)_
- **Why:** Proves absence of the twitter:site meta via .toBeNull(), which a selector typo would satisfy spuriously. Encode the absence as observable state rather than relying on a null query result.
- **Verifier:** Line 398 matches (twitterSite from meta[name=twitter:site] on line 397); proves absence via .toBeNull() — a selector typo passes spuriously, exactly the rule's rationale.

```
expect(twitterSite).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/home/home.static.route.test.ts`:223

- **Rule:** Structured Parsing Over Regex (never regex-extract structured markup) _(source: web/SKILL.md)_
- **Why:** Extracts <loc> values from an XML sitemap with a regex. The web skill mandates a proper parser (linkedom parseHTML / DOM APIs) for any structured format including XML; regex parsing requires explicit human approval.
- **Verifier:** Line 223 matches verbatim; web/SKILL.md lines 240-244 'Use a proper parser for any structured format (HTML, XML, JSON)' and 'Regex-based markup parsing requires explicit human approval'.

```
Array.from(response.text.matchAll(/<loc>([^<]+)<\/loc>/g)).map((m) => m[1]);
```

#### `projects/hutch/src/runtime/web/pages/home/home.component.ts`:14-103

- **Rule:** Browser JS Is Bundled and Served Same-Origin (no inline scripts; use a bundled *.client.ts via <script src>) _(source: web/SKILL.md)_
- **Why:** The headline word-rotator and smooth-scroll behaviours are exactly the htmx-can't-express case the web skill assigns to a compiled *.client.ts bundle referenced via a same-origin <script src>. The skill says to reserve *.client.js for animations/inline behaviour and serve them as a bundle, not inline these large hand-written IIFE strings into the page body.
- **Verifier:** Lines 14-103 hold two large inline IIFE <script> string literals (HOME_HEADLINE_SCRIPT rotator animation, HOME_SCROLL_HINT_SCRIPT smooth-scroll); web/SKILL.md 'Browser JS Is Bundled and Served Same-Origin' mandates a compiled *.client.ts bundle via <script src> and reserves client JS for exactly these animations.

```
const HOME_HEADLINE_SCRIPT = `<script>(function () { var rotator = document.querySelector('.hero-headline__rotator'); ...`
```

### projects/hutch/src/runtime/web/pages/queue  _(8)_

#### `projects/hutch/src/runtime/web/pages/queue/save-content-limit-handler.test.ts`:15

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids faking a partial object with `as` (`{ status: 200 } as Response`) and requires `Partial<T>` to make the subset explicit. Here a hand-rolled fake response is force-cast via `as unknown as Response` instead of being typed as `Partial<Response>`.
- **Verifier:** Line 15 contains `} as unknown as Response;` closing a hand-rolled fake; matches the forbidden partial-object-via-`as` pattern, no documented exception applies (not a Node API wrapper).

```
} as unknown as Response;
```

#### `projects/hutch/src/runtime/web/pages/queue/save-content-limit-handler.test.ts`:29

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** Same rule: the fake Express request is force-cast with `as unknown as Request` rather than declared `Partial<Request>`. `as` bypasses the compiler and hides shape mismatches if Request's type changes.
- **Verifier:** Line 29 is `} as unknown as Request;` closing a partial fake Express Request; same forbidden pattern, no exception applies.

```
} as unknown as Request;
```

#### `projects/hutch/src/runtime/web/pages/queue/queue-onboarding.route.test.ts`:208

- **Rule:** Never Rely on querySelector(...).toBeNull() — assert existence first, then check state _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The skill states `expect(querySelector(...)).toBeNull()` is a fragile way to prove an element is hidden/absent: a selector typo also returns null so the assertion passes for the wrong reason. Visibility should be encoded as a state class/attribute on an unconditionally-rendered element and asserted positively.
- **Verifier:** Lines 207-208 match exactly; SKILL.md 249-266 explicitly uses `[data-test-onboarding]` `.toBeNull()` as the canonical BAD example.

```
const onboarding = doc.querySelector("[data-test-onboarding]");
expect(onboarding).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/queue/queue.subscription-banner.route.test.ts`:31

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Proves banner state via `.toBeNull()`; a selector typo passes for the wrong reason. The skill requires asserting the element exists then checking a state class/attribute rather than absence.
- **Verifier:** Line 31 matches exactly; proves the trial-countdown is absent via `.toBeNull()`, the forbidden absence pattern (the sibling banner aside on line 29 uses the correct assert+class pattern, so the countdown line is a genuine miss).

```
expect(doc.querySelector("[data-test-trial-countdown]")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/queue/queue.mutations.route.test.ts`:31

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Asserts the empty-state region is absent via `.toBeNull()`, which the skill calls out as fragile (a selector typo passes trivially). Should render the region unconditionally and toggle a state class asserted positively.
- **Verifier:** Line 31 matches exactly; `[data-test-empty-queue]` asserted absent via `.toBeNull()`, the forbidden pattern.

```
expect(doc.querySelector("[data-test-empty-queue]")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/queue/queue.listing.route.test.ts`:204

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Uses `.toBeNull()` to prove the url element is not rendered; a selector typo makes the assertion pass for the wrong reason per the skill. Assert presence + state metadata instead.
- **Verifier:** Line 204 matches exactly; `[data-test-article-url]` proved absent via `.toBeNull()`, the forbidden pattern.

```
expect(doc.querySelector("[data-test-article-url]")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/queue/queue.card.route.test.ts`:128

- **Rule:** Never Rely on querySelector(...).toBeNull() / .not.toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Proves the thumbnail is absent via `.toBeNull()`. The skill explicitly forbids this pattern (and the `.not.toBeNull()` variant): a selector typo passes/fails for the wrong reason. Render unconditionally and toggle a state class to assert visibility.
- **Verifier:** Line 128 matches exactly; `.queue-article__thumbnail` proved absent via `.toBeNull()`, the forbidden pattern.

```
expect(doc.querySelector(".queue-article__thumbnail")).toBeNull();
```

#### `projects/hutch/src/runtime/web/pages/queue/queue.card.route.test.ts`:282, 286

- **Rule:** Never Rely on querySelector(...).not.toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The skill calls out the subtler `.not.toBeNull()` (a typo fails for the wrong reason) and the `.toBeNull()` absence check. Both appear here; assert the element exists then check a state class/attribute (e.g. `--loaded`/`--hidden`) instead of existence/absence.
- **Verifier:** Line 282 is `.not.toBeNull()` and line 286 is `.toBeNull()`; both match exactly, and both are the existence/absence checks SKILL.md 249-266 forbids.

```
expect(thumbnail?.closest("a")).not.toBeNull(); ... expect(...querySelector(".queue-article__thumbnail-link")).toBeNull();
```

### projects/hutch/src/runtime/web/pages/queue/queue-card  _(1)_

#### `projects/hutch/src/runtime/web/pages/queue/queue-card/queue-card.template.html`:2

- **Rule:** No time-bound or plan-bound comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md forbids plan-bound comments ("temporary until rollout completes", "remove when Y is concluded"). This comment ties the code's lifetime to a future migration that no one will remember to revisit; the condition should be encoded as a tracked issue/feature flag, not narrated in a comment that rots.
- **Verifier:** Snippet verified verbatim at line 2; ties deletion of code to a future backfill migration, matching CLAUDE.md's BAD 'remove this branch after the migration' plan-bound example.

```
once a backfill migration rewrites every legacy row to a CDN URL, this onerror (and the paired onload) can be deleted.
```

### projects/hutch/src/runtime/web/shared/article-body/reader-slot  _(2)_

#### `projects/hutch/src/runtime/web/shared/article-body/reader-slot/reader-failed.template.html`:2

- **Rule:** No emojis in product UI _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES.md (Tone Rules / Don'ts) states 'No emojis in UI. Emojis are fine in social posts or community replies, never in the product interface.' This title renders directly in the reader product UI and embeds a ✅ emoji.
- **Verifier:** Snippet exact at line 2; BRAND_GUIDELINES.md:271 states 'No emojis in UI' verbatim; this is a product reader-slot template, not social copy.

```
<h2 class="article-body__reader-notice-title">Your link is saved ✅</h2>
```

#### `projects/hutch/src/runtime/web/shared/article-body/reader-slot/reader-iframe.client.test.ts`:29

- **Rule:** Avoid TypeScript type assertions (as) / faking a type with as _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' forbids `as` outside the allowed exceptions (as const, isolated Node wrappers). A double `as unknown as` cast is the explicitly-discouraged pattern of forcing a type rather than declaring the fake to satisfy the interface (e.g. typing the class to implement FakeObserverCtor directly).
- **Verifier:** Snippet exact at line 29; double `as unknown as` is not as const nor an isolated Node wrapper; rule has no test-file exemption and FakeObserver could implement FakeObserverCtor directly.

```
Ctor: FakeObserver as unknown as FakeObserverCtor,
```

### projects/hutch/src/runtime/web/shared/article-reader  _(2)_

#### `projects/hutch/src/runtime/web/shared/article-reader/article-reader.test.ts`:890-891

- **Rule:** Never Rely on querySelector(...).toBeNull() — assert existence first, then check state _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The SKILL explicitly forbids proving absence with querySelector(...).toBeNull() because a typo in the selector also returns null, so the assertion passes for the wrong reason. The 'header omitted when the row is missing' case should render a state-bearing element (or assert positively on what IS emitted) rather than asserting the OOB fragments are null.
- **Verifier:** Snippet matches lines 890-891 verbatim; SKILL.md:249-257 forbids this exact .toBeNull() absence pattern. The neighboring positive slot assert (line 893) does not cure the two forbidden lines.

```
expect(doc.querySelector("#article-header")).toBeNull();
expect(doc.querySelector("title#document-title")).toBeNull();
```

#### `projects/hutch/src/runtime/web/shared/article-reader/article-reader.test.ts`:613

- **Rule:** Comments/identifiers must not encode time-bound or stale claims; test names must match behaviour _(source: CLAUDE.md (Comments Document Why, Not What — time-bound wording))_
- **Why:** The test title hardcodes 'MAX_POLLS=40', but MAX_POLLS is defined as 300 in article-reader.ts. This is a stale, now-false embedded constant in a human-readable identifier; it misleads readers about the actual cap and will not be caught by the compiler since it lives in a string literal.
- **Verifier:** Title at line 613 says 'MAX_POLLS=40' but article-reader.ts:38 defines MAX_POLLS=300; the test body uses pollCount: MAX_POLLS so the literal 40 in the string is stale and false.

```
it("stops polling at MAX_POLLS=40 and swaps to the 'Your link is saved' slow reframe", async () => {
```

### projects/hutch/src/runtime/web/shared/extension-suggestion-banner  _(1)_

#### `projects/hutch/src/runtime/web/shared/extension-suggestion-banner/extension-suggestion-banner.client.ts`:23-28

- **Rule:** Use assert for runtime invariants instead of if/throw _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Use assert for Runtime Invariants' mandates assert from node:assert for invariant checks instead of if/throw: 'Use assert ... for runtime invariant checks instead of if/throw. Assert is more concise ... and integrates with coverage tooling (no uncovered branches for the truthy path).' This hand-rolled ensure() reimplements an invariant guard with the exact if/throw shape the rule labels BAD ('if (!entity.properties) { throw new Error(...) }'), creating the coverage branch the rule warns against.
- **Verifier:** Snippet matches lines 23-28 exactly; ensure() is the literal if/throw invariant-guard shape the rule's BAD example forbids, in production .client.ts code with no applicable exception.

```
function ensure<T>(value: T | null | undefined, description: string): T {
	if (value === null || value === undefined) {
		throw new Error(`extension-suggestion-banner: ${description}`);
	}
	return value;
}
```

### projects/hutch/src/runtime/web/shared/founding-progress  _(1)_

#### `projects/hutch/src/runtime/web/shared/founding-progress/founding-progress.styles.css`:5

- **Rule:** No time/plan-bound comments; comments document why not what _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound comments that reference the current task/experiment (e.g. 'this is a test to do X, remove when Y is concluded'). This is a transient publish-probe note ('test 1/4') with no bearing on the CSS rule it sits inside; it will rot and conveys nothing about the styling. It should be removed, not committed.
- **Verifier:** Snippet verified verbatim at line 5; it is a plan/task-bound 'test 1/4' probe note unrelated to the CSS, exactly what CLAUDE.md's plan-bound-comment rule forbids.

```
/* tag-based publish probe (test 1/4): hutch-only edit must not publish either extension */
```

### projects/hutch/src/runtime/web/shared/save-article  _(1)_

#### `projects/hutch/src/runtime/web/shared/save-article/save-article-from-url.ts`:76-81

- **Rule:** Partial Application for Domain Functions (init* prefix) _(source: test-driven-design/SKILL.md)_
- **Why:** This is a domain function carrying injected dependencies (saveArticle, markCrawlPending, markSummaryPending, publishUpdateFetchTimestamp, publishLinkSaved, updateArticleStatus, refreshArticleIfStale). The test-driven-design skill states: 'Domain functions with dependencies must use partial application. Use an init* prefix for the initialization function.' The example shows initCreatePaymentPlan(deps)(input) rather than createPaymentPlan(input, deps). Here deps and the per-call params are mixed in a single positional signature (deps re-passed at every call site in queue.page.ts and import.page.ts) instead of an initSaveArticleFromUrl(deps) that returns (params) => Promise<...>.
- **Verifier:** Snippet matches exactly at lines 76-81; deps interface (7 injected deps) is mixed positionally with per-call params, matching the skill's BAD shape, and 7 call sites in queue.page.ts/import.page.ts re-pass deps every call. Rule at SKILL.md lines 68-80 applies verbatim; no exception covers it.

```
export function saveArticleFromUrl(
	deps: SaveArticleFromUrlDependencies,
	params: { userId: UserId; url: SaveableUrl; freshness: ContentFreshnessResult },
): Promise<{ saved: SavedArticle }> {
	return saveByFreshness(deps, params);
}
```

### projects/hutch/src/runtime/web/shared/share-balloon  _(2)_

#### `projects/hutch/src/runtime/web/shared/share-balloon/share-balloon.client.ts`:46-51

- **Rule:** Use assert for Runtime Invariants (if/throw where assert fits) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Use assert for Runtime Invariants' says to use assert from node:assert for runtime invariant checks instead of if/throw, because assert is more concise, communicates intent, and avoids an uncovered branch for the truthy path. This hand-rolled ensure() uses if/throw for an invariant (element/attribute must exist) that assert covers directly.
- **Verifier:** Snippet exact at lines 46-51; ensure() uses if/throw to enforce a not-null invariant, which CLAUDE.md says should use assert; no documented exception for .client.ts.

```
function ensure<T>(value: T | null | undefined, description: string): T {
		if (value === null || value === undefined) {
			throw new Error(`share balloon: ${description}`);
		}
		return value;
	}
```

#### `projects/hutch/src/runtime/web/shared/share-balloon/share-balloon.client.ts`:105-108

- **Rule:** Comments must not be time/plan/task-bound (no PR/issue references) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids comments that reference a particular task/PR moment in time ('added for the auth migration', PR/issue references). 'pre-#389 behaviour' ties the comment to a specific PR number and will rot once #389 is forgotten; the intent should be encoded in code or the reference removed.
- **Verifier:** Comment block at lines 105-108 ends with 'pre-#389 behaviour'; this is a PR/issue-bound reference that CLAUDE.md flags as rot-prone and forbidden.

```
* so the balloon retains its pre-#389 behaviour in those contexts. */
```

### projects/save-link  _(2)_

#### `projects/save-link/src/runtime/domain/article-parser/ocr-pdf.ts`:362

- **Rule:** Named Parameters Over Positional When Types Repeat _(source: CLAUDE.md)_
- **Why:** Two consecutive same-typed (number, number) positional parameters are trivially swappable at the call site (connascence of position). CLAUDE.md requires a named parameter object when 2+ consecutive params share a type.
- **Verifier:** Line 362 matches exactly: two consecutive (number, number) params, which the rule explicitly cites as requiring a named object.

```
function chunkPages(numPages: number, batchSize: number): number[][] {
```

#### `projects/save-link/src/runtime/providers/pdf-page-ocr/init-tesseract-ocr.ts`:84

- **Rule:** Named Parameters Over Positional When Types Repeat _(source: CLAUDE.md)_
- **Why:** Two consecutive same-typed (string, string) positional parameters are easy to transpose. CLAUDE.md requires a named parameter object when 2+ consecutive params share a type; there is no privacy exception.
- **Verifier:** Line 84 matches exactly: two consecutive (string, string) params; the rule's (string,string) example applies directly with no exception.

```
function spawnTesseract(pngPath: string, languageFlag: string): Promise<string> {
```

### projects/web-embed  _(2)_

#### `projects/web-embed/src/runtime/embed/snippet.component.test.ts`:41,47,54

- **Rule:** Avoid Negative Test Assertions (.not.toContain / .not.toMatch become stale) _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** test-driven-design's 'Avoid Negative Test Assertions' rule says negative assertions like .not.toContain() become stale when code is refactored and should be replaced with an assertion on the actual expected value. These cases (no <script>, no document.cookie/onclick/onload, no rel=nofollow) are all negative .not.toMatch assertions rather than asserting the concrete rendered output.
- **Verifier:** Three .not.toMatch negative assertions exist at lines 41 (/<script/i), 47 (document.cookie|onclick=|onload=) and 54 (rel=nofollow); the cited snippet is exact (the finding's 42/53/55 line numbers are off by one but point at the same it.each blocks). Rule 'Avoid Negative Test Assertions' applies as written.

```
expect(html).not.toMatch(/<script/i);
```

#### `projects/web-embed/src/runtime/embed/embed.route.test.ts`:151

- **Rule:** Avoid Negative Test Assertions (.not.toContain becomes stale) _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** test-driven-design's 'Avoid Negative Test Assertions' rule explicitly calls out .not.toContain() as becoming stale on refactor. The positive assertions on lines 149-150 already prove the canonical URLs are present; this trailing negative assertion adds a fragile check that should be dropped or replaced with an exact-value assertion.
- **Verifier:** Line 151 is exactly `expect(source.textContent).not.toContain("http://127.0.0.1:9999");`, preceded at 149-150 by positive toContain assertions on the canonical URLs. This is the literal .not.toContain() pattern the rule names as stale-prone.

```
expect(source.textContent).not.toContain("http://127.0.0.1:9999");
```

### src/packages/article-parser  _(7)_

#### `src/packages/article-parser/src/readability-parser.test.ts`:210

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' forbids .not.toContain() because it becomes stale on refactor and can pass for the wrong reason. The line above already positively asserts the resolved absolute URL; the negative assertion adds no signal and should be dropped (assert the exact expected value instead).
- **Verifier:** Line 210 matches exactly; line 209 already positively asserts the absolute src, so the .not.toContain() is a redundant negative assertion the rule explicitly bans. No exception applies to tests.

```
expect(result.article.content).not.toContain('src="/images/diagram.jpg"');
```

#### `src/packages/article-parser/src/readability-parser.test.ts`:313, 630, 661, 680

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' — these prove 'X is absent' via .not.toContain(), which goes stale on refactor. Assert the actual expected content/state positively (e.g. assert the placeholder element exists and its href) rather than asserting absence of the old markup.
- **Verifier:** Lines 313, 630, 661 are .not.toContain("...Original..."/"<video"); line 680 is .not.toContain("reader-video-placeholder") — all negative assertions banned by the rule. Each is paired with positive assertions, confirming the negatives add no unique signal.

```
expect(result.article.content).not.toContain("Original body that should be replaced"); ... expect(result.article.content).not.toContain("<video");
```

#### `src/packages/article-parser/src/medium-pre-parser.test.ts`:84, 96-97, 146, 161, 186-189, 236, 247, 269, 281-282, 294-295, 306, 317, 328, 340, 355, 421-423, 458, 470, 483-484, 499-500, 513-514, 573

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' bans .not.toContain(). Nearly every chrome-stripping test here proves removal solely with negative assertions, which pass for the wrong reason if the selector/fingerprint changes. Tests should positively assert the resulting cleaned body shape (the exact retained content) rather than the absence of stripped strings.
- **Verifier:** Spot-checked every cited line: all are .not.toContain() negative assertions verifying chrome removal. Lines and snippet quoted accurately; rule applies directly to test files with no exception.

```
expect(result?.bodyHtml).not.toContain("authorPhoto"); ... expect(result?.bodyHtml).not.toContain("5 min read");
```

#### `src/packages/article-parser/src/medium-pre-parser.test.ts`:383

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' — .not.toMatch() is the negative-assertion anti-pattern; it asserts the claps separator is absent and will silently pass if the markup shape changes. Assert the exact expected cleaned body instead.
- **Verifier:** Line 383 matches exactly. .not.toMatch() is the same negative-assertion family the rule warns about; the test has no positive assertion on the resulting body, so it can pass for the wrong reason if the markup shape drifts.

```
expect(result?.bodyHtml).not.toMatch(/<p[^>]*><span[^>]*>\s*--\s*<\/span><\/p>/);
```

#### `src/packages/article-parser/src/the-information-pre-parser.test.ts`:73, 89

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' forbids .not.toContain(). The escaping test should assert the positively-escaped form only (line 90 already does &lt;tag&gt;); the .not.toContain('<tag>') on line 89 is a stale-prone negative assertion.
- **Verifier:** Lines 73 and 89 match exactly. Line 89's negative is redundant with the positive &lt;tag&gt; assertion on line 90; line 73 asserts absence of the optional caption. Both are negative assertions the rule bans.

```
expect(result?.bodyHtml).not.toContain("Photo by"); ... expect(result?.bodyHtml).not.toContain("<tag>");
```

#### `src/packages/article-parser/src/promote-br-paragraph-hosts.test.ts`:166, 185

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' — both assert absence via .not.toContain(), which goes stale and can pass for the wrong reason. The paragraph-rebuild test already counts <p> tags positively; assert the exact paragraph structure rather than the absence of <br><br>.
- **Verifier:** Lines 166 and 185 match exactly; both are .not.toContain(). Line 166's test already positively counts <p> tags and asserts retained text, so the negative is the kind the rule flags as stale-prone.

```
expect(content).not.toContain("<br><br>"); ... expect(result.article.content).not.toContain("123 Example Street");
```

#### `src/packages/article-parser/src/linkedin-pre-parser.test.ts`:158

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** SKILL.md 'Avoid Negative Test Assertions' forbids .not.toContain(). This asserts the un-rebuilt run-on text is absent; assert the positive rebuilt paragraph boundaries instead (the test already counts <p> tags and asserts retained text).
- **Verifier:** Line 158 matches exactly. The test already positively counts <p> tags (line 155) and asserts retained text (156-157), so the .not.toContain() negative is exactly the redundant stale-prone pattern the rule prohibits.

```
expect(content).not.toContain("dev positions:\n\n1.");
```

### src/packages/article-store  _(4)_

#### `src/packages/article-store/src/dynamodb-article-store.ts`:68-73

- **Rule:** No time/plan-bound comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound comments that assume a future moment ('Fallback is dropped after a follow-up canary scan...'). Such comments rot because no one updates them; if the fallback genuinely needs revisiting it should be encoded as a failing test or tracked issue, not a comment.
- **Verifier:** Snippet exists at lines 71-72; 'Fallback is dropped after a follow-up canary scan' is exactly the future-tense plan-bound language the rule prohibits.

```
* Legacy rows saved before `pendingSince` existed default to epoch 0 ... Fallback is dropped after a follow-up canary scan reports zero legacy rows still in flight.
```

#### `src/packages/article-store/src/dynamodb-article-store.ts`:76-81

- **Rule:** No time/plan-bound comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound language ('Dropped after a follow-up canary scan reports zero legacy rows'). It encodes a future-tense plan in a comment that will rot rather than in code.
- **Verifier:** Snippet exists at lines 79-80; 'Dropped after a follow-up canary scan reports zero legacy rows' is the same future-plan-bound pattern the rule forbids.

```
* Legacy rows wrote a plain string ... new rows write a JSON-encoded discriminated union. Dropped after a follow-up canary scan reports zero legacy rows.
```

#### `src/packages/article-store/src/dynamodb-article-store.test.ts`:11

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids `as` (and especially the `as unknown as` double-cast) outside the allowed exceptions (as const, isolated Node API wrappers). This bypasses the compiler to force-fit the fake's send into the SDK signature; test files are in scope for this rule.
- **Verifier:** Exact double-cast `as unknown as SendFn` is at line 11; not `as const` and not a Node.js API wrapper, so no documented exception applies.

```
send: (async (input: unknown) => impl(input)) as unknown as SendFn,
```

#### `src/packages/article-store/src/dynamodb-article-store.test.ts`:52

- **Rule:** Avoid TypeScript Type Assertions / faking partial objects with `as` _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' shows that faking a partial object should stay typed as Partial<T> rather than cast back to the full type. Here createFakeClient returns Partial<DynamoDBDocumentClient> but every call site re-casts it to the full DynamoDBDocumentClient via `as`, defeating the Partial<T> exception and hiding any missing-method mismatch. Repeated at every initDynamoDbArticleStore call in the file (e.g. lines 52, 84, 128, 397, 499, 1087).
- **Verifier:** `client as DynamoDBDocumentClient` confirmed at line 52 and recurs at 84,129,149+ (grep); createFakeClient returns Partial<DynamoDBDocumentClient>, so casting back to the full type is precisely the anti-pattern the doc's Partial<T> example warns against.

```
client: client as DynamoDBDocumentClient,
```

### src/packages/check-stuck-articles  _(2)_

#### `src/packages/check-stuck-articles/scripts/collect-stuck-rows.ts`:80-81

- **Rule:** No time-bound / plan-bound comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound comments ('temporary until rollout completes', notes that assume a future cleanup moment). 'Dropped once the canary scan reports zero rows hitting this branch' is a plan-bound note that will rot; if the branch genuinely needs revisiting it should be encoded as a failing test / tracked issue, not a comment.
- **Verifier:** Text present (lines 78-80, citation slightly off but snippet accurate); 'Dropped once...' is exactly the future-cleanup framing CLAUDE.md bans.

```
Legacy disjunct on contentFetchedAt/savedAt — covers rows saved
 *      before pendingSince existed. Dropped once the canary scan reports
 *      zero rows hitting this branch.
```

#### `src/packages/check-stuck-articles/scripts/collect-stuck-rows.ts`:65-66

- **Rule:** No time-bound comments / PR references in comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids time-bound wording ('Bumped to' narrates a past edit, an assumption of a particular moment) and task/PR references embedded in comments. The constraint (the 20-min gate) is the why; the historical 'Bumped to 20 min … documented in #251' narration rots as the code evolves.
- **Verifier:** Snippet present (lines 63-65); 'Bumped to 20 min' narrates a past edit (time-bound stance). Note: the #251 reference itself is a legitimate tracked-issue pointer; the violation is the past-edit narration, hence medium.

```
Generate-summary retry chain is visibility 300s × default maxReceiveCount
 *    3 = 900s = 15 min. Bumped to 20 min to absorb DeepSeek slow periods
 *    documented in #251
```

### src/packages/crawl-article  _(3)_

#### `src/packages/crawl-article/scripts/tier-1-plus-pipeline-health.ts`:30-34

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Environment Variable Access' states to use requireEnv/getEnv from projects/hutch/src/require-env.ts and 'Never use process.env directly.' This script redefines a local requireEnv that reads process.env[name] directly. The only documented exception is playwright.config.* files, which this canary is not.
- **Verifier:** Snippet matches lines 30-34 exactly; the file accesses process.env[name] directly and is a node:test canary, not a playwright.config.* file (the sole documented exception).

```
function requireEnv(name: string): string {
	const value = process.env[name];
	assert(value, `${name} env var is required`);
	return value;
}
```

#### `src/packages/crawl-article/src/x-twitter-preprocessor.ts`:45

- **Rule:** Avoid `as` type assertions at a system boundary (use Zod) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' and 'Runtime Validation with Zod' require validating external API responses at the boundary with a Zod schema, not asserting the shape with `as`. This is the oembed HTTP response body (an external API at a system boundary), so a Zod schema (.safeParse) belongs here instead of `as Record<string, unknown>`. This is not an allowed exception (not `as const`, not an isolated Node API wrapper).
- **Verifier:** Line 45 matches; `text` is the parsed body of an external oembed HTTP response (publish.twitter.com), exactly the 'Zod schema at system boundary (HTTP)' case CLAUDE.md flags, and not an allowed `as` exception.

```
const data = JSON.parse(text) as Record<string, unknown>;
```

#### `src/packages/crawl-article/src/crawl-article.test.ts`:16-19

- **Rule:** Dependency Injection Over Mocks (no jest.mock) _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The test-driven-design skill 'Dependency Injection Over Mocks' rule states: 'Prefer dependency injection over jest.mock(). Mocks couple tests to implementation details.' The PDF size cap should be injected (the production code already injects deps via init* factories) rather than module-mocked.
- **Verifier:** jest.mock at lines 16-19 matches; SKILL.md line 54 explicitly says prefer DI over jest.mock(), and crawl-article.ts imports MAX_PDF_BYTES as a hard module constant (line 13) rather than via the existing initCrawlArticle deps seam, so the limit is mocked instead of injected.

```
jest.mock("./pdf-page-limits", () => ({
	MAX_PDF_BYTES: { bytes: 25 * 1024 * 1024, label: "25 MB" },
	MAX_PDF_PAGES: 300,
}));
```

### src/packages/domain  _(2)_

#### `src/packages/domain/src/user/user-id-prefix.ts`:17

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `as UserIdPrefix` bypasses the compiler to brand a derived string. CLAUDE.md forbids `as` for producing branded values; the sanctioned producer is the brand factory/schema (e.g. route the result through UserIdPrefixSchema.parse) rather than an inline assertion.
- **Verifier:** Line 17 matches; inline `as UserIdPrefix` to brand a computed string; UserIdPrefixSchema exists in same file as the sanctioned producer.

```
return userId.slice(0, USER_ID_PREFIX_LENGTH).toLowerCase() as UserIdPrefix;
```

#### `src/packages/domain/src/article/article.schema.ts`:24

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `Minutes` branded number is produced with `as Minutes` inside `.transform()`. CLAUDE.md's 'Avoid TypeScript Type Assertions' rule applies to branded types and shows `.brand<>()` as the correct mechanism (`z.number().brand<"Minutes">()`); `as` is not an allowed exception here.
- **Verifier:** Line 24 matches; Minutes is a branded number type; `.brand<>()` is the correct mechanism, `as` is not an exception.

```
export const MinutesSchema = z.number().transform((n): Minutes => n as Minutes);
```

### src/packages/hutch-infra-components  _(2)_

#### `src/packages/hutch-infra-components/src/events.ts`:456

- **Rule:** Comments must not be time/plan-bound (no "Phase 2", PR/task references) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md ("Comments Document Why, Not What") forbids plan-bound wording that assumes a particular moment in an implementation plan ("added for the auth migration", "temporary until rollout completes"). "Broadened in Phase 2" pins the comment to a specific delivery phase; it rots once Phase 2 is history and describes the change's timeline rather than the why of the current shape.
- **Verifier:** Snippet verified at line 456; "Broadened in Phase 2" is exactly the plan/phase-bound wording CLAUDE.md prohibits and narrates change history, not the current why.

```
/** Broadened in Phase 2 to carry `userId` so handlers can update the row by
```

#### `src/packages/hutch-infra-components/src/events.ts`:634

- **Rule:** Comments must not be time-bound ("not ... yet", "currently", "for now") _(source: CLAUDE.md)_
- **Why:** CLAUDE.md ("Comments Document Why, Not What") lists time-bound wording ("new", "recently", "for now", "currently", "as of today") as forbidden because it becomes a lie as the code evolves. "is not populated yet" assumes a moment in time and will be stale the instant the field starts being populated; the state should be encoded in code/types rather than narrated in a comment.
- **Verifier:** Snippet verified at line 634; "reserved for a future ... and is not populated yet" is time-bound wording that rots once the optional contentSourceTier field is populated.

```
* "loaded with a more complete version" notification and is not populated yet. */
```

### src/packages/hutch-storage-client  _(2)_

#### `src/packages/hutch-storage-client/src/define-table.ts`:197

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `as Key[]` is a type assertion outside the allowed exceptions (not `as const`, not an isolated Node.js API wrapper). `batchKeys` is `readonly Key[]`; the cast strips `readonly` to satisfy the SDK input type and bypasses the compiler instead of using a typed/copied value (e.g. spreading into a mutable array).
- **Verifier:** Snippet exists at line 197 (finding cited 196, off by one but unambiguous); batchKeys is readonly Key[] and as Key[] strips readonly, not an allowed exception.

```
Keys: batchKeys as Key[],
```

#### `src/packages/hutch-storage-client/src/define-table.ts`:220

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `unprocessed` is derived from `result.UnprocessedKeys?.[tableName]?.Keys` (DynamoDB SDK boundary data typed as a looser record type). Casting it with `as Key[]` bypasses the compiler at a system boundary where a Zod schema / validated mapping is the documented approach, hiding any shape mismatch.
- **Verifier:** Confirmed at line 220; unprocessed comes from SDK UnprocessedKeys (Record<string,unknown>[]-shaped) and is cast to Key[] without validation, not an allowed exception.

```
pending = unprocessed as Key[];
```

### src/packages/provider-contracts  _(4)_

#### `src/packages/provider-contracts/src/events.ts`:9

- **Rule:** Branded Types for Domain IDs _(source: CLAUDE.md)_
- **Why:** This is a user identifier typed as a raw `string` while the `UserId` brand is imported at the top of the same file and used by sibling publishers (e.g. PublishCancelSubscriptionCommand, PublishSubscriptionCancelled). The branded-types rule says to use branded types to prevent mixing up identifiers; a raw `string` here defeats that protection and lets a different string be passed by mistake.
- **Verifier:** Line 8 of events.ts has `userId: string` in PublishExportUserDataCommand while UserId is imported (line 1) and used by siblings; rule applies. (Finding cites line 9 = the `email` line, but the userId offence is real one line up.)

```
export type PublishExportUserDataCommand = (params: {
	userId: string;
```

#### `src/packages/provider-contracts/src/events.ts`:15

- **Rule:** Branded Types for Domain IDs _(source: CLAUDE.md)_
- **Why:** `userId` is a user identifier typed as raw `string` even though the `UserId` brand is imported and used elsewhere in this same file. Using the brand prevents transposing a url/userId or passing an unrelated string.
- **Verifier:** Line 15 is exactly `userId: string` in PublishLinkSaved, sitting beside `url: string`; snippet matches and UserId brand is available — genuine violation.

```
export type PublishLinkSaved = (params: {
	url: string;
	userId: string;
```

#### `src/packages/provider-contracts/src/events.ts`:45

- **Rule:** Branded Types for Domain IDs _(source: CLAUDE.md)_
- **Why:** `userId` is a domain identifier typed as raw `string` while the `UserId` brand is available (imported and used by other publishers in this file). The brand should be used to prevent mix-ups between this and other string fields such as `url`/`title`.
- **Verifier:** Line 45 is `userId: string` in PublishSaveLinkRawHtmlCommand, adjacent to url/title strings; snippet and line exact, brand available — confirmed.

```
export type PublishSaveLinkRawHtmlCommand = (params: {
	url: string;
	userId: string;
```

#### `src/packages/provider-contracts/src/events.ts`:50-51

- **Rule:** Branded Types for Domain IDs _(source: CLAUDE.md)_
- **Why:** `userId` is a user identifier typed as raw `string` although the `UserId` brand is imported and applied to the same concept by the other publish types in this file. The raw string here is inconsistent and loses the mix-up protection the brand provides.
- **Verifier:** Lines 49-51: PublishSaveLinkRawPdfCommand has `url: string` then `userId: string`; snippet and line range exact, brand available — confirmed.

```
export type PublishSaveLinkRawPdfCommand = (params: {
	url: string;
	userId: string;
```

### src/packages/test-fixtures  _(1)_

#### `src/packages/test-fixtures/src/providers/events/in-memory-refresh-article-content.ts`:1, 18

- **Rule:** c8 ignore without an approved reason _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'No Coverage Ignore Comments' states c8 ignore is forbidden unless explicitly approved, and the 'Allowed c8 ignore Cases' list is limited to thin AWS SDK wrappers, CI-only retry callbacks, V8 async artifacts, and V8 block-coverage phantoms. "only used in dev composition root" is not an approved reason; the doc says to restructure code to eliminate untestable branches rather than ignore them. This whole-file ignore is hiding a no-op provider that could be exercised by a test.
- **Verifier:** Whole-file ignore present at lines 1/18; reason 'only used in dev composition root' is not in the four allowed c8-ignore categories, and the no-op provider is plainly testable.

```
/* c8 ignore start -- only used in dev composition root (app.ts) */ ... /* c8 ignore stop */
```

### src/packages/web-shell  _(7)_

#### `src/packages/web-shell/src/send-component.test.ts`:27

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md ("Avoid TypeScript Type Assertions") shows this exact fake-response case: a partial Response must be expressed with `Partial<Response>` (the documented GOOD example), not faked with `as`. The double `as unknown as Response` bypasses the compiler so a Response API change is silently hidden.
- **Verifier:** Line 27 ends `createFakeResponse` with `} as unknown as Response;`; CLAUDE.md's GOOD example for exactly this createFakeResponse case prescribes Partial<Response>, and `as unknown as` is not an allowed exception (as const / isolated Node wrapper).

```
} as unknown as Response;
```

#### `src/packages/web-shell/src/send-component.test.ts`:47

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** A partial Express Request is fabricated via `as unknown as Request`. CLAUDE.md forbids `as` for faking partial objects and prescribes `Partial<Request>` so the subset is explicit and the compiler still checks the shape.
- **Verifier:** Line 47 closes `requestWithAccept` returning a `{ get, accepts }` literal cast `as unknown as Request`; this is the forbidden partial-object-via-as pattern, not an allowed exception.

```
} as unknown as Request;
```

#### `src/packages/web-shell/src/content-negotiation.test.ts`:20

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** The fake Request is cast with `as unknown as Request`, bypassing the compiler. CLAUDE.md requires `Partial<Request>` for partial fakes rather than a type assertion; the `as` is not one of the allowed exceptions (as const / isolated Node wrapper).
- **Verifier:** Line 20 ends `requestWithAccept` with `} as unknown as Request;`, a partial Request faked via double assertion — exactly the prohibited pattern.

```
} as unknown as Request;
```

#### `src/packages/web-shell/src/content-negotiation.test.ts`:49

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** Another partial Request faked with `as unknown as Request` instead of the documented `Partial<Request>`. CLAUDE.md's "Avoid TypeScript Type Assertions" rule applies in test code too.
- **Verifier:** Line 49 matches the snippet verbatim; a two-method partial Request is cast `as unknown as Request`, the forbidden faking-via-as pattern.

```
const req = { get: () => undefined, accepts: () => false } as unknown as Request;
```

#### `src/packages/web-shell/src/shared/extension-suggestion-banner/extension-suggestion-banner.styles.ts`:3-5

- **Rule:** Hardcode hex values — always use the CSS custom properties _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES.md ("Don't: Hardcode hex values — always use the CSS custom properties") forbids hardcoded hex outside base.styles.ts (the declared source of truth). This component-styles file hardcodes the navy hero gradient and white text rather than using the defined tokens (e.g. --color-on-brand for white); base.styles.ts already centralises these values.
- **Verifier:** Lines 3-4 hardcode the navy gradient and #FFFFFF; the file elsewhere (line 132) uses --color-on-brand, proving a token exists, so the literals violate the "never hardcode hex" rule. BRAND_GUIDELINES line 112 documents the gradient value but still mandates CSS custom properties.

```
background: linear-gradient(135deg, #2B3A55 0%, #1E2A40 100%);
    color: #FFFFFF;
```

#### `src/packages/web-shell/src/changelog-banner.test.ts`:155

- **Rule:** Never Rely on querySelector(...).toBeNull() — assert existence then check state _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** test-driven-design SKILL.md forbids proving a hidden/absent state with `querySelector(...).toBeNull()` (a selector typo passes for the wrong reason). The changelog banner is a binary shown/hidden element rendered unconditionally with a --hidden class; the test should assert on the rendered state metadata rather than the hook's null absence.
- **Verifier:** Line 155 matches verbatim and asserts the hook's absence via `.toBeNull()`; this is the named anti-pattern (a `__hook` typo would pass). The test already has the correct state assertion available (lines 152-154 check the --hidden/--visible class), so the empty-content check should also key off rendered metadata.

```
expect(banner.querySelector(".changelog-banner__hook")).toBeNull();
```

#### `src/packages/web-shell/src/shared/extension-suggestion-banner/extension-suggestion-banner.component.test.ts`:138-140

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Asserting the installed-variant hides the CTA via `.toBeNull()` is the fragile pattern the SKILL.md rule names: a selector typo makes the assertion trivially pass. The doc requires asserting the variant element exists and checking its state metadata (here data-test-extension-suggestion-variant='installed' is already rendered) instead of the CTA's absence.
- **Verifier:** Lines 138-140 match verbatim and prove CTA absence via `.toBeNull()`. The installed variant already renders data-test-extension-suggestion-variant='installed' (asserted at lines 124-130), so the state-based alternative the rule mandates exists.

```
expect(
					doc.querySelector("[data-test-extension-suggestion-cta]"),
				).toBeNull();
```

## ⚪ Low severity (86)

### projects/blog-site  _(2)_

#### `projects/blog-site/src/runtime/web/content-signal.middleware.test.ts`:11

- **Rule:** `as` cast faking a partial object instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Avoid TypeScript Type Assertions (`as`)" explicitly calls out faking a partial object with `as` (its example fakes a Response) and prescribes `Partial<T>` instead. `as unknown as Request` is the strongest form of cast bypass; a `Partial<Request>` (or a typed fake) expresses the subset without defeating the compiler. The sibling blog.route.test.ts builds typed `BlogPosts` stubs without any cast, showing the typed-stub path is available here too.
- **Verifier:** Line 11 matches exactly; fabricating a Request via `as unknown as` is the named anti-pattern and the rule lists no test-only exception (note: the cited sibling blog.route.test.ts does not exist, but that does not affect the violation).

```
const req = { method, path } as unknown as Request;
```

#### `projects/blog-site/src/runtime/web/content-signal.middleware.test.ts`:12-19

- **Rule:** `as` cast faking a partial object instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Avoid TypeScript Type Assertions (`as`)" gives the exact anti-pattern of fabricating a `Response` with `as` and requires `Partial<Response>` so the subset is explicit. The fake `res` object is forced to `Response` via `as unknown as`, bypassing the compiler instead of typing it as `Partial<Response>`.
- **Verifier:** Lines 12-19 fabricate a Response via `as unknown as Response`, matching the rule's named Response-faking anti-pattern; no documented test exception applies.

```
const res = {
		set: (name: string, value: string) => { ... },
		vary: (header: string) => { ... },
	} as unknown as Response;
```

### projects/extensions/chrome-extension  _(1)_

#### `projects/extensions/chrome-extension/src/runtime/popup/popup.browser.ts`:447-448

- **Rule:** Comment explains WHAT (justifies an assertion) rather than encoding the why in code _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What': this discretionary comment narrates the type-assertion workaround instead of restructuring the code to carry the why (a typed augmentation). It also rots ('TypeScript DOM types don't include it yet' is a time-bound claim).
- **Verifier:** Lines 447-448 match verbatim; the word 'yet' is a time-bound claim and the comment narrates the assertion workaround rather than encoding a why, violating the comment rule.

```
// navigator.userAgentData is the modern replacement for navigator.platform
// but TypeScript DOM types don't include it yet, so use a type assertion
```

### projects/extensions/firefox-extension  _(4)_

#### `projects/extensions/firefox-extension/src/e2e/login-flow/run.e2e-local.main.ts`:136-143

- **Rule:** Force cast via `as unknown as` _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' lists double `as unknown as` as a compiler-bypassing cast. The installAddon method is fabricated onto the driver type via a force cast rather than a typed wrapper; it is not one of the documented allowed exceptions (as const / isolated Node API wrapper).
- **Verifier:** Lines 136-143 match; `driver as unknown as { installAddon... }` is a double force-cast fabricating a method onto the selenium driver type, the strongest form of the cast the rule prohibits, and not a listed exception.

```
await (
					driver as unknown as {
						installAddon: (
							path: string,
							temporary: boolean,
						) => Promise<void>;
					}
				).installAddon(EXTENSION_DIR, true);
```

#### `projects/extensions/firefox-extension/scripts/build-extension.js`:8,14,29,30

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Environment Variable Access' states 'Never use process.env directly' with the sole documented exception being playwright.config.*.ts. This build CLI is not that exception. Line 14 also uses a `??` default fallback for EXTENSION_VERSION, which the same section forbids ('Never default missing environment variables').
- **Verifier:** Lines 8, 14, 28, 29-30 use process.env directly; this build CLI is not the playwright exception, so the direct-access rule is violated. Note the `??` default claim is weak: line 14 defaults only in dev to '0.0.0' and line 15's assert still fails the prod build if EXTENSION_VERSION is unset — but the primary direct-process.env violation stands.

```
const serverUrl = process.env.HUTCH_SERVER_URL;
...
const version = process.env.EXTENSION_VERSION ?? (isDev ? '0.0.0' : undefined);
```

#### `projects/extensions/firefox-extension/scripts/sync-signed-extension.js`:39-40

- **Rule:** Direct process.env access _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Environment Variable Access' — direct process.env reads instead of requireEnv/getEnv; only playwright config files are exempted.
- **Verifier:** Lines 39-40 match exactly; direct process.env reads in a publish CLI that is not the playwright exception. (Presence is then asserted at 43-44, but the rule targets the direct access itself.)

```
const issuer = process.env.AMO_JWT_ISSUER;
	const secret = process.env.AMO_JWT_SECRET;
```

#### `projects/extensions/firefox-extension/scripts/sync-signed-extension.js`:54,82,91,97,99,118,158,167

- **Rule:** Raw console instead of HutchLogger _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Logging' states never use raw console.log/console.error; use HutchLogger.from(consoleLogger). This publish CLI logs throughout with raw console. (Same applies to console.error in build-extension.js:40, run-tests-with-coverage.js:20, and console.warn in submit-to-amo.js:20.)
- **Verifier:** Verified console.log at 54,82,91,97,98,118,158 and console.error at 167 in sync-signed-extension.js, plus console.error at build-extension.js:40, run-tests-with-coverage.js:20 and console.warn at submit-to-amo.js:20 — all raw console in app-adjacent code with no playwright/test exemption; rule says never use raw console.log/error.

```
console.log(`No updates.json on S3 yet (first run): ...`);
...
console.error("Sync failed:", error.message);
```

### projects/hutch/src/e2e/queue-flow  _(2)_

#### `projects/hutch/src/e2e/queue-flow/run.e2e-local.ts`:15-16

- **Rule:** Environment Variable Access (use requireEnv/getEnv, never process.env directly) _(source: CLAUDE.md)_
- **Why:** Direct `process.env` access. The CLAUDE.md exception is worded only for `playwright.config.*.ts`; this is a `.e2e-local.ts` test file, so it falls outside the literal exception. (The same V8-coverage rationale that motivates the playwright.config exception plausibly extends to any file loaded inside the Playwright process, which is why this is low rather than high; the established e2e composition root `e2e-server.main.ts` does use `requireEnv`.)
- **Verifier:** Lines 15-16 match; CLAUDE.md exception text is literally scoped to `playwright.config.*.ts` only, so this `run.e2e-local.ts` file is technically outside it (low severity is appropriate given the e2e skill itself uses process.env.E2E_PORT directly).

```
assert(process.env.E2E_PORT, "E2E_PORT is required")
const BASE_URL = `http://localhost:${process.env.E2E_PORT}`
```

#### `projects/hutch/src/e2e/queue-flow/banner-on-reader-actions.ts`:128

- **Rule:** Avoid Negative Test Assertions (prefer asserting a positive state class for binary rendering decisions) _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The skill discourages negative assertions and, for binary shown/hidden decisions, prescribes 'render unconditionally, toggle a class, assert on the class' with an explicit positive state (e.g. a `--hidden`/`--dismissed` class) rather than asserting the absence of the `--visible` class. Asserting `.not.toHaveClass(--visible)` passes for the wrong reason if the visibility class is renamed. Same pattern recurs in view-page-actions.ts:115,125,151.
- **Verifier:** Line 128 matches; SKILL.md lines 275-279 do prescribe a positive state class over negative assertions for binary shown/hidden decisions. Low confidence noted: element existence is asserted separately (line 121-122) which mitigates the typo failure mode, but the rename-passes-for-wrong-reason concern the skill targets still applies.

```
await expect(banner).not.toHaveClass(/extension-suggestion-banner--visible/)
```

### projects/hutch/src/runtime/cancel-subscription  _(1)_

#### `projects/hutch/src/runtime/cancel-subscription/cancel-subscription-handler.test.ts`:84,122,160,192,215,234,270,287,303

- **Rule:** Avoid TypeScript type assertions (`as`) _(source: CLAUDE.md)_
- **Why:** The Lambda `Context` argument is faked with `as never`, a type assertion that is not one of the documented allowed exceptions (`as const`, isolated Node.js API wrappers). CLAUDE.md's example for faking a partial object prescribes `Partial<T>` instead of `as`; here the unused framework-mandated positional arg should be typed via a `Partial<Context>` (or a typed helper) rather than cast with `as never`.
- **Verifier:** Confirmed: `{} as never` appears verbatim at all 9 cited lines as the second handler arg; `as never` is a type assertion outside the documented exceptions (as const / isolated Node wrappers), and the 'faking a partial object' guidance prescribes Partial<T>.

```
{} as never,
```

### projects/hutch/src/runtime/checkout-recovery  _(1)_

#### `projects/hutch/src/runtime/checkout-recovery/send-checkout-recovery-emails.cli.main.ts`:1

- **Rule:** c8 ignore without an approved reason _(source: CLAUDE.md)_
- **Why:** The 'No Coverage Ignore Comments' rule states c8 ignore is 'forbidden unless explicitly approved.' The 'Allowed c8 ignore Cases' list enumerates only thin AWS SDK wrappers, CI-only retry callbacks, V8 async function artifacts, and V8 block-coverage phantoms. 'Composition root, no logic to test' is not among the approved categories, so this whole-file ignore lacks a documented-approved reason.
- **Verifier:** Snippet matches line 1 exactly; 'composition root, no logic to test' is not in the enumerated Allowed c8 ignore Cases, so it is a forbidden/unapproved ignore reason.

```
/* c8 ignore start -- composition root, no logic to test */
```

### projects/hutch/src/runtime/domain  _(1)_

#### `projects/hutch/src/runtime/domain/logger.test.ts`:9,22

- **Rule:** Avoid TypeScript type assertions (as) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md (Avoid TypeScript Type Assertions) forbids `as` casts (e.g. it flags `dbResult.Item.url as string`) outside allowed exceptions (as const, isolated Node wrappers). These test-code `as string` casts of jest mock call args fall under none of the documented exceptions.
- **Verifier:** Confirmed: lines 9 and 22 both use `spy.mock.calls[0][0] as string`, a type assertion that matches none of the documented exceptions (as const, isolated Node wrapper); the rule's own example flags an analogous `... as string`.

```
const parsed = JSON.parse(spy.mock.calls[0][0] as string);
```

### projects/hutch/src/runtime/export-user-data  _(1)_

#### `projects/hutch/src/runtime/export-user-data/export-user-data-handler.test.ts`:118-121

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids casting an `unknown` value with `as` to a shape; a zod schema parse at this boundary is the prescribed approach. The same `parsedBody as {...}` cast repeats at lines 169, 184, 273.
- **Verifier:** Verified the `parsedBody as {...}` cast of an `unknown` value at lines 118-121 (parsedBody is typed unknown at line 42), and the repeats at 169, 184, 273; this is a shape assertion with no allowed-exception coverage.

```
const body = upload.parsedBody as { articleCount: number; articles: Array<{ url: string; title: string }>; }
```

### projects/hutch/src/runtime/handle-subscription-cancellation-scheduled  _(1)_

#### `projects/hutch/src/runtime/handle-subscription-cancellation-scheduled/handle-subscription-cancellation-scheduled-handler.test.ts`:65, 95, 124, 150, 169, 197

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** The Lambda Handler's second positional argument is a `Context`. `{} as never` is a type assertion used to fake that object, which the rule forbids: 'Faking a partial object with `as`' is BAD and should use `Partial<T>` (e.g. `{} as Partial<Context>` / a typed fixture). `as never` is not one of the allowed exceptions (`as const`, isolated Node.js API wrappers). Occurs at 6 call sites.
- **Verifier:** Snippet `{} as never,` verified verbatim at all 6 cited lines; `as never` is a real type assertion bypassing the compiler and is not `as const` nor an isolated Node API wrapper, so it falls under the forbidden `as` rule (test-only, unused arg → low severity).

```
{} as never,
```

### projects/hutch/src/runtime/handle-subscription-cancelled  _(5)_

#### `projects/hutch/src/runtime/handle-subscription-cancelled/handle-subscription-cancelled-handler.test.ts`:80

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** The Lambda Context argument is faked with `{} as never`, a type assertion that bypasses the compiler. CLAUDE.md's 'Avoid TypeScript Type Assertions' section explicitly marks faking a partial object with `as` as BAD (its `{ status: 200, ok: true } as Response` example) and lists only `as const` and isolated Node wrappers as allowed exceptions; neither covers `as never`.
- **Verifier:** Snippet and line verified: `{} as never,` at line 80 as the third positional handler arg; `as never` is a type assertion and is not among CLAUDE.md's listed exceptions (`as const`, isolated Node wrapper).

```
{} as never,
```

#### `projects/hutch/src/runtime/handle-subscription-cancelled/handle-subscription-cancelled-handler.test.ts`:130

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** Same `{} as never` Lambda Context fake. Type assertion bypassing the compiler, not covered by the `as const`/isolated-wrapper exceptions in CLAUDE.md's 'Avoid TypeScript Type Assertions' section.
- **Verifier:** Verified at line 130: `{} as never,` as handler Context arg; matches the forbidden `as` assertion pattern, no applicable exception.

```
{} as never,
```

#### `projects/hutch/src/runtime/handle-subscription-cancelled/handle-subscription-cancelled-handler.test.ts`:149

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** Same `{} as never` Lambda Context fake. Type assertion bypassing the compiler, not an allowed exception per CLAUDE.md.
- **Verifier:** Verified at line 149: `{} as never,` as handler Context arg; an `as` assertion outside the documented exceptions.

```
{} as never,
```

#### `projects/hutch/src/runtime/handle-subscription-cancelled/handle-subscription-cancelled-handler.test.ts`:167

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** Same `{} as never` Lambda Context fake. Type assertion bypassing the compiler, not an allowed exception per CLAUDE.md.
- **Verifier:** Verified at line 167: `{} as never,` as handler Context arg; an `as` assertion outside the documented exceptions.

```
{} as never,
```

#### `projects/hutch/src/runtime/handle-subscription-cancelled/handle-subscription-cancelled-handler.test.ts`:193

- **Rule:** Avoid TypeScript Type Assertions (`as`) — faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** Same `{} as never` Lambda Context fake. Type assertion bypassing the compiler, not an allowed exception per CLAUDE.md.
- **Verifier:** Verified at line 193: `{} as never,` as handler Context arg; an `as` assertion outside the documented exceptions.

```
{} as never,
```

### projects/hutch/src/runtime/observability  _(3)_

#### `projects/hutch/src/runtime/observability/subscription-events.test.ts`:86

- **Rule:** Avoid Negative Test Assertions _(source: test-driven-design/SKILL.md)_
- **Why:** The SKILL's 'Avoid Negative Test Assertions' rule names `.not.toContain()` as the bad pattern because it goes stale when the serialized format changes. The positive `toEqual` assertion on lines 79-85 already proves subscription_id is absent, so this negative assertion is the exact redundant/stale form the rule warns against.
- **Verifier:** Snippet matches line 86 verbatim; rule flags `.not.toContain()`, and the exhaustive toEqual on lines 79-85 already proves absence, making this the redundant stale-prone form.

```
expect(JSON.stringify(captured[0])).not.toContain("subscription_id");
```

#### `projects/hutch/src/runtime/observability/analytics-dashboard.test.ts`:202

- **Rule:** Avoid Negative Test Assertions _(source: test-driven-design/SKILL.md)_
- **Why:** The SKILL lists `.not.toContain()` as a bad assertion that becomes stale when the query format changes. The test asserts an absence rather than the actual expected query value, the pattern the rule says to avoid.
- **Verifier:** Snippet matches line 202 verbatim; it is the literal `.not.toContain()` form the rule names, asserting absence of a clause rather than the actual expected query value.

```
expect(q).not.toContain("visitor_hash not in");
```

#### `projects/hutch/src/runtime/observability/analytics-dashboard.test.ts`:149

- **Rule:** Avoid Negative Test Assertions _(source: test-driven-design/SKILL.md)_
- **Why:** The SKILL flags `.not.toContain()` assertions as stale-prone. Asserting the substring is absent (rather than asserting the actual SOURCE form already checked on line 147) matches the bad example in the 'Avoid Negative Test Assertions' rule.
- **Verifier:** Snippet matches line 149 verbatim; line 147 already positively asserts the actual `SOURCE '<name>'` form, so this negative assertion is the redundant stale-prone pattern the rule names.

```
expect(q).not.toContain("logGroups(namePrefix");
```

### projects/hutch/src/runtime/providers/article-store  _(1)_

#### `projects/hutch/src/runtime/providers/article-store/dynamodb-article-store.test.ts`:34, 36, 55

- **Rule:** Avoid type assertions (`as`) — faking SDK client / narrowing with `as` _(source: CLAUDE.md)_
- **Why:** Three further `as` casts bypass the compiler: casting the fake send fn to the SDK type, intersecting the literal client with the full DynamoDBDocumentClient, and narrowing `ExpressionAttributeValues` to `Record<string, unknown>`. CLAUDE.md prohibits `as` outside the documented exceptions (as const, isolated Node API wrappers); none of these qualify. Lower severity because the documented `Partial<DynamoDBDocumentClient>` fake-client test pattern creates real tension at the injection seam.
- **Verifier:** Verified all three: line 34 `as DynamoDBDocumentClient["send"]`, line 36 `as typeof client & DynamoDBDocumentClient`, line 55 `as Record<string, unknown>`; none fall under the two CLAUDE.md `as` exceptions.

```
}) as DynamoDBDocumentClient["send"], ... return { client: client as typeof client & DynamoDBDocumentClient, commands }; ... update?.input.ExpressionAttributeValues as Record<string, unknown>
```

### projects/hutch/src/runtime/providers/article-summary  _(1)_

#### `projects/hutch/src/runtime/providers/article-summary/dynamodb-generated-summary.test.ts`:13-16

- **Rule:** Avoid TypeScript type assertions (`as`) _(source: CLAUDE.md (Avoid TypeScript Type Assertions))_
- **Why:** The fake-client `Partial<DynamoDBDocumentClient>` is cast back to the full client type with `as` (repeated 11 times across the suite). CLAUDE.md endorses `Partial<T>` to make a subset explicit precisely so `as` is not needed; widening the partial back to the full type with `as` re-introduces the assertion the Partial pattern is meant to avoid, hiding any future signature mismatch between the fake and the real client.
- **Verifier:** Snippet exists verbatim at line 14 (within cited 13-16) and recurs 11x; it is a real `as` assertion matching none of CLAUDE.md's allowed exceptions (as const / isolated Node wrappers), and the `as` rule carries no test-only carve-out. SKILL.md endorses the Partial fake-client but never the widening cast.

```
client: client as typeof client & DynamoDBDocumentClient,
```

### projects/hutch/src/runtime/providers/auth  _(1)_

#### `projects/hutch/src/runtime/providers/auth/dynamodb-auth.ts`:186

- **Rule:** Comment explains what, not why _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What': the inline comment restates what the arithmetic computes rather than why. A named constant (e.g. SESSION_TTL_SECONDS) would carry the meaning in code instead of a what-comment.
- **Verifier:** Snippet exact at line 186; '7 days in seconds (TTL)' restates the arithmetic (7*24*60*60) — a what-comment with no why, matching the rule's anti-pattern.

```
const expiresAt = Math.floor(Date.now() / 1000) + 7 * 24 * 60 * 60; // 7 days in seconds (TTL)
```

### projects/hutch/src/runtime/providers/import-session  _(1)_

#### `projects/hutch/src/runtime/providers/import-session/dynamodb-import-session.ts`:1-156

- **Rule:** Allowed c8 ignore Cases — thin AWS SDK wrapper only (3-5 lines, no branching) _(source: CLAUDE.md / test-driven-design SKILL.md)_
- **Why:** The whole-file c8 ignore is justified by CLAUDE.md/SKILL.md only for a 'thin AWS SDK wrapper' that is 'truly trivial (3-5 lines, no branching)'. This file is not thin: toSession (lines 32-46) and toggleImportSelection (lines 112-132) contain real branching selection/pagination logic (allSelected fallbacks, Set construction, index math) that is not a single SDK call. SKILL.md says to extract such mapping logic into a helper and unit-test it directly rather than ignore-wrapping branching code. The blanket ignore over branching domain logic exceeds the documented exception.
- **Verifier:** SKILL.md L320-321 limits the whole-file ignore to 'truly trivial (3-5 lines, no branching)' and mandates extracting mapping logic to a unit-tested helper; toSession (L32-46) and toggleImportSelection (L112-132) carry ?? fallbacks and ternary Set/index branching, so the blanket ignore exceeds the exception.

```
/* c8 ignore start -- thin AWS SDK wrapper, tested via integration */
```

### projects/hutch/src/runtime/providers/oauth  _(1)_

#### `projects/hutch/src/runtime/providers/oauth/dynamodb-oauth-model.ts`:144

- **Rule:** No Silent Fallbacks for Missing Values / No Defensive Checks Without Valid Tests _(source: test-driven-design/SKILL.md)_
- **Why:** `?? ""` provides an empty-string default for a possibly-missing value and is then used as a truthiness gate (`if (refreshToken)` at line 171), masking the absent case rather than modelling it explicitly. The rule forbids `?? defaultValue` fallbacks unless a test exercises the fallback, and the file's blanket `c8 ignore` means no coverage proves the empty branch is exercised.
- **Verifier:** Snippet exact at line 144 and gated by `if (refreshToken)` at 171; no `*.test.ts` exists beside the implementation, so the `?? ""` fallback has no test, matching the forbidden `?? defaultValue` shape.

```
const refreshToken = token.refreshToken ?? "";
```

### projects/hutch/src/runtime/providers/password-reset  _(1)_

#### `projects/hutch/src/runtime/providers/password-reset/dynamodb-password-reset.ts`:15

- **Rule:** Comments Document Why, Not What _(source: CLAUDE.md)_
- **Why:** The inline comment '// 1 hour' restates what the arithmetic 60 * 60 already expresses (a 'what' comment). CLAUDE.md says an approved comment explains why, never what; the value and constant name already carry the meaning, so the comment should be removed.
- **Verifier:** Snippet matches line 15 exactly; '// 1 hour' merely restates 60*60 already named TOKEN_TTL_SECONDS, a what-comment forbidden by CLAUDE.md's 'why, never what' rule.

```
const TOKEN_TTL_SECONDS = 60 * 60; // 1 hour
```

### projects/hutch/src/runtime/providers/rate-limit  _(2)_

#### `projects/hutch/src/runtime/providers/rate-limit/dynamodb-rate-limit.test.ts`:14

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** Double `as unknown as SendFn` cast bypasses the compiler to fake the send function. The doc forbids `as` to fabricate typed objects (allowed exceptions are only `as const` and isolated Node API wrappers). The fake is already typed via `Partial<DynamoDBDocumentClient>`, but the send method is force-cast rather than typed against the real signature.
- **Verifier:** Snippet is verbatim but on line 14, not the cited 15 (line 15 is `};`); the `as unknown as SendFn` cast is real and matches no allowed exception (`as const`/isolated Node wrapper).

```
send: (async (command: unknown) => impl(command)) as unknown as SendFn,
```

#### `projects/hutch/src/runtime/providers/rate-limit/dynamodb-rate-limit.test.ts`:40, 73, 91

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** The fake is correctly built as `Partial<DynamoDBDocumentClient>` (the documented GOOD pattern), but is then re-cast up to the full `DynamoDBDocumentClient` with `as` at three call sites to satisfy `initDynamoDbRateLimit`. This `as` defeats the Partial<T> safety the doc prescribes and is not one of the allowed exceptions (`as const`, isolated Node wrapper).
- **Verifier:** Exact snippet present at all three cited lines 40, 73, 91; widens Partial back to full type via `as`, defeating the doc's prescribed Partial<T> pattern (createFakeResponse example) and matching no allowed exception.

```
}) as DynamoDBDocumentClient,
```

### projects/hutch/src/runtime/providers/reader-ready-state  _(1)_

#### `projects/hutch/src/runtime/providers/reader-ready-state/dynamodb-reader-ready-state.test.ts`:34

- **Rule:** Avoid TypeScript Type Assertions (`as`) — branded domain IDs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md's 'Avoid TypeScript Type Assertions' section gives `const userId = rawValue as UserId` as a BAD example and requires constructing branded IDs via the validated Zod factory (`UserIdSchema.parse(...)`). `UserIdSchema` is already exported from `@packages/domain/user` and used in the production file, so the cast is avoidable.
- **Verifier:** Snippet exists at line 34; matches the exact BAD example in CLAUDE.md, `UserIdSchema` is exported from `@packages/domain/user` and is the dominant convention across sibling tests (UserIdSchema.parse), so the cast is genuinely avoidable.

```
const USER = "abc123" as UserId;
```

### projects/hutch/src/runtime/providers/stripe-subscriptions  _(3)_

#### `projects/hutch/src/runtime/providers/stripe-subscriptions/stripe-subscriptions.test.ts`:28

- **Rule:** Avoid TypeScript Type Assertions (as) _(source: CLAUDE.md)_
- **Why:** `as Record<string, string>` is a type assertion that bypasses the compiler to read header values from the `HeadersInit | undefined` type. It is not one of the allowed exceptions (`as const`, isolated Node.js API wrapper). The captured init could instead be typed/constructed so headers are a `Record<string,string>` without asserting.
- **Verifier:** Snippet exists verbatim at line 28; `as Record<string,string>` on HeadersInit is a genuine assertion, not `as const` nor an isolated-wrapper exception, and the rule's own BAD example flags `as` in test code.

```
const headers = receivedInit?.headers as Record<string, string>;
```

#### `projects/hutch/src/runtime/providers/stripe-subscriptions/stripe-subscriptions.test.ts`:116

- **Rule:** Avoid TypeScript Type Assertions (as) _(source: CLAUDE.md)_
- **Why:** Same `as Record<string, string>` assertion on `receivedInit?.headers` (typed `HeadersInit | undefined`), bypassing the compiler outside the allowed `as const` / isolated-wrapper exceptions.
- **Verifier:** Snippet exists verbatim at line 116; same disallowed `as` assertion on HeadersInit, no documented exception applies.

```
const headers = receivedInit?.headers as Record<string, string>;
```

#### `projects/hutch/src/runtime/providers/stripe-subscriptions/stripe-subscriptions.test.ts`:200

- **Rule:** Avoid TypeScript Type Assertions (as) _(source: CLAUDE.md)_
- **Why:** Same `as Record<string, string>` assertion on `receivedInit?.headers`, a type assertion that hides type mismatches and is not a documented allowed exception.
- **Verifier:** Snippet exists verbatim at line 200; same disallowed `as` assertion on HeadersInit, no documented exception applies.

```
const headers = receivedInit?.headers as Record<string, string>;
```

### projects/hutch/src/runtime/providers/subscription-providers  _(1)_

#### `projects/hutch/src/runtime/providers/subscription-providers/dynamodb-subscription-providers.ts`:150

- **Rule:** Use `assert` for Runtime Invariants (if/throw where assert fits) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Use `assert` for Runtime Invariants' states: use `assert` from `node:assert` for runtime invariant checks instead of `if`/`throw` because it is more concise, communicates intent, and avoids an extra coverage branch for the truthy path. Here a missing GSI row is an invariant guard and could be written `assert(row, ...)`.
- **Verifier:** Snippet matches line 150 exactly; this if/throw guard on a GSI query result is the exact node:assert invariant pattern CLAUDE.md mandates, in production code, no exception applies.

```
if (!row) throw new Error(`No subscription row for subscriptionId ${subscriptionId}`);
```

### projects/hutch/src/runtime/providers/trial-scheduler  _(1)_

#### `projects/hutch/src/runtime/providers/trial-scheduler/aws-trial-scheduler.test.ts`:24-30

- **Rule:** Avoid TypeScript Type Assertions (`as`) / fake partial objects without `as` _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Avoid TypeScript Type Assertions" forbids `as` to bypass the compiler and lists faking objects with `as` (preferring Partial<T>) as a violation. This double `as unknown as SchedulerClient["send"]` fakes the SDK's send signature via assertion rather than a typed seam; it is not one of the allowed exceptions (`as const`, isolated Node API wrapper). The provider already types the dependency as `Pick<SchedulerClient, "send">`, so the fake could be typed to match instead of force-cast.
- **Verifier:** Snippet exists verbatim at lines 24-30; `as unknown as SchedulerClient["send"]` is a compiler-bypassing double-cast, and neither allowed exception (`as const`, isolated Node.js API wrapper) covers an AWS SDK client method.

```
send: (async (cmd: unknown) => { ... }) as unknown as SchedulerClient["send"]
```

### projects/hutch/src/runtime/providers/user-data-export  _(1)_

#### `projects/hutch/src/runtime/providers/user-data-export/user-data-export.types.ts`:2

- **Rule:** Branded Types for Domain IDs _(source: CLAUDE.md)_
- **Why:** userId is a domain ID left as a raw string. The codebase defines a branded `UserId` type (`export type UserId = string & { readonly __brand: "UserId" }` in @packages/domain/user) which is the established convention used across other providers. The caller (export-user-data-handler.ts:66) already produces a branded userId via `UserIdSchema.parse(...)` and passes it here, but this param widens it back to a plain string at the provider boundary, defeating the brand that prevents mixing up identifiers. CLAUDE.md 'Branded Types for Domain IDs' mandates branded types for domain IDs.
- **Verifier:** Snippet exists at line 2; branded UserId convention is real (src/packages/domain/src/user/user.types.ts) and sibling provider contracts use userId: UserId. Note: caller passes raw detail.userId at the call site (line 100), not the branded userId from line 66, but the param type is still a raw string violating the rule.

```
userId: string;
```

### projects/hutch/src/runtime/send-trial-feedback-email  _(1)_

#### `projects/hutch/src/runtime/send-trial-feedback-email/send-trial-feedback-email-handler.test.ts`:72

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** `{} as never` is a type assertion used to fake the Lambda Context argument. The 'Avoid TypeScript Type Assertions' rule forbids using `as` to cast/fake objects outside the allowed exceptions (`as const`, isolated Node API wrappers); the doc's own example shows faking a partial object should use `Partial<T>` instead of an `as` cast. The same `{} as never` pattern recurs at lines 73, 100, 121, 143, 161, 176, 194, 212, 247, 264, 278.
- **Verifier:** Snippet `{} as never,` exists verbatim at line 72 faking the unused Lambda Context; rule forbids `as` casts and only exempts `as const`/isolated Node wrappers, neither of which applies.

```
{} as never,
```

### projects/hutch/src/runtime/stripe-webhook-receiver  _(2)_

#### `projects/hutch/src/runtime/stripe-webhook-receiver/stripe-webhook-receiver-handler.test.ts`:79

- **Rule:** Avoid TypeScript type assertions (`as`); fake partial objects with Partial<T> _(source: CLAUDE.md)_
- **Why:** The Lambda Context argument is faked with `as never`, an `as` assertion that bypasses the compiler. CLAUDE.md forbids `as` outside the allowed exceptions (`as const`, isolated Node wrappers) and shows faking a partial object should use `Partial<T>` (e.g. `{} as Partial<Context>`) instead of an assertion. `as never` is not an allowed exception.
- **Verifier:** Line 79 contains `{} as never` exactly; CLAUDE.md's `as` rule forbids this with no `as never`/test-only exception, and the faked Context matches the 'faking a partial object' BAD example.

```
const result = await handler(buildApiGatewayEvent({ body }), {} as never, () => {});
```

#### `projects/hutch/src/runtime/stripe-webhook-receiver/stripe-webhook-receiver-handler.test.ts`:100, 125, 149, 178, 204

- **Rule:** Avoid TypeScript type assertions (`as`); fake partial objects with Partial<T> _(source: CLAUDE.md)_
- **Why:** Same `as never` fake of the Lambda Context argument repeated across the remaining test cases. CLAUDE.md forbids `as` outside the allowed exceptions and prescribes `Partial<T>` for faking a subset of an object's shape.
- **Verifier:** Verified `{} as never,` appears verbatim on each cited line (100, 125, 149, 178, 204); same forbidden `as` pattern, no documented exception applies.

```
{} as never,
```

### projects/hutch/src/runtime/subscription-charge-failed  _(1)_

#### `projects/hutch/src/runtime/subscription-charge-failed/subscription-charge-failed-handler.test.ts`:49

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** The Lambda Context argument is faked with an `as never` type assertion. The 'Avoid TypeScript Type Assertions' rule forbids `as` outside the allowed exceptions (as const, isolated Node wrappers) and specifically calls out faking a partial object with `as` instead of using Partial<T>. The same `{} as never` cast is repeated at lines 49, 86, 107, 130, and 157.
- **Verifier:** Snippet `{} as never,` exists verbatim at line 49 and recurs at lines 86, 107, 130, 157; it is a type assertion faking the Lambda Context, not on the allowed-exceptions list (as const / isolated Node wrappers), and the rule's own faked-object example is test-shaped, so no test-only carve-out applies.

```
{} as never,
```

### projects/hutch/src/runtime/subscription-charge-succeeded  _(3)_

#### `projects/hutch/src/runtime/subscription-charge-succeeded/subscription-charge-succeeded-handler.test.ts`:55

- **Rule:** Avoid TypeScript Type Assertions (`as`) — fake a partial object with Partial<T>, not a cast _(source: CLAUDE.md)_
- **Why:** The Lambda `context` argument is faked with an `as never` type assertion. CLAUDE.md's 'Avoid TypeScript Type Assertions' rule forbids using `as` to fake objects (shown by the `as Response` BAD example) and directs using `Partial<T>` to make the subset explicit. `as never` is not one of the allowed exceptions (`as const`, isolated Node wrappers).
- **Verifier:** Snippet exists verbatim at line 55; `as never` is an `as` cast, matches no allowed exception (as const / isolated Node wrapper), and the rule has no test-file waiver.

```
{} as never,
```

#### `projects/hutch/src/runtime/subscription-charge-succeeded/subscription-charge-succeeded-handler.test.ts`:81

- **Rule:** Avoid TypeScript Type Assertions (`as`) — fake a partial object with Partial<T>, not a cast _(source: CLAUDE.md)_
- **Why:** Same `as never` cast used to fabricate the Lambda `context` argument; forbidden by the 'Avoid TypeScript Type Assertions' rule, which is not waived for test files and is not among the allowed `as` exceptions.
- **Verifier:** Snippet exists verbatim at line 81; identical `as never` cast on the unused context arg, not an allowed exception, no test-file waiver in the rule.

```
{} as never,
```

#### `projects/hutch/src/runtime/subscription-charge-succeeded/subscription-charge-succeeded-handler.test.ts`:98

- **Rule:** Avoid TypeScript Type Assertions (`as`) — fake a partial object with Partial<T>, not a cast _(source: CLAUDE.md)_
- **Why:** Same `as never` cast used to fabricate the Lambda `context` argument; forbidden by the 'Avoid TypeScript Type Assertions' rule, not covered by the allowed exceptions.
- **Verifier:** Snippet exists verbatim at line 98; same `as never` cast pattern, outside the `as const`/isolated-wrapper exceptions.

```
{} as never,
```

### projects/hutch/src/runtime/web  _(1)_

#### `projects/hutch/src/runtime/web/reader-ready-email.template.html`:6,15

- **Rule:** No exclamation marks in confirmations or status messages _(source: BRAND_GUIDELINES.md)_
- **Why:** Brand voice rule: 'No exclamation marks in confirmations or status messages. Save those for moments that actually warrant excitement.' This is a routine status notification (an already-saved article finished its reader view), the same category as the 'Article saved.' confirmation example, not the import-complete celebration the brand reserves exclamation marks for. The exclamation appears in both the <title> and the in-body <h1>.
- **Verifier:** Verified: line 6 <title> and line 15 <h1> both read '...reader view!'; BRAND_GUIDELINES.md:272 bars exclamation marks in status messages, and this is a processing-complete status notice, not the reserved import-complete celebration.

```
<h1 ...>An article you saved now has a reader view!</h1>
```

### projects/hutch/src/runtime/web/api  _(1)_

#### `projects/hutch/src/runtime/web/api/api.routes.test.ts`:20

- **Rule:** Avoid TypeScript type assertions (`as`) — brand IDs via validated factory, not `as` _(source: CLAUDE.md)_
- **Why:** CLAUDE.md ("Branded Types for Domain IDs" / "Avoid TypeScript Type Assertions") shows branded IDs must come from a validated factory (e.g. `UserIdSchema.parse(...)`), not a raw `as UserId` cast which bypasses the brand's intended guard. `as const`/isolated-wrapper exceptions do not cover this.
- **Verifier:** Exact match at line 20; raw `as UserId` brand cast, not covered by the listed `as const`/isolated-wrapper exceptions; rule has no test-file carve-out.

```
const TEST_USER_ID = "test-user-123" as UserId;
```

### projects/hutch/src/runtime/web/auth  _(1)_

#### `projects/hutch/src/runtime/web/auth/trial-feedback-email.ts`:39

- **Rule:** No emojis in product copy _(source: BRAND_GUIDELINES.md)_
- **Why:** Brand guidelines: "No emojis in UI" and the Email section gives no exemption; copy includes a handshake emoji and ":D" emoticon. Also borders the all-caps rule ("ALL") which is reserved for short labels only. Low severity because emails are a softer, personal channel.
- **Verifier:** Line 39 contains the handshake emoji and ":D"; BRAND_GUIDELINES.md restricts the emoji exemption to "social posts or community replies" only, and a transactional trial-feedback email is neither — it is product copy.

```
I respond to ALL replies personally, this is my promise 🤝 :D
```

### projects/hutch/src/runtime/web/middleware  _(3)_

#### `projects/hutch/src/runtime/web/middleware/ban.test.ts`:9-17

- **Rule:** Avoid TypeScript Type Assertions (as) — fake partial objects with Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md's 'Avoid TypeScript Type Assertions' section forbids faking a partial object with `as` and shows `Partial<T>` as the correct alternative (the `createFakeResponse(): Partial<Response>` example). The sibling rate-limit.test.ts already uses the documented `Partial<Request>` pattern, so the double-cast here is avoidable.
- **Verifier:** Snippet verified at lines 9-17; double-cast `as unknown as Response/Request/NextFunction` is the forbidden fake-partial pattern, not a documented exception; sibling rate-limit.test.ts line 29 uses Partial<Request>.

```
const res = { status: ..., end: jest.fn() } as unknown as Response;
	const req = { ip: "1.2.3.4" } as unknown as Request;
	const next = jest.fn() as unknown as NextFunction;
```

#### `projects/hutch/src/runtime/web/middleware/error-handler.test.ts`:8-14

- **Rule:** Avoid TypeScript Type Assertions (as) — fake partial objects with Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md forbids faking a partial object with `as` and mandates `Partial<T>` instead. These `as unknown as` double-casts (and `{} as Request`) bypass the compiler where `Partial<HutchLogger>`/`Partial<Response>`/`Partial<Request>` would express the subset explicitly.
- **Verifier:** Verified lines 8, 11, 14: `as unknown as HutchLogger`, `as unknown as Response`, and `{} as Request` all fake partial objects via `as`, the exact forbidden pattern; no allowed exception applies.

```
const logger = { error: (msg: string) => errorCalls.push(msg) } as unknown as HutchLogger;
		const res = { status: statusMock, json: jsonMock } as unknown as Response;
		logAndRespondOnError(logger)(new Error("boom"), {} as Request, res, next);
```

#### `projects/hutch/src/runtime/web/middleware/analytics.test.ts`:43

- **Rule:** Avoid TypeScript Type Assertions (as) — fake partial objects with Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md's 'Avoid TypeScript Type Assertions' section forbids faking partial objects via `as`, recommending `Partial<T>`. The `base as unknown as Request` double-cast fakes a Request and bypasses the compiler instead of typing the stand-in as `Partial<Request>`.
- **Verifier:** Verified line 43: `base` (a partial Request stand-in built lines 34-42) is returned via `as unknown as Request`, the forbidden fake-partial cast; not `as const` or an isolated Node wrapper exception.

```
return base as unknown as Request;
```

### projects/hutch/src/runtime/web/oauth  _(4)_

#### `projects/hutch/src/runtime/web/oauth/oauth.component.ts`:48

- **Rule:** Off-scale border-radius instead of a defined radius token _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES Border Radius defines only 6px (--radius-sm), 8px (--radius), 12px (--radius-lg) and says to use the CSS custom properties from base.styles.ts. 4px is an arbitrary off-scale value; buttons should use var(--radius-sm).
- **Verifier:** Line 48 has 'border-radius: 4px;' on .oauth-authorize__btn; the radius scale (lines 191-195) starts at 6px (--radius-sm for buttons), so 4px is off the radius token scale and hardcoded rather than using the custom property.

```
border-radius: 4px;
```

#### `projects/hutch/src/runtime/web/oauth/oauth.routes.test.ts`:333

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids casting a partial literal with `as` (the `as Response` example) and requires Partial<T> to make the subset explicit. A partial token literal is cast to the full Token type via `as`.
- **Verifier:** Off by one — '} as Token,' is at line 332, not 333 (line 333 is 'client,') — but the violation is real and unambiguous: a 4-field token literal cast to Token via `as`, matching the forbidden `{...} as Response` pattern in CLAUDE.md; the rule has no test-code exemption.

```
} as Token,
```

#### `projects/hutch/src/runtime/web/oauth/oauth.routes.test.ts`:371

- **Rule:** Faking a partial object with `as` instead of Partial<T> _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids casting a partial literal with `as` and requires Partial<T>. A partial token literal is cast to the full Token type via `as`.
- **Verifier:** Line 371 is exactly '} as Token,' closing a 4-field token literal cast to Token; matches the forbidden partial-object-via-`as` pattern, no documented exception applies.

```
} as Token,
```

#### `projects/hutch/src/runtime/web/oauth/oauth.routes.ts`:141

- **Rule:** Missing Zod validation at an HTTP boundary _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Runtime Validation with Zod' requires validating external input at system boundaries (HTTP requests). The /revoke handler reads req.body.token directly with only a truthy check, while sibling handlers (/authorize) validate req.query/req.body via zod schemas — the request body here is unparsed external input.
- **Verifier:** Line 141 reads req.body.token with only a truthy guard (line 142); no zod schema, unlike the /authorize handlers which use authorizeQuerySchema/denyBodySchema.safeParse. CLAUDE.md mandates zod at HTTP boundaries, so this is a genuine (low-severity) inconsistency.

```
const token = req.body.token;
```

### projects/hutch/src/runtime/web/onboarding  _(1)_

#### `projects/hutch/src/runtime/web/onboarding/extension-install.ts`:32

- **Rule:** Comments document why, not what _(source: CLAUDE.md)_
- **Why:** CLAUDE.md "Comments Document Why, Not What" requires comments to explain why, not restate behaviour evident from the code. This comment restates what the function returns (an install URL, or undefined when the extension is present) which the signature and one-line body already convey.
- **Verifier:** Line 32 comment restates the function's return contract (URL, or undefined when installed) already evident from the name `extensionInstallUrlIfMissing` and the two-line body at lines 34-35; documents what, not why.

```
/** Install URL the reader-failed slot should link to, or undefined when the user already has the extension. */
```

### projects/hutch/src/runtime/web/pages/home  _(4)_

#### `projects/hutch/src/runtime/web/pages/home/home.route.test.ts`:302

- **Rule:** Avoid Negative Test Assertions (.not.toContain becomes stale on refactor) _(source: test-driven-design/SKILL.md)_
- **Why:** Negative assertion the doc warns against: it goes stale and asserts nothing about the actual rendered provider list. Assert the expected provider names positively instead.
- **Verifier:** Line 302 matches verbatim; SKILL.md 'Avoid Negative Test Assertions' names .not.toContain() as the anti-pattern, and positive provider assertions (DeepSeek/Tesseract) already sit alongside it.

```
expect(text).not.toContain("Deep Infra");
```

#### `projects/hutch/src/runtime/web/pages/home/home.static.route.test.ts`:69

- **Rule:** Avoid Negative Test Assertions (.not.toContain becomes stale on refactor) _(source: test-driven-design/SKILL.md)_
- **Why:** Negative full-text assertion that goes stale if copy changes; the doc prefers asserting the actual expected value (e.g. that the title element is absent via a rendered state class).
- **Verifier:** Line 69 matches verbatim; negative .not.toContain on rendered copy, the exact stale-on-refactor pattern SKILL.md lines 277-287 warn against.

```
expect(response.text).not.toContain(`Free for the first ${TEST_FOUNDING_MEMBER_LIMIT} members.`);
```

#### `projects/hutch/src/runtime/web/pages/home/home.styles.css`:39-57

- **Rule:** Comments Document Why, Not What (no orphaned/what comments) _(source: CLAUDE.md)_
- **Why:** The CSS comment index lists point 1 ('Uppercase for short eyebrow labels') but no rule in the block applies text-transform: uppercase and no /* 1 */ reference exists in the code. It is an orphaned what-comment that has rotted away from the code it claims to explain (per the indexed-reference convention each numbered point must map to a referenced line).
- **Verifier:** Verified: the .section-header block (lines 39-65) carries /* 2 */ (line 54) and /* 3 */ (line 63) but no /* 1 */ and no text-transform: uppercase anywhere; grep confirms no eyebrow selector exists, so point 1 is orphaned per CLAUDE.md indexed-reference convention.

```
/** 1. Uppercase for short eyebrow labels per brand guidelines ... */ .section-header__title { ... line-height: 1.3; /* 2 */ }
```

#### `projects/hutch/src/runtime/web/pages/home/home.styles.css`:13, 249, 448, 558, 672, 834, 908, 943

- **Rule:** Hardcode hex values — always use the CSS custom properties _(source: BRAND_GUIDELINES.md)_
- **Why:** Brand guidelines list 'Hardcode hex values' under Don't and say 'never hardcode colour values'. The navy #2B3A55 heading colour is repeated as a literal across many selectors. (Mitigating: the palette table lists navy with no dedicated CSS variable, and the hero gradient literal on line 74 is explicitly sanctioned by the brand doc, so those gradient stops are not flagged.)
- **Verifier:** grep confirms 'color: #2B3A55;' at exactly lines 13, 249, 448, 558, 672, 834, 908, 943 (8 occurrences); BRAND_GUIDELINES.md Don't list (line 339) 'Hardcode hex values — always use the CSS custom properties' is unqualified, and navy has no CSS variable so the literal is the only hook — but the rule still forbids it.

```
color: #2B3A55;
```

### projects/hutch/src/runtime/web/pages/install  _(1)_

#### `projects/hutch/src/runtime/web/pages/install/install.component.ts`:69-71

- **Rule:** Comments document why, not what (discretionary comment should be code) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md states 'An approved comment explains why, never what' and treats discretionary comments as a last resort requiring explicit human approval. The first sentences here describe what the type already conveys (a setup step; `note` is an optional caveat shown under the instruction) rather than a non-obvious why. The `BetaSetupStep` interface with named `title`/`note?` fields is self-documenting; the descriptive prose is discretionary and should be removed or the intent encoded in the code/types.
- **Verifier:** Snippet verified exact at lines 69-71; first clause restates what the self-documenting `BetaSetupStep { title; note? }` interface already conveys, making it a discretionary 'what' comment the rule discourages.

```
/** One numbered step in the iPhone beta setup guide. `note` is an optional
 * caveat shown under the instruction (e.g. why the app must be opened once
 * before the iOS share option appears). */
```

### projects/hutch/src/runtime/web/pages/queue  _(1)_

#### `projects/hutch/src/runtime/web/pages/queue/queue.reader.advanced.route.test.ts`:668

- **Rule:** Avoid Negative Test Assertions _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The skill's 'Avoid Negative Test Assertions' rule states `.not.toContain()` becomes stale on refactor and should be replaced by asserting the exact expected value. The preceding line already asserts the positive value (`toContain('...this post!')`), making the negative assertion redundant and brittle.
- **Verifier:** Line 668 is `.not.toContain("...share this view!")`; line 667 already asserts the exact positive value, so the negative assertion is the redundant/brittle pattern SKILL.md 277-289 forbids.

```
expect(hints).not.toContain("Click here to share this view!");
```

### projects/hutch/src/runtime/web/pages/save  _(1)_

#### `projects/hutch/src/runtime/web/pages/save/save-error.component.ts`:12-23

- **Rule:** Browser JS must be a same-origin bundled <script src>, not inlined _(source: web/SKILL.md)_
- **Why:** The web skill's 'Browser JS Is Bundled and Served Same-Origin' / 'Client-Side JavaScript Conventions' rules require behaviour htmx cannot express (here, the countdown animation) to live in a compiled *.client.ts bundle referenced via a relative same-origin <script src="/...">. This countdown logic is instead emitted as a hand-written inline <script> string injected into the page, which is the inlining pattern the skill steers away from (no bundle, ships as raw inline JS rather than the runtime asset tree).
- **Verifier:** Snippet matches lines 12-23 exactly; COUNTDOWN_SCRIPT is an inline <script> string injected via scripts: at line 41, running an animation (the exact case SKILL.md line 137 reserves for *.client.ts) instead of a same-origin bundled <script src>, violating the rule and Pre-Commit Checklist line 275 ('not inlined').

```
const COUNTDOWN_SCRIPT = `<script>
(function() {
  var el = document.querySelector('.save-error__seconds');
  ...
})();
</script>`;
```

### projects/hutch/src/runtime/web/pages/view  _(1)_

#### `projects/hutch/src/runtime/web/pages/view/view.route.test.ts`:895

- **Rule:** Never Rely on querySelector(...).toBeNull() — assert existence first, then check state _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The doc explicitly forbids proving 'this element should be hidden/absent' with querySelector(...).toBeNull(): a typo in the selector also returns null, so the assertion passes for the wrong reason. The share-balloon component is unconditionally renderable, so the test should look the element up and check a visibility state class/attribute (or, since the balloon is genuinely absent on the landing page, assert on the positive elements the landing page does render) rather than relying on a null DOM lookup.
- **Verifier:** Snippet matches line 895 exactly; SKILL.md lines 249-275 forbid this exact pattern, extending it to any binary shown/absent rendering decision — a selector typo would falsely pass.

```
expect(doc.querySelector("[data-test-share-balloon]")).toBeNull();
```

### projects/hutch/src/runtime/web/shared/article-body  _(1)_

#### `projects/hutch/src/runtime/web/shared/article-body/progress-bar.client.ts`:36

- **Rule:** Named Parameters Over Positional When Types Repeat _(source: test-driven-design/SKILL.md)_
- **Why:** Two consecutive parameters of the same type (BarTick) are passed positionally. The rule requires a named parameter object when a signature has 2+ consecutive same-typed parameters, because prev and next are trivial to swap by accident (connascence of position). Should be e.g. computeRate({ prev, next }).
- **Verifier:** Snippet verified verbatim at line 36; both params are BarTick (consecutive, same type), and swapping prev/next inverts the rate, exactly the connascence-of-position hazard the rule targets.

```
export function computeRate(prev: BarTick, next: BarTick): number {
```

### projects/hutch/src/runtime/web/shared/article-body/reader-slot  _(1)_

#### `projects/hutch/src/runtime/web/shared/article-body/reader-slot/reader-slot.component.ts`:45-48

- **Rule:** No plan-bound / time-bound comments _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' forbids plan-bound comments that assume a future task ('(follow-up)'). Such notes rot when the surrounding code changes; a tracked issue or failing test should encode the intent instead of a comment.
- **Verifier:** Snippet present in the lines 39-48 comment block; the '(follow-up)' phrasing is exactly the plan-bound future-task wording CLAUDE.md says will rot.

```
When the article aggregate gains a persisted `mediaType` field (follow-up), the hint will read that instead
```

### projects/hutch/src/runtime/web/shared/article-reader  _(1)_

#### `projects/hutch/src/runtime/web/shared/article-reader/article-reader.test.ts`:78-81

- **Rule:** Do not leave time-bound or plan-bound comments in the code _(source: CLAUDE.md (Comments Document Why, Not What))_
- **Why:** Plan/history-bound comment narrating a past change ('Auto-heal was removed'). CLAUDE.md forbids leftover notes that reference a particular moment in time or a prior implementation; it rots when the surrounding code evolves. The invariant ('counters never tick') is already encoded by the assertions, so the historical narration adds nothing durable.
- **Verifier:** Comment at lines 78-79 matches; 'Auto-heal was removed' narrates a past implementation change, which CLAUDE.md's time/plan-bound comment rule prohibits.

```
// Auto-heal was removed: the reader no longer marks anything pending.
// The counters stay so the regression test can assert they don't tick.
```

### projects/save-link  _(1)_

#### `projects/save-link/src/runtime/providers/pdf-page-ocr/init-tesseract-ocr.ts`:1

- **Rule:** c8 ignore without an approved reason _(source: CLAUDE.md)_
- **Why:** CLAUDE.md's 'Allowed c8 ignore Cases' enumerates only thin AWS SDK wrappers, CI-only retry callbacks, V8 async artifacts, and V8 block-coverage phantoms. A whole-file ignore justified by 'c8 cannot merge coverage from two runners' is none of these; the file contains real branching logic (resolveTessdataDir, discoverInstalledScripts, buildLanguageFlag, error handling) that is being excluded wholesale rather than restructured/tested in one runner. Coverage-ignore is forbidden unless explicitly approved.
- **Verifier:** Line 1 matches exactly; whole-file c8 ignore reason (two-runner merge) is not among the four enumerated allowed cases, and the file has real branching/error-handling logic.

```
/* c8 ignore start -- tested via Jest unit tests + node:test integration; c8 cannot merge V8 coverage from both runners (bcoe/c8#126) */
```

### projects/web-embed  _(1)_

#### `projects/web-embed/src/runtime/embed/preview.styles.css`:46,56,57

- **Rule:** Never hardcode hex values — always use the CSS custom properties _(source: BRAND_GUIDELINES.md)_
- **Why:** BRAND_GUIDELINES 'Do's and Don'ts' says 'Hardcode hex values — always use the CSS custom properties.' These rules hardcode #FFFFFF and #1A202C even though the same file's :root (embed-base.styles.css) already defines --color-background (#FFFFFF) and --color-text-primary (#1A202C), so the literals should reference the existing custom properties.
- **Verifier:** Lines 46 (#FFFFFF), 56 (#1A202C) and 57 (#FFFFFF) hardcode hex; embed-base.styles.css :root defines --color-background:#FFFFFF and --color-text-primary:#1A202C, so the literals duplicate existing tokens. BRAND_GUIDELINES Don't list forbids hardcoded hex. Low severity, no documented exception.

```
.embed-preview__stage--white {
  background: #FFFFFF;
...
.embed-preview__stage--dark {
  background: #1A202C;
  color: #FFFFFF;
```

### src/packages/article-store  _(3)_

#### `src/packages/article-store/src/dynamodb-article-store.ts`:24-33

- **Rule:** Comments explain WHAT not WHY _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Comments Document Why, Not What' makes discretionary comments a last resort requiring explicit human approval; this multi-line block largely narrates what the schema and UpdateExpression do (descriptive prose) rather than encoding the why in code (e.g. an assert or test).
- **Verifier:** Block exists at 24-33; it is largely descriptive prose ('the UpdateExpression below only writes...') restating what the code does, qualifying as a discretionary what-comment, though it does carry some genuine why — hence low severity.

```
Row schema for the Article aggregate. Each attribute is `dynamoField` because legacy rows may be missing any of these ... Fields outside this schema ... are owned by separate writers and are *not* touched by the aggregate save ...
```

#### `src/packages/article-store/src/dynamodb-article-store.test.ts`:404

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids `as` casts of captured values; the test casts the captured `received: unknown` into an ad-hoc shape instead of validating it (e.g. via a Zod schema at the boundary). This `received as {...}` pattern recurs throughout the save tests (e.g. lines 510, 569, 618, 1098, 1131).
- **Verifier:** Cast confirmed verbatim at line 404 and recurs at 510,569,593,618 (grep); a captured-value `as` cast with no applicable exception.

```
const command = received as { input: { Key?: Record<string, unknown> } };
```

#### `src/packages/article-store/src/dynamodb-article-store.test.ts`:849-851

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' forbids `as string` to coerce a Record<string, unknown> value before JSON.parse; this bypasses the compiler rather than validating the value. Recurs at the analogous assertions (e.g. lines 1149-1151, 1184-1186, 1259-1262).
- **Verifier:** `as string` coercion confirmed at lines 848-850 (cited 849-851 spans the same expression); a value-coercion `as` cast outside any documented exception.

```
command.input.ExpressionAttributeValues?.[":summaryFailureReason"] as string,
```

### src/packages/crawl-article  _(4)_

#### `src/packages/crawl-article/src/pdf-extract.types.ts`:34-35

- **Rule:** Internal contract weakened 'for backward compatibility' _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The 'Internal Contracts Are Not Versioned — Prefer Required Fields' rule states internal provider/function types (PdfExtractProgress is consumed only inside the monorepo) carry no backward-compatibility obligation and must not be weakened 'for backward compatibility' (an optional field lets a producer silently omit the value and defers failure to runtime). The justifying comment cites backward compatibility for an in-repo provider type.
- **Verifier:** Comment at lines 34-35 matches and `stage?` is optional on the PdfExtractProgress provider type (line 40); SKILL.md lines 139-143 forbid weakening an internal monorepo-only contract 'for backward compatibility', which this comment explicitly cites.

```
* The field is optional for backward compatibility: when omitted, the orchestrator
 * keeps the prior "latch on first onProgress" behaviour.
```

#### `src/packages/crawl-article/src/aia-fetch.test.ts`:108

- **Rule:** Avoid `as` type assertions _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' forbids `as` casts (the `as unknown as T` double-cast is the strongest form of bypassing the compiler). A jest.fn() typed to the function shape, or a parameter typed to accept the fake, avoids the assertion. Repeated at lines 122, 134, 154, 167, 180, 197. Not an allowed exception.
- **Verifier:** Line 108 matches verbatim and the `as unknown as` double-cast recurs at 122/134/154/167/180; CLAUDE.md's `as` rule carries no test-code exemption (only `as const` and isolated Node wrappers), so the double-cast is in scope.

```
const wrapped = withAiaChasing(baseFetch, fetchAia as unknown as ReturnType<typeof initFetchAia>);
```

#### `src/packages/crawl-article/src/persona-fallback.test.ts`:60

- **Rule:** Avoid `as` type assertions _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' forbids `as` casts. The fetch init.headers union (HeadersInit) is asserted to Record<string, string> rather than normalised via `new Headers(...)` (as the production code does in toPlainHeaders). Repeated at lines 76, 93, 146, 161. Not an allowed exception.
- **Verifier:** Line 60 matches and the `init?.headers as Record<string, string>` cast recurs at 76/93/146/161; production persona-fallback.ts normalises headers via `new Headers(...)` (line 52), confirming a non-`as` alternative exists, and this is not an allowed exception.

```
calls.push({ headers: { ...(init?.headers as Record<string, string>) } });
```

#### `src/packages/crawl-article/src/crawl-fetch-ssrf.test.ts`:73

- **Rule:** Avoid `as` type assertions _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions (as)' forbids `as`. server.address() returns `string | AddressInfo | null`; the code asserts AddressInfo rather than asserting/guarding the shape (e.g. assert non-null and a typeof check). Same pattern at h2-fetch.test.ts:18. Not an allowed exception.
- **Verifier:** Line 73 matches; AddressInfo is imported as a type (line 2) and `server.address()` returns `string | AddressInfo | null`, so the `as AddressInfo` cast bypasses the union check — same pattern confirmed at h2-fetch.test.ts:18 — and is not an allowed `as` exception.

```
const { port } = server.address() as AddressInfo;
```

### src/packages/domain  _(1)_

#### `src/packages/domain/src/article/estimated-read-time.ts`:7,9

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** Both returns brand a computed number with `as Minutes`, bypassing the compiler. CLAUDE.md forbids `as` for branded domain values; the value should flow through the brand factory (MinutesSchema/`.brand<>()`) so the brand cannot drift from the schema. Lower severity because it is a pure compute result with no external boundary, but it is still an `as` assertion outside the documented exceptions.
- **Verifier:** Lines 7 and 9 match exactly; both `as Minutes` cast computed numbers; not `as const` nor an isolated Node API wrapper, so outside exceptions.

```
return 1 as Minutes;
return Math.max(1, Math.ceil(wordCount / WORDS_PER_MINUTE)) as Minutes;
```

### src/packages/hutch-storage-client  _(2)_

#### `src/packages/hutch-storage-client/src/dynamo-field.ts`:14

- **Rule:** Avoid TypeScript Type Assertions (`as`) _(source: CLAUDE.md)_
- **Why:** Branded types are supposed to be produced via a validated factory (e.g. `z.string().brand<...>()` / `z.infer`), not via an `as` intersection cast. This `as` fabricates the `__dynamoField` brand by assertion outside the allowed exceptions (as const / isolated Node wrapper).
- **Verifier:** Confirmed at line 14; brand is fabricated via `as` intersection rather than a validated factory, outside the as const / Node-wrapper exceptions.

```
return wrapped as typeof wrapped & { readonly __dynamoField: true };
```

#### `src/packages/hutch-storage-client/src/define-table.ts`:41

- **Rule:** Comments Document Why, Not What (an approved comment explains why, never what) _(source: CLAUDE.md)_
- **Why:** Pure what-comment: it restates that `update` performs an update passthrough, adding no why. The rule says remove such comments rather than describe behavior obvious from the code.
- **Verifier:** Confirmed at line 41; pure what-comment that restates the method name with no why, exactly what the rule says to remove.

```
/** Raw Update passthrough. */
```

### src/packages/test-fixtures  _(3)_

#### `src/packages/test-fixtures/src/providers/email-verification/email-verification.schema.ts`:4

- **Rule:** Avoid `as` casts at a system boundary where a zod schema belongs _(source: CLAUDE.md)_
- **Why:** CLAUDE.md 'Avoid TypeScript Type Assertions' gives the GOOD boundary example as `z.string().brand<'UserId'>()` and lists `const userId = rawValue as UserId` as BAD. This zod transform uses an `as` cast to brand the value, which is not one of the allowed exceptions (`as const`, isolated Node.js wrapper). The sibling provider google-auth.schema.ts demonstrates the compliant pattern `z.string().brand<"GoogleId">()`.
- **Verifier:** Line 4 uses `s as VerificationToken`; brand type defined as `string & { __brand: 'VerificationToken' }`, so `z.string().brand<'VerificationToken'>()` (as in sibling google-auth.schema.ts) is the documented GOOD alternative.

```
export const VerificationTokenSchema = z.string().transform((s): VerificationToken => s as VerificationToken);
```

#### `src/packages/test-fixtures/src/providers/password-reset/password-reset.schema.ts`:4

- **Rule:** Avoid `as` casts at a system boundary where a zod schema belongs _(source: CLAUDE.md)_
- **Why:** Same as above: the boundary schema brands via an `as` cast rather than zod's `.brand<>()`. CLAUDE.md's 'Avoid TypeScript Type Assertions' rule lists this `as`-at-boundary form as BAD and shows the zod brand factory as the GOOD alternative; the cast is not within the allowed-exceptions table.
- **Verifier:** Line 4 uses `s as PasswordResetToken`; brand type is `string & { __brand: 'PasswordResetToken' }`, replaceable by `.brand<'PasswordResetToken'>()`; not an allowed `as` exception.

```
export const PasswordResetTokenSchema = z.string().transform((s): PasswordResetToken => s as PasswordResetToken);
```

#### `src/packages/test-fixtures/src/providers/stripe-checkout/stripe-checkout.schema.ts`:7

- **Rule:** Avoid `as` casts at a system boundary where a zod schema belongs _(source: CLAUDE.md)_
- **Why:** Same pattern: the schema brands the validated string with an `as CheckoutSessionId` cast instead of zod's `.brand<>()`. CLAUDE.md 'Avoid TypeScript Type Assertions' classifies an `as Brand` at a system boundary as BAD and points to the zod brand factory; this cast falls outside the documented exceptions.
- **Verifier:** Line 7 uses `s as CheckoutSessionId` (after .min(1)); brand type `string & { __brand: 'CheckoutSessionId' }` makes `.min(1).brand<'CheckoutSessionId'>()` the compliant alternative; not an allowed `as` exception.

```
.transform((s): CheckoutSessionId => s as CheckoutSessionId);
```

### src/packages/web-shell  _(5)_

#### `src/packages/web-shell/src/banner-state.test.ts`:12

- **Rule:** Avoid TypeScript type assertions (`as`) _(source: CLAUDE.md)_
- **Why:** A branded UserId is produced with a double `as unknown as` assertion. CLAUDE.md forbids `as` for branding domain IDs and prescribes a Zod-validated branded factory (z.string().brand()) instead; this case is not one of the allowed exceptions.
- **Verifier:** Line 12 matches verbatim; it brands a literal via `as unknown as`, which CLAUDE.md's branded-ID guidance forbids in favour of a validated factory. The inline comment notes the shell has no domain UserId schema, but that does not make `as` an allowed exception.

```
const USER_ID = "user-1" as unknown as NonNullable<BannerStateSource["userId"]>;
```

#### `src/packages/web-shell/src/shared/extension-suggestion-banner/extension-suggestion-banner.styles.ts`:123,165

- **Rule:** Hardcode hex values — always use the CSS custom properties _(source: BRAND_GUIDELINES.md)_
- **Why:** Further hardcoded #FFFFFF literals in a component styles file. BRAND_GUIDELINES.md requires the CSS custom properties (e.g. --color-on-brand) instead of literal hex outside the base.styles.ts source of truth.
- **Verifier:** Line 123 is `text-decoration-color: #FFFFFF;` and line 165 is `color: #FFFFFF;`; both are hardcoded hex where --color-on-brand (used at line 132 in the same file) is the token, violating the rule.

```
text-decoration-color: #FFFFFF;
```

#### `src/packages/web-shell/src/shared/extension-suggestion-banner/extension-suggestion-banner.component.test.ts`:148-150

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Same anti-pattern: the inline-link hidden state is proven only by `.toBeNull()`, which passes for the wrong reason on a selector typo. SKILL.md prescribes asserting the rendered variant and its state metadata instead of an element's null absence.
- **Verifier:** Lines 148-150 match verbatim and assert inline-link absence via `.toBeNull()`; same forbidden pattern as the CTA case, with the variant marker available as the state-based alternative.

```
expect(
					doc.querySelector("[data-test-extension-suggestion-inline]"),
				).toBeNull();
```

#### `src/packages/web-shell/src/base.component.test.ts`:611-614

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** The 'no trial countdown' case is proven with `.toBeNull()`, which SKILL.md flags as fragile (a selector typo passes for the wrong reason). The trial countdown is a binary shown/hidden element; the doc requires rendering it unconditionally with a state class and asserting on that, not on null absence.
- **Verifier:** Lines 611-614 match verbatim; both the countdown element and its client script are proven absent via `.toBeNull()`, the named anti-pattern for a binary shown/hidden decision.

```
expect(doc.querySelector("[data-test-trial-countdown]")).toBeNull();
		expect(
			doc.querySelector('script[src$="/client-dist/trial-countdown.client.js"]'),
		).toBeNull();
```

#### `src/packages/web-shell/src/nav.component.test.ts`:28

- **Rule:** Never Rely on querySelector(...).toBeNull() _(source: .claude/skills/test-driven-design/SKILL.md)_
- **Why:** Proves the trial countdown is omitted using `.toBeNull()`, the exact fragile assertion SKILL.md forbids; a selector typo makes it pass for the wrong reason. Assert the element's rendered state metadata instead.
- **Verifier:** Line 28 matches verbatim and proves the trial countdown's omission via `.toBeNull()` for a binary shown/hidden element, exactly the pattern the rule names.

```
expect(doc.querySelector("[data-test-trial-countdown]")).toBeNull();
```
