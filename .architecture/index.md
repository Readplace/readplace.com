# Architecture Snapshots

Point-in-time architecture documentation, pinned to the commit hash that the codebase was at when the snapshot was generated. Each subfolder is one self-contained snapshot (Markdown document + rendered SVG diagrams).

A snapshot reflects the code as it existed at that commit. It does not auto-update — re-generate against the new HEAD when something material shifts. **Any file paths referenced inside a past snapshot may have since been renamed, moved, or deleted; treat them as historical artefacts, not as a guide to the current codebase.**

> **For AI agents:** if a user points you at this folder and asks to create a new snapshot, this file is the single source of truth — read it end-to-end and follow every step below. Make no assumptions about file paths, project layout, language, framework, build tool, or package manager based on past snapshots — discover all of that from the current working tree.

---

## BPMN renderings

Every snapshot's Mermaid diagrams are also rendered in **BPMN** notation under
its `diagrams-bpmn/` folder (`<name>.png` plus an editable, valid-BPMN-2.0
`<name>.bpmn`). See [`tools/bpmn/README.md`](./tools/bpmn/) for the
event-storming → BPMN element mapping and how to regenerate.

---

## Snapshots

| Commit | Date (commit) | Generated | Branch | Subject | Contents |
|---|---|---|---|---|---|
| [`1748f31`](./2026-04-18-1748f31/) | 2026-04-18 | 2026-04-19 | `main` | feat(hutch): point share permalinks at /view/<url> instead of /save | Save-link flow event storming — UI submit → Lambda → SQS → EventBridge → datastore, with authenticated and anonymous branches. Three pre-rendered SVG diagrams. |
| [`52017f3`](./2026-04-20-52017f3/) | 2026-04-20 | 2026-04-20 | `main` | feat(hutch,save-link,@packages/hutch-infra-components): add summary generation state machine with DLQ | Summary-generation pipeline event storming — LinkSaved/AnonymousLinkSaved dispatch a GenerateSummaryCommand; generator writes ready/skipped and publishes SummaryGenerated; SQS retry exhaustion drives a DLQ handler that flips the row to failed and publishes SummaryGenerationFailed into a parse-errors log. Reader UI polls a pending slot every 3 s up to a 40-poll cap. Four pre-rendered SVG diagrams. |
| [`d5f38258`](./2026-04-24-d5f38258/) | 2026-04-24 | 2026-04-24 | `main` | fix(save-link): catch Readability.parse() throws in readability-parser | Article crawl pipeline event storming — authenticated, extension-Tier-0 (raw-DOM from S3), anonymous /view, and admin-recrawl entries all converge on the same save-link worker shape (crawl → parse → markCrawlReady/Failed → publish LinkSaved/AnonymousLinkSaved). Covers the Tier 1+ HTTP waterfall (Twitter oembed, H2 + AIA-chasing fetch, 304 short-circuit), the pluggable site pre-parser system in front of Readability, terminal-vs-transient parse-failure split (inline markCrawlFailed vs SQS-retry → DLQ), and the boundary where LinkSaved hands off to the summary pipeline. Four pre-rendered SVG diagrams. |
| [`bfd85c7`](./2026-04-24-bfd85c7/) | 2026-04-24 | 2026-04-24 | `main` | feat(save-link): Tier 0 canonical promotion via Deepseek selector + DLQ consumer | Save-link-raw (extension DOM) flow event storming — toolbar/context-menu click captures rendered DOM via content-script message → POST /queue/save-html stages HTML to a pending bucket and publishes SaveLinkRawHtmlCommand in parallel with the Tier-1 SaveLinkCommand. The raw-HTML worker parses, rewrites media, writes a tier-0 source, reads any existing canonical, and either promotes immediately (canonical empty) or runs a Deepseek JSON-mode selector to pick the more complete body; promotion is an S3 CopyObject + Dynamo UpdateItem and only then publishes LinkSaved, keeping the summary pipeline idempotent on losses. A dedicated DLQ consumer flips the article to crawlStatus=failed and emits CrawlArticleFailed when the queue exceeds maxReceiveCount. Four pre-rendered SVG diagrams. |
| [`a6949fd`](./2026-04-28-a6949fd/) | 2026-04-28 | 2026-04-30 | `feat/tier-content-selector` | feat: extract content selector into its own Lambda; tier sources are first-class | Tier-content selector flow event storming — workers stop touching canonical and instead write per-tier source slots (HTML + JSON metadata sidecar) and emit the new past-tense TierContentExtractedEvent. A new select-most-complete-content Lambda subscribes to that event, lists available per-tier sources from S3, runs the Deepseek selector when there is competition (short-circuits on a single tier), and is the only thing that promotes to canonical. The selector emits LinkSaved/AnonymousLinkSaved (only on canonical change) and CrawlArticleCompleted (every successful selection); the summary pipeline trigger relocates with these events. A new contentSourceTier column on the article row and a UI badge on the admin recrawl page surface which tier is in canonical. Recrawl now reconsiders any prior tier-0 source automatically — fixes the previous regression where a paywalled origin's HTTP recrawl could overwrite a strictly-better extension capture. Four pre-rendered SVG diagrams. |
| [`56099f1`](./2026-04-30-56099f1/) | 2026-04-30 | 2026-04-30 | `claude/fix-export-data-error-XkGXV` | feat(hutch,@packages/hutch-infra-components): async user data export via Command -> Lambda -> Event | User-data export flow event storming — POST /export/start (web Lambda) looks up the user's email via a new findEmailByUserId provider and publishes ExportUserDataCommand, returning 303 to a "preparing" page so the API Gateway 30s cap is never the bottleneck. EventBridge -> SQS -> a new export-user-data Lambda (900s, 1GB) paginates the user's articles, streams a JSON envelope to a new private S3 bucket (hutch-user-exports-{stage}) with a 7-day lifecycle rule, generates a 7-day presigned GetObject URL using a TTL constant shared between infra and runtime, emails the user via Resend, and publishes UserDataExportedEvent. DLQ exhaustion still pages the operator via the existing CloudWatch alarm + SNS email convention. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`98f2e47`](./2026-05-01-98f2e47/) | 2026-05-01 | 2026-05-01 | `main` | chore: bump firefox extension version to 1.0.82 | Recrawl & auto-heal flow event storming — admin recrawl now publishes a dedicated RecrawlLinkInitiated → RecrawlContentExtracted → RecrawlCompleted chain backed by two new Lambdas with their own SQS queues + DLQs; the recrawl selector is a clone of the user-save selector that always dispatches GenerateSummary regardless of canonical change so the AI excerpt regenerates every operator click. Anonymous /view auto-heal stays on the SaveAnonymousLink chain via refreshArticleIfStale's reprime action — recrawl is admin-only by design. The article reader gains legacy-stub healing for pre-state-machine rows and promotion-race polling that holds open the reader-slot when crawlStatus=ready but canonical S3 hasn't landed. The summary pipeline now produces a one-or-two-sentence AI excerpt alongside the summary in a single Deepseek json_schema call; both write atomically and surface in article-list views. A new unified single-bar progress component drives a percent from whichever pipeline is further along, swapped out-of-band on each HTMX poll. Five pre-rendered SVG diagrams. |
| [`1c1095ca`](./2026-05-13-1c1095ca/) | 2026-05-13 | 2026-05-13 | `claude/harden-article-aggregate-bqGgo` | refactor(hutch): exhaustive switch over crawl.status in renderReaderSlot | Refresh tier-selection & summary auto-heal flow event storming — refresh now writes the freshly-fetched HTML as a tier-1 source and publishes a new RefreshContentExtractedEvent; a new refresh-content-extracted Lambda runs the Deepseek selector across all available tier sources so a prior tier-0 winner is preserved on refresh. The stale-check handler gains summary auto-heal: decideSummaryAutoHeal + incrementSummaryAutoHealAttempt reprime summary-failed rows with a bounded retry budget (3 attempts, then 24h cool-off) by dispatching GenerateSummaryCommand via the effect dispatcher. Three pre-rendered SVG diagrams. |
| [`a8c3a85`](./2026-05-13-a8c3a85/) | 2026-05-13 | 2026-05-13 | `claude/redesign-entry-point-flznu` | refactor(@packages/domain,@packages/hutch-infra-components,save-link): entry-point redesign foundation (EventBridge route, deploy-safe) | Entry-point redesign foundation — two new aggregate transitions (`submitLink` upsert + `requestRecrawl` operator recovery), a new `SubmitLinkCommand` wire format, a new `dispatch-submit-link` effect routed via EventBridge using the existing `EVENT_BUS_NAME` (no new SQS queue or env var), and the `upsertAndPersist` orchestrator that allows the article to be undefined and skips the DDB save on empty writes. Re-do of the reverted PR #303 with the publisher-side wiring intentionally narrow so the chain is deploy-safe without infra changes. The downstream subscriber Lambda is deferred to a follow-up PR; this PR has no runtime caller. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`a44a728`](./2026-05-18-a44a728/) | 2026-05-18 | 2026-05-18 | `claude/split-crawl-paths-XTNnX` | refactor(save-link): split comprehensive (PDF) crawl into its own Lambda | Cross-Lambda comprehensive-crawl split — the PDF / heavy crawl path is lifted out of the three save-link Lambdas (save-link-command, save-anonymous-link-command, recrawl-link-initiated) into a dedicated `comprehensive-crawl-command` Lambda fed by a new `ComprehensiveCrawlCommand` EventBridge command. The save-link Lambdas drop their mupdf + OpenAI + DEEPINFRA_API_KEY footprints; memory shrinks 2048→512 MB, timeout 600→240 s. A new `tier-1-deferred` SaveLinkWorkResult variant signals that the simple crawl dispatched the command and the comprehensive Lambda owns the downstream event emission (TierContentExtractedEvent for saves, RecrawlContentExtractedEvent when the recrawl flag is set). A new comprehensive-crawl-dlq Lambda flips crawlStatus=exhausted and emits CrawlArticleFailedEvent on retry exhaustion. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`628bc23`](./2026-05-18-628bc23/) | 2026-05-18 | 2026-05-18 | `claude/merge-pr-350-7ZGlA` | feat(save-link): switch OCR Lambdas to container images with native pdftoppm (#337) | Stale-check migration through the comprehensive-crawl path — the stale-check Lambda becomes simple-only (drops mupdf + DeepInfra; memory 2048→512 MB, timeout 600→240 s, SQS visibility 1200→480 s). PDFs encountered during a freshness refresh now flow through the same SimpleCrawlUnsupportedEvent → policy → ComprehensiveCrawlCommand chain as the save-link Lambdas, with a new `refresh?: boolean` field on both schemas (mutex with `recrawl`). The comprehensive Lambda branches on `refresh=true` to emit RefreshContentExtractedEvent (with re-fetch etag/lastModified/contentFetchedAt) instead of TierContentExtracted or RecrawlContentExtracted, keeping the existing refresh-content-extracted selector + canonical write flow intact. After this snapshot, `mupdf` is fully removed from the repository — `pdftoppm` (in the comprehensive Lambda's container image) is the only PDF rasterizer; all four entry points (save-link-command, save-anonymous-link-command, recrawl-link-initiated, stale-check) are simple-only at 512 MB / 240 s zip-based. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`527ea4c`](./2026-05-19-527ea4c/) | 2026-05-19 | 2026-05-19 | `claude/remove-html-payloads-foNSC` | fix: stage refresh HTML in S3 to keep EventBridge payloads under 256 KB | Refresh HTML S3 staging — `RefreshArticleContentCommand.detailSchema` drops `html` (mirrors the existing `SaveLinkRawHtmlCommand` pattern); both publishers (stale-check Lambda and synchronous hutch `/queue/freshness`) now PUT the fetched HTML to a new `refresh-html/` prefix in the existing `PENDING_HTML_BUCKET` before publishing the sub-1 KB event; the refresh-article-content Lambda derives the same key from the URL and GETs the staged bytes. `initEventBridgePublisher` gains a `PayloadTooLargeError` guard at 240 KB so an oversized event fails at the publisher instead of bouncing 4xx off AWS for the SQS visibility window. `HutchS3ReadWrite` gains an optional `expirationRules` arg and the pending-html bucket gets a 1-day lifecycle on both `pending-html/` and `refresh-html/` prefixes. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`1f9d7d78`](./2026-05-23-1f9d7d78/) | 2026-05-23 | 2026-05-23 | `main` | feat(hutch): wire trial signup, access gating, and Stripe webhook (#396) | Stripe webhook cancellation flow — Stripe sends `customer.subscription.deleted` to an API Gateway-fronted `stripe-webhook-receiver` Lambda that verifies HMAC-SHA256, parses the body, and emits `SubscriptionCancelledEvent` via EventBridge. A SQS-backed `handle-subscription-cancelled` Lambda subscribes and marks the `subscription_providers` row as cancelled. The SSR Lambda's `initGetEffectiveAccess` reads the updated status at request time and `requireWriteAccess` middleware 402-gates write endpoints. |
| [`af1a3b2f`](./2026-05-24-af1a3b2f/) | 2026-05-24 | 2026-05-23 | `main` | feat(hutch): account menu + user-initiated cancellation chain (#402) | Account page and user-initiated cancel flow — new `CancelSubscriptionCommand` is published by `POST /account/cancel` and consumed by a new SQS-backed `cancel-subscription` Lambda that branches on the row's status: paid users go through Stripe's immediate-cancel API (which fires the existing webhook chain), trial users emit `SubscriptionCancelledEvent` directly. The event payload is broadened from `{ subscriptionId }` to `{ userId, subscriptionId?, reason }` and the webhook receiver now GSI-looks-up `userId` before publishing. The `SubscriptionCancelled` handler switches to `markCancelledByUserId` (primary-key update, no GSI). Subscribe is synchronous: `POST /account/subscribe` creates a Stripe Checkout session (`trial_period_days: 0` for trial users to suppress double-trial), stores an `existing-user-subscribe` pending-signup row, and 303s to Stripe; the existing `/auth/checkout/success` handler `upsertActive`s the row on payment. `pending_cancellation` is narrowed in `initGetEffectiveAccess` to read-only (defensive — no flow in the redesign produces it). |
| [`bf80b57a`](./2026-05-24-bf80b57a/) | 2026-05-24 | 2026-05-24 | `main` | feat(hutch): trial-end scheduler, charge chain, one-click resubscribe (#407) | Trial-end auto-conversion and user-initiated one-click resubscribe — new `SubscriptionStartRequestCommand` is published by an EventBridge Scheduler one-shot rule created at trial signup (fires at `trialEndsAt`) and consumed by a new SQS-backed `subscription-start-request` Lambda that reads the row and either calls Stripe `subscriptions.create` on an existing customer (rare — only if a card was attached out-of-band) or publishes `SubscriptionChargeFailed({ reason: "no_card_on_file" })` (the typical no-card path). A new `SubscriptionChargeSucceededEvent` → `subscription-charge-succeeded` Lambda calls `upsertActive`; a new `SubscriptionChargeFailedEvent` → `subscription-charge-failed` Lambda dispatches `CancelSubscriptionCommand`, reusing the Phase 2 cancel chain to flip the row to `cancelled`. The Phase 2 cancel-subscription handler now also calls `deleteTrialEndSchedule` before branching, and `/auth/checkout/success` deletes the schedule on conversion. `POST /account/subscribe` for a cancelled row WITH `customerId` skips Stripe Checkout entirely — synchronous `subscriptions.create` on the saved customer + `upsertActive` → 303 to /account (Stripe failure 303s to `/account?error=payment_method`). The active-state Cancel button becomes a GET link to `/account?confirm=cancel`; the destructive POST lives only behind the reassurance copy. |
| [`60e8e699`](./2026-05-28-60e8e699/) | 2026-05-28 | 2026-05-28 | `claude/laughing-dirac-QlUGH` | feat(hutch): defer subscription cancellation to end of billing period | Deferred subscription cancellation flow — `POST /account/cancel` now schedules cancellation to `current_period_end` (paid) or `trialEndsAt` (trial) instead of immediate delete. The `cancel-subscription` Lambda PATCH-es Stripe `cancel_at_period_end=true` (paid) or deletes the trial-end charge schedule (trial), creates a deferred-cancellation EventBridge Scheduler firing at the effective instant + 1h, and emits `SubscriptionCancellationScheduledEvent`. A new `handle-subscription-cancellation-scheduled` Lambda writes `pending_cancellation` + `cancellationEffectiveAt` to the row. Two paths converge on `cancelled`: Stripe's `customer.subscription.deleted` webhook (happy path) and the deferred scheduler re-firing `CancelSubscriptionCommand` against the `pending_cancellation` row (fallback). `POST /account/reactivate` reverses a scheduled cancel (Stripe PATCH for paid, recreate trial-end schedule for trial) and publishes `SubscriptionReactivatedEvent`. The access gate grants full access while `now() < cancellationEffectiveAt`, showing a `cancellation-scheduled` banner with a Reactivate CTA. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`0610dc08`](./2026-05-30-0610dc08/) | 2026-05-30 | 2026-05-31 | `main` | feat(hutch): event-driven summary regeneration on canonical-content change | Event-driven summary regeneration — the tier selector's `promoteTier` stops resetting the summary axis and instead publishes a new `CanonicalContentChangedEvent` whenever the canonical tier flips OR the readable text changes. A new `canonical-content-changed` Lambda subscribes and re-primes the summary (`markSummaryPending`, preserving an existing `pendingSince` so the SLO age-gate is not reset), dispatching `GenerateSummaryCommand` so the unchanged worker regenerates instead of cache-hitting a stale terminal `skipped`/`ready` row. Closes the incident where a tier flip with byte-identical readable text (`canonicalChanged=true`, `contentChanged=false`) left a row stuck at `skipped(content-too-short)`. Staleness now lives in one place (OCP): future transcript/embedding consumers attach as new `eventBus.subscribe(CanonicalContentChangedEvent, …)`. Only `promoteTier` is migrated; recrawl/refresh keep their inline regeneration (documented follow-up). Two pre-rendered SVG diagrams. |
| [`333da40`](./2026-05-30-333da40/) | 2026-05-30 | 2026-05-30 | `claude/loving-johnson-ZZw55` | feat(crawl-article,save-link,hutch-infra-components): pre-parse body-hash gate | Pre-parse body-hash gate for the refresh chain — the crawl library now computes SHA-256 of the materialised response body before dispatching to a parser and short-circuits to `not-modified` when the hash matches a `previousBodyHash` the caller threaded in. The freshness row gains a `bodyHash` attribute that stale-check reads and threads through `SimpleCrawlUnsupportedEvent` / `ComprehensiveCrawlCommand` (PDF path) or the in-process crawl call (HTML path); the freshly computed hash propagates through `RefreshArticleContentCommand` → `RefreshContentExtractedEvent` → `refreshContent` aggregate transition (HTML) and through `updateFetchTimestamp` (PDF, save, recrawl). The comprehensive Lambda gains a `not-modified` branch that calls `updateFetchTimestamp` in-process and emits no event because the canonical row is already correct. All new wire-format fields are optional; legacy rows lazy-backfill on their first post-deploy refresh. Two pre-rendered SVG diagrams. |
| [`fc406ec`](./2026-05-30-fc406ec/) | 2026-05-30 | 2026-05-30 | `claude/dazzling-cray-tkyDl` | feat(hutch,save-link): email savers when their reader view is ready | Reader-ready notification flow — when an article's reader view reaches the successful terminal state, the summary worker emits a new global per-URL fact that fans out through the user/article join table: a SQS-backed fan-out reverse-looks-up every saver via a new url-index, stamps a per-user success timestamp, and dispatches a delayed per-user notify command only for savers who opened the reader while it was loading. The notify worker re-checks per-user gates (not read, not re-saved, took over a minute, viewed-before-ready, verified email, not already sent) against the live row, atomically claims a 6h-per-user email cooldown, sends a reader-ready email, and publishes an email-sent fact. Presence is detected entirely server-side by stamping a viewedAt on the reader open and in-reader polls (never the queue-list card) — no new client JavaScript. Introduces a shared successful-state derivation in the article-state package, a per-message SQS delay option, and three new event/command wire formats. Mermaid sources only — SVG render skipped (sandboxed Chromium unavailable in this environment). |
| [`23f75dd`](./2026-06-24-23f75dd/) | 2026-06-24 | 2026-06-24 | `claude/awesome-turing-evfh9d` | feat(hutch,@packages/domain): receive and read forwarded email (M2) | Inbound email receive & read flow — a forwarded newsletter arrives via AWS SES, whose catch-all receipt rule stores the raw message to a forever-retained bucket and notifies SNS; an SNS→SQS-backed receive worker fetches the raw bytes, resolves the recipient forwarding address, parses MIME → HTML (cid inline images rehosted as data: URIs, remote images stripped) through an allowlist sanitizer, writes the safe body to the content store plus a row, and publishes the new past-tense EmailReceivedEvent (no M2 consumer; M3 seam). Oversize, unknown-recipient, disabled-recipient, and unparseable mail each record an auditable row and fail to the DLQ so the existing alarm pages the operator — never silently dropped. The read side turns /inbox into a Gmail-style received list (M1 address management relocates to /inbox/addresses) and an email detail page that renders the full sanitized newsletter inside a sandboxed, script-less iframe under a default-src 'none' CSP. Four pre-rendered SVG diagrams. |
| [`cd26b26`](./2026-06-28-cd26b26/) | 2026-06-28 | 2026-06-28 | `claude/email-link-previews-bs64f0` | feat(hutch,save-link): wire the inbox link-preview pipeline end to end (M3) | Inbox link-preview flow — M2's consumer-less EmailReceivedEvent now drives two new SQS-backed consumers. extract-email-links re-derives the body from the immutable raw .eml (re-parse → re-sanitize via a shared deriveSanitizedBody), extracts links, writes one pending row per link to a new hutch-inbox-email-links table, and fans out one CrawlEmailLinkPreview command per link (mirroring how /queue fans each import URL into its own SaveLinkCommand); a per-email soft cap (200) bounds the fan-out and a truncated email still ships the first N previews while writing a truncated meta item and SendMessage-ing its own DLQ to page the operator (★14). crawl-email-link-preview consumes one command per link, runs the same SSRF-guarded crawlAndFinalizeArticle the save pipeline uses (★16), keeps only the metadata, discards the body, and stamps crawled/failed onto the link row — nothing is written to the articles/user-articles tables, enforced at the IAM boundary. The Articles tab replaces its M2 placeholder with one preview card per link, each pending card self-polling /inbox/:id/links/:ordinal/card every 3s (etag + 304, shared MAX_POLLS) to terminal exactly like /queue; the list and detail header gain an "N links" badge derived from the same per-email partition Query (no parent-row denormalisation). The lead-image upload reads the save-link content-media CDN base URL cross-stack. Mermaid sources only — SVG render skipped. |
| [`bec433e1`](./2026-07-10-bec433e1/) | 2026-07-10 | 2026-07-10 | `inbox-split` | feat(hutch): export the inbox notification topic ARN for the inbox deployable | Inbox deployable split — the inbound-email pipeline mid-extraction into its own deployable, captured during the deliberate double-delivery overlap window. A new inbox stack stands up its own copies of the three consumers (SNS→SQS receive worker, EmailReceivedEvent→link extractor, CrawlEmailLinkPreview→preview crawler) beside the still-live originals: the SES receipt topic carries two SQS subscriptions and each event matches two EventBridge rules, so every stage runs twice per mail — with distinct inbox-prefixed rule/queue/Lambda names throughout because PutRule upserts and a same-named resource would steal the live one. Replay-idempotent handlers (conditional puts, always-last meta barrier, terminal last-write-wins UpdateItem) make the overlap cost duplicate compute, never duplicate state. SES receiving (domain identity, catch-all receipt rule set, immutable raw bucket, SNS topic — its auto-named ARN newly exported cross-stack) and the three inbox tables stay with the original stack; the /inbox pages re-route to a new web Lambda attached to the same API Gateway by route precedence, shadowing the still-mounted original router until a follow-up cleanup removes the duplicated consumers. Six pre-rendered SVG diagrams. |
| [`37d5f61a`](./2026-07-19-37d5f61a/) | 2026-07-19 | 2026-07-19 | `main` | refactor(@packages/article-store,hutch): move the crawl and summary mark providers into the article-store package | Submit-link entry-point activation — the previously consumer-less SubmitLinkCommand (its dormant aggregate-effect publisher still has no runtime caller) gains its subscriber: a new SQS-backed submit-link Lambda (480s visibility, maxReceiveCount 3, alarm-only DLQ with deliberately no consumer — a dead-letter either wrote no row or touched a row owned by another saver's in-flight crawl, so no row mutation is correct at that boundary) implementing the authenticated URL-only shape; rawHtml (tier-0) and anonymous shapes assert, retry, and dead-letter. The handler accepts the save synchronously through the same shared accept phase the queue save bar / extension / import commit / MCP use — global stub + per-user row + savedAt bump + read→unread resurface, so the queue card appears before any crawl — then runs the tier-1 crawl in-process with the link-saved publish swapped for an in-memory collector. Crawl failures terminalise in-process (crawl-exhausted + CrawlArticleFailed, keyed on the canonical URL) and the record still acks, so SQS retries are reserved for accept-phase blips. The submit freshness decision is crawl-status-aware: absent / purged / legacy / pending rows re-run the full accept-and-crawl (a stuck pending row self-heals; concurrent duplicate crawls accepted — the selector is idempotent); only settled rows publish StaleCheckRequestedEvent so the existing async stale-check pipeline owns refresh (Command → System → Event(s); the handler dispatches no commands). Non-HTML bodies defer via SimpleCrawlUnsupportedEvent to the comprehensive-crawl chain; a written tier-1 source emits TierContentExtractedEvent into the existing selector → summary chain. Intended first producer (the inbox newsletter-link extractor) is not yet in the tree. Captured from a dirty working tree — the subscriber wiring is uncommitted on top of the base commit. Five pre-rendered SVG diagrams. |
| [`f6b67fff`](./2026-07-19-f6b67fff/) | 2026-07-19 | 2026-07-19 | `main` | feat(save-link,@packages/hutch-infra-components): add the submit-link Lambda consuming SubmitLinkCommand | Inbox link → queue save — SubmitLinkCommand gains its first live producer (the base commit is the subscriber the previous snapshot documented pre-commit; the two snapshots are companions). The inbox link extractor, alongside its existing per-kept-link preview fan-out, now also publishes one SubmitLinkCommand per kept link — deliberately BEFORE the preview command, because the preview consumer flips the link row terminal and a crash between the two publishes would otherwise let the retry's pending-gate silently skip a submit that never happened — so a forwarded newsletter's articles land in the reader's unread queue through the same entry point every save surface uses. Three producer-side guards keep the subscriber's DLQ quiet and backfills save-free: a new required origin field on the received-email event ("receive" vs "backfill") pins queue saves to genuine arrivals, so the operator backfill — which deletes link rows first, resetting the pending-gate dedupe — re-derives previews only and can never mass-save or resurface historical mail; audit-partition (unrouted) mail never saves — previews still render for operator inspection; and only URLs passing the shared saveable-URL validation are submitted, so localhost/private-IP/oversized links get preview cards but no queue save. The unrouted sentinel moves into the domain package so both inbox handlers share it. Zero infra changes — the extract Lambda already had bus publish rights; only invariant comments changed. Supersedes M3's "nothing saved to /queue": the IAM boundary stands (no inbox Lambda touches the queue tables) but queue writes now happen in save-link's subscriber, routed by command. Re-delivery is bounded by link-row state: terminal rows re-publish nothing, still-pending rows re-publish both commands and both consumers converge idempotently; the per-email link cap bounds the save fan-out exactly as it bounds previews. Captured from a dirty working tree — the producer wiring is uncommitted on top of the base commit. Three pre-rendered SVG diagrams. |
| [`cc404eb8`](./2026-07-19-cc404eb8/) | 2026-07-19 | 2026-07-19 | `main` | feat(inbox,@packages/domain,@packages/hutch-infra-components): save kept newsletter links to the reader's queue | Reader-initiated newsletter link save — the inbox web tier becomes the second live producer of the unified save command (the auto-save extractor committed at this base commit is the first; the article-aggregate effect publisher stays dormant), while the downstream save flow is unchanged. Each preview card on an email's Articles tab gains a per-link "Save to queue" button: a POST behind the same two write gates the address-creation route uses loads the stored link row, 404s for an unparseable ordinal, a missing row, a skipped row, or a URL the save pipeline would reject, then publishes one save command and redirects back to the tab with a confirmation notice. It exists because mail received before the auto-save shipped holds preview cards that were never queued, and the operator replay path deliberately re-derives previews only — the button is the user-facing counterpart of that gate, granting the save decision to the reader one link at a time rather than mass-saving history. The button renders on pending and failed preview cards too, since the queue save is an independent pipeline from the preview crawl; skipped links never render a card at all. The command carries the URL as extraction stored it (never the preview's post-redirect URL), so redirect-following and canonical identity stay owned by the save pipeline, and re-clicks converge idempotently into a savedAt bump plus read-to-unread resurface. Infrastructure adds only event-bus publish rights to the web Lambda: the queue write still happens exclusively in the save subscriber's role, keeping the inbox IAM boundary intact. Captured from a dirty working tree — the button wiring is uncommitted on top of the base commit. Two pre-rendered SVG diagrams. |
| [`599d63be`](./2026-07-26-599d63be/) | 2026-07-26 | 2026-07-26 | `main` | fix(ios): verify App Store screenshots against git rather than trust deliver | Saved-link read model — a newsletter link's Save button learns whether it is already in the reader's queue. A new accept-terminal fact is published from the one shared accept phase every save surface routes through (web save bar, extension/iOS Siren action, bulk save, tier-0 capture, MCP tool, import commit, and the unified save command's subscriber), so it fires for all of them, including the duplicate-save branch that upserts the queue row and otherwise publishes nothing. It deliberately does NOT reuse the existing link-saved event, which signals canonical-content promotion: that one is silent on duplicates and carries the post-alias-resolution URL, so a read model keyed on it would leave a re-saved link permanently unsaved and would not match the URL the consumer submitted; the new fact carries the URL exactly as submitted. A companion failure fact is published by a new consumer on the save command's dead-letter queue (publish-only — a dead letter there wrote no row, or the row belongs to another saver's in-flight crawl), so a save that exhausted its accept retries retracts its own claim; the queue's depth alarm stays wired alongside it. Both facts feed one new inbox subscriber through two queues (one rule per queue — the bus provisions a queue policy per subscription), which upserts a per-user/per-URL row into a new inbox-owned table; the Articles tab resolves a whole page of cards from it in a single batch read. The inbox IAM boundary is untouched — no inbox role reads or writes the queue tables, which is precisely why the state arrives as a fact rather than a cross-boundary read. The rendered button swaps to a checked "Saved" state but stays clickable, re-posting the same route so re-saving behaves exactly like re-saving from the website or extension (savedAt bump plus read-to-unread resurface); a failed save renders as still-savable. The per-card poll validator now covers save state, without which a card whose save landed after the reader's last poll would keep revalidating stale forever. Semantics are moment-in-time by decision: removing an article publishes no fact, so the row outlives the queue row it describes. Known, accepted gaps: the same article reached through a different wrapper URL reads as unsaved, and saves predating the subscriber are absent with no backfill. Account deletion drops the new partition. Captured from a dirty working tree — the whole feature is uncommitted on top of the base commit. Three pre-rendered SVG diagrams. |
| [`bf096547`](./2026-07-27-bf096547/) | 2026-07-27 | 2026-07-27 | `main` | refactor(inbox): assert which state an element is in, not that it is absent | Dead-letter terminal states for the inbound-newsletter pipeline — both of the inbox's async chains had a happy path that ended in a state the reader's page renders from and a failure path that ended nowhere, so a give-up after three exhausted receives left the UI describing work that had already stopped. The per-link preview chain was fixed first (committed at the base commit's ancestor): a consumer on its dead-letter queue stamps the link terminal with a retries-exhausted reason, through a new store write **guarded on the row still being pending** rather than the existing unconditional outcome write — the queue's visibility timeout equals its worker timeout with no receive-to-invoke guard, so a slow-but-successful crawl can be redelivered and the copy that dead-letters may be chasing a link another copy already crawled; that race returns an already-terminal outcome instead of repainting a good preview as a failure. The extraction chain is added here: its consumer writes the per-email "extraction finished" barrier itself, carrying a new required marker that distinguishes a scan that gave up from one that genuinely found nothing — without it, a barrier with no link rows behind it would read as the terminal "no links found", a claim about an email nobody managed to read. That write is a conditional put keyed on the barrier's absence, because at-least-once delivery lets one attempt succeed while a duplicate exhausts its receives; the reverse race self-heals since the success barrier stays unconditional. The detail view derives the new flag and makes both tab panels terminal on first render — no poll URL at all rather than burning the fifteen-minute budget — reusing the existing "couldn't scan this email" copy, withholding the header badge and tab counts an aborted scan cannot back, and suppressing the out-of-band tab strip; a shared precedence helper is the one place the four-state ladder is written down so the two panels cannot drift. Neither consumer reuses the fleet's shared dead-letter component, which hardcodes an articles-table env var that would undercut the invariant that no inbox role touches the queue tables; its wiring was extracted into a standalone attach helper reused by both. Both are store-only and publish nothing — the only reader of a failed preview or a failed scan is the reader's own page. Three sibling dead-letter queues stay alarm-only by design: the receive path already writes its audit row before failing (and its earlier failures have no key to write against, the immutable raw message being the audit trail), and the saved-link read model's only write is an unconditional upsert, so exhausting it means the datastore itself was unavailable. Recorded tradeoff: a consumer drains the queue it watches, destroying the messages reserved for operator redrive — cheap for one link's preview, expensive for a whole email's link set, accepted here only because the raw message is retained forever and the trigger can be re-published from that source of truth. Captured from a dirty working tree — the extraction half is uncommitted on top of the base commit. Five pre-rendered SVG diagrams. |
| [`759068f8`](./2026-08-03-759068f8/) | 2026-08-03 | 2026-08-03 | `main` | feat: bind inbox save buttons to live queue membership | Queue-membership retraction fact — the inbox saved-link read model stops being append-only. A deleted article used to read "Saved" forever and the Skipped tab hardcoded "Save to queue"; now both hutch delete surfaces (queue-card delete and remove-my-copy) route through a shared delete orchestrator that resolves the row's canonical URL, drops the per-user queue row, and publishes a new dequeued fact — deliberately even when the row was already gone, because the retry of a delete whose publish failed after the commit is the only chance to re-announce it, and a duplicate retraction is harmless while a suppressed one leaves the link reading as saved for good. Two deliberate non-publishers: marking read announces nothing (a read article is still in the queue, its button keeps reading "Save again"), and account deletion drops the consumer's whole partition directly. The existing inbox recorder Lambda becomes a detail-type dispatcher fed by a third SQS queue + DLQ on its own explicitly-named rule (one rule per queue, since each bus subscription owns the queue's single physical policy): accepted → unconditional upsert, gave-up → conditional put an accepted save outranks, dequeued → row delete, restoring the absence that is the model's only "not saved" state; a detail-type outside the table dead-letters rather than defaulting, since defaulting is how a deletion would be recorded as a save. Both email-detail tabs now render one shared save-button view model — in-queue reads "Save again" (check icon, muted, still clickable: the same POST, converging on the same savedAt-bump + read-to-unread upsert), absent or failed reads "Save to queue" — and the per-card poll reads save state before computing its validator, so a membership change after the reader's last poll can never revalidate stale. The retraction carries the deleted row's canonical key rather than the submitted URL the accept fact carries — divergent only for a save that adopted a redirect terminal, accepted without a second key for lack of an observed stale row. The inbox IAM boundary is unchanged: the only new grant is the recorder's DeleteItem on the inbox's own table. Five pre-rendered SVG diagrams. |
| [`b1e7ece7`](./2026-08-03-b1e7ece7/) | 2026-08-03 | 2026-08-04 | `main` | chore(@packages/check-failed-articles): exclude bizjournals, archive.ph, an HN soft-404, and two JS Weekly trackers | Related saved articles — a save now asks, once, which of the reader's earlier saves relate to the one just saved, so the pile they already built comes back at the moment they are engaged with a new piece rather than through a scheduled reminder. Only *interactive* saves ask: the save bar, the extension and iOS action, Save All Tabs, tier-0 capture, and the assistant's save tool are wrapped by a decorator around the shared accept phase, while the bulk import commit and the inbox newsletter-link subscriber keep calling the bare accept phase and publish nothing — the latter could not publish it regardless, since it already handles a command and command → command across a Lambda boundary is forbidden. The new command carries the canonical url the accept phase resolved to (the articles partition key), deliberately unlike the accept-terminal fact beside it, which carries the url exactly as submitted. Its subscriber waits rather than guesses: a save writes a placeholder title before the crawl fills in real metadata, so a still-pending crawl or an absent article row throws and is redelivered, and only a crawl that reached a terminal state still holding the placeholder is recorded as skipped, because no metadata will ever arrive for it. Given at least a hundred earlier saves it makes exactly one model call over the titles, sites, and clipped AI summaries of up to the newest thousand — the model is shown numbered positions and answers with positions plus a one-line reason, never urls, and out-of-range positions, repeats, and over-long reasons are discarded server-side, with the prompt stating that the candidate text is untrusted scraped content. The result, its two token counts, and the timestamp are written to the reader's own save row rather than the shared article, so relations die with the save and can never surface on another reader's page; a second save of the same article cache-hits the terminal state, and recomputation when the canonical content changes is deliberately not wired. The dead-letter queue is alarm-and-email only with no consumer, because an unwritten status renders as an empty slot and there is no terminal row state to correct. On the read side the owner's reader gains a slot after the article body — an after-the-reading aside rather than something that pushes the article down the page — always rendered and toggling between hidden and visible, gated behind an opt-in querystring flag so the pipeline can fill rows before anyone sees them; the public reader and the admin preview pass nothing and get the hidden slot by construction; a failed read degrades to hidden rather than failing the page. Each stored relation is re-checked at render against the reader's current saves and the article behind it, so a since-deleted or purged item drops out instead of rendering a dead link. An assistant-facing read tool exposes the same relations ungated, each with the id needed to fetch the article again. Four pre-rendered SVG diagrams. |
| [`cb3ec007`](./2026-08-04-cb3ec007/) | 2026-08-04 | 2026-08-04 | `main` | refactor(hutch,@packages/hutch-infra-components): merge low-traffic handler families onto shared queues | Shared-queue handler consolidation — nine async workers become two, with every command and event definition left untouched. The forcing measurement is that a queue's polling cost is charged per event source mapping rather than per message: an idle queue holds almost exactly 360 empty receives an hour (two pollers against a twenty-second long poll, the documented idle floor) and that rate barely moves across a 539x swing in daily volume, so the production fleet spent roughly 410 polling calls for every message it actually delivered, and a queue plus its handler costs about five dollars a year across both accounts before carrying anything. Two families whose runtime profile already matched — the seven-handler subscription and billing state machine at thirty-second visibility, and the two long-running data-rights commands at fifteen-minute visibility — each collapse onto one queue, one Lambda, one dead-letter queue and one alarm, removing seven mappings and seven alarms. Membership was decided by measured profile, not by theme: the trial-feedback email sender is deliberately left out because its timeout and memory do not match the seven, and folding it in would have mis-provisioned all of them; the digest pair is left alone because its producer/consumer self-loop and dedicated scheduler target are a different shape rather than a different size. Records route on the envelope's detail-type — a discriminator every EventBridge delivery already carries, so no message shape changed — through a new shared router that groups a batch by handler, unions the partial-batch failures so a record failed by two handlers is reported once, and dead-letters any type absent from its table rather than defaulting, since defaulting is how an unrouted billing event would vanish. Only the composition roots merged: every domain handler and its tests are untouched. The cancellation fact previously had two rules and two queues; the second rule is deleted and its fan-out now happens inside one invocation, which couples the two handlers' retries — accepted only after verifying both are idempotent under redelivery, the row marker being an unconditional status write and the feedback scheduler a delete-then-create on a deterministic per-user name. The event bus component gains a multi-event subscribe because SQS permits exactly one policy per queue, so N rules converging on one queue cannot each mint their own; the single-rule case deliberately keeps a scalar source-ARN condition so the change renders as a no-op for the forty-odd existing subscriptions across the other stacks. Observability collapses in step through the lambda-name table every log group, dashboard origin filter and forwarder source derives from, so a partial edit is a compile error; handler attribution moves from the log-group name into each line's message prefix. Two recorded trade-offs: the merged roles hold the union of their group's permissions, and the export path inherits the account-scrub's twelve-receive redrive window because riding out a third-party outage is the stronger of the two properties. Validated end to end on staging by a targeted apply and revert before landing. Five pre-rendered SVG diagrams. |
| [`dfbaa030`](./2026-08-05-dfbaa030/) | 2026-08-05 | 2026-08-06 | `main` | feat(inbox,@packages/web-shell): swap the Skipped tab's save button in place | Authored-content removal folded to one scope — the erasure command's snapshot id becomes required, and the whole-copy scope it used to distinguish is deleted along with the route that published it, which had had no user interface since an earlier commit removed its form. The two behaviours only that scope reached are re-derived from stored state instead. A reader's tier-0 capture and its attribution sidecar are now erased as a consequence of deleting the last snapshot they authored, rather than as a separate request. The repair tail that rebuilds or retires the shared copy no longer keys off what the delivery asked for but off what the row holds: read the tier the canonical body was copied from, list the per-tier sources still present, and run the tail only when that tier's source is gone — which is what keeps a redelivery correct, since on a second attempt the objects are already deleted and nothing resolves, so a predicate derived from "what did I just erase" would skip the repair entirely. The purge-versus-recrawl decision switches from counting other savers to counting every saver, because a snapshot delete deliberately leaves the remover's own queue row in place and excluding them would purge and tombstone an article they still hold, breaking the invariant that a tombstone can never coexist with an owning row; the exclusion-counting variant is kept for the account scrub, which counts before dropping the user's rows. Downstream is untouched: sources remaining re-run the selector with no saver identity so a canonical flip cannot fire a save notification at the remover, no sources but savers remaining re-crawls the public page, and nothing-and-nobody purges every stored object and tombstones the row with no event at all. Two accepted gaps on the record: a capture that lost the selector's comparison is never credited in the version log, so it gets no delete control and cannot be reached by this flow — pre-existing, since the removed route's form was gated on the same condition, and properly fixed upstream by attributing at capture time; and on the recrawl branch a failed public fetch leaves the canonical body derived from the erased capture readable until a later crawl succeeds. A second, unrelated withdrawal rides the same tree: the export path's pre-declared failure fact — never emitted, never subscribed — is deleted, leaving the dead-letter alarm as that flow's only failure signal, so the export diagram highlights its surviving terminal fact instead. Captured from a dirty working tree — the whole change is uncommitted on top of the base commit. Six pre-rendered SVG diagrams. |

---

## AI prompt — generate a new snapshot

Hand this verbatim to a fresh agent (replace `<TOPIC>` and `<ENTRY>` first if you want to skip the inference step; otherwise leave them and let the agent infer from the surrounding ask).

> Read `.architecture/index.md` end-to-end before doing anything else. It is the single source of truth for this task. Make no assumptions about file paths, project layout, language, framework, build tool, or package manager based on past snapshots — discover all of that from the current working tree.
>
> Create a new architecture snapshot for `<TOPIC>` starting from `<ENTRY>` (an HTTP route, CLI command, queue consumer, scheduled job, webhook, or UI action). If `<TOPIC>` and `<ENTRY>` are not given, infer them from the user's request and confirm only if ambiguous.
>
> **Step 0 — Establish context from scratch.** Treat the repo as unknown. Find the project root (highest-level `.git/`); read top-level `README*`, `CLAUDE.md`, `AGENTS.md`, `CONTRIBUTING.md` to absorb domain language and layout. Detect the package manager and build tool from manifests (`package.json` + lockfile, `pyproject.toml`, `Cargo.toml`, `go.mod`, etc.) and use whatever the project uses. Choose a short kebab-case slug for the snapshot file/folder name. Locate the entry point in the code by searching for the most distinctive symbol or path the user mentioned; search synonyms before asking.
>
> **Step 1 — Capture the commit context.** Run `git rev-parse --short HEAD` (→ `<hash>`), `git show -s --format='%ai%n%s' HEAD` (→ commit date + subject), `git rev-parse --abbrev-ref HEAD` (→ branch). Create `.architecture/<commit-date>-<hash>/` where `<commit-date>` is the ISO `YYYY-MM-DD` date from `%ai` (this keeps snapshot folders in chronological order under an alphabetical sort). If the working tree is dirty, surface that to the user before proceeding.
>
> **Step 2 — Map the system.** Apply event-storming notation: **command → system → event(s)**, then the commands triggered by each event. Inside every step, surface the underlying infrastructure (handlers, queues / DLQs, queue policies, event-bus rules, datastore reads/writes, external APIs, scheduled rules). Cover every code path that branches off the entry — including parallel, anonymous, retry, fallback, and error variants. Walk the call graph outward from the entry until each chain terminates (a write with no further publish, a logger, an HTTP response). Discover source files yourself with file search (glob/grep), starting from the entry-point keyword and following imports / publish-subscribe pairs. Do not rely on any file list from past snapshots.
>
> Write the diagram as Mermaid `flowchart` blocks inside `.architecture/<commit-date>-<hash>/<TOPIC>.md`. Include a Legend block at the top. Each Mermaid block must declare its own `classDef`s (the SVG renderer treats each block in isolation). Use this colour convention so all snapshots stay visually consistent:
>
> | Role | Fill | Stroke |
> |---|---|---|
> | Command | `#a6d8ff` | `#1e6fb8` |
> | System / aggregate | `#fff2a8` | `#a08a00` |
> | Event | `#ffb976` | `#a85800` |
> | Policy / reaction | `#d6b8ff` | `#6b3fb0` |
> | Read model / store | `#b8e8c5` | `#2f7a45` |
> | Queue | `#e8e8e8` | `#666` |
> | DLQ | `#f8c8c8` | `#a83434` |
>
> After the diagrams, include a **Command → System → Event(s) reference table** that lists every command and event with: the system that handles it, the event(s) emitted, and the next command(s) those events trigger. This is the textual index of the diagram and what most readers will scan first.
>
> **Step 3 — Render diagrams to SVG.** Mermaid blocks must be pre-rendered so the document opens in any standard Markdown viewer (MacDown, GitHub, Quicklook, browsers) without a plugin. Run:
>
> ```bash
> cd .architecture/<commit-date>-<hash>
> mkdir -p diagrams
> npx -y -p @mermaid-js/mermaid-cli mmdc \
>   -i <TOPIC>.md \
>   -o diagrams/<TOPIC>.svg \
>   --backgroundColor white
> ```
>
> `mmdc` writes one SVG per Mermaid block, numbered `<TOPIC>-1.svg`, `<TOPIC>-2.svg`, etc. Rename them to descriptive filenames (e.g. `legend.svg`, `end-to-end-flow.svg`). In the Markdown document, embed each SVG with `![alt](diagrams/<name>.svg)` and keep the original Mermaid source in a collapsed `<details><summary>Mermaid source</summary>...</details>` block underneath so the diagram can be re-rendered. If `mmdc` fails (network, sandbox, missing Chromium), surface the error to the user before proceeding — do not leave raw unrendered Mermaid blocks in the snapshot.
>
> **Step 3b — Render the same diagrams in BPMN.** Every snapshot also carries a `diagrams-bpmn/` folder — one `<name>.png` + `<name>.bpmn` per Mermaid block — produced by the browserless renderer in [`tools/bpmn/`](./tools/bpmn/README.md):
>
> ```bash
> cd .architecture/tools/bpmn
> printf '{ "private": true }\n' > package.json   # npm needs one outside the pnpm workspace
> npm install @resvg/resvg-js --no-save
> node generate-bpmn.mjs
> rm package.json package-lock.json
> ```
>
> The batch command rewrites **every** snapshot's `diagrams-bpmn/` folder; after it runs, `git restore` any modified renders under earlier snapshots so only the new folder lands (past snapshots are historical records — their pixels must not drift with the generating host). A snapshot whose `diagrams/` exists but `diagrams-bpmn/` does not is **incomplete**: do not proceed to Step 4 without both, and if the renderer fails, surface the error rather than skipping the folder.
>
> **Step 4 — Update the snapshots table in `.architecture/index.md`.** Append a row with: commit short hash linked to the folder (`[\`<hash>\`](./<commit-date>-<hash>/)`), commit date, today's date, branch, commit subject, a one-line contents summary that does **not** reference current file paths (those may rot — describe the flow at the level of "X submits → Y processes → Z stores"). Keep snapshots in chronological order (newest at the bottom).
>
> **Step 5 — Do not commit.** Leave everything in the working tree. The user controls when to commit.

---

## Maintenance

When a past snapshot becomes substantially wrong (the underlying flow has been redesigned), do not edit the old snapshot — it is a historical record. Generate a new snapshot at the current commit instead.
