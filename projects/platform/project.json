{
  "name": "platform",
  "targets": {
    "deploy-infra": {
      "command": "pnpm build-curl-impersonate-layer && PULUMI_CONFIG_PASSPHRASE='' pulumi up --stack \"${PULUMI_STACK:?PULUMI_STACK is not set}\" $([ \"$CI\" = true ] && echo --yes) $([ \"$CI\" = true ] && [ \"$PULUMI_STACK\" = staging ] && echo --skip-preview)",
      "options": { "cwd": "{projectRoot}" }
    },
    "check": {
      "command": "true",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["lint"]
    },
    "lint": {
      "dependsOn": ["^compile"]
    }
  }
}
