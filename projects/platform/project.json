{
  "name": "platform",
  "targets": {
    "deploy-infra": {
      "command": "PULUMI_CONFIG_PASSPHRASE='' pulumi up --stack \"${PULUMI_STACK:?PULUMI_STACK is not set}\" $([ \"$CI\" = true ] && echo --skip-preview --yes)",
      "options": { "cwd": "{projectRoot}" }
    },
    "check": {
      "command": "true",
      "options": { "cwd": "{projectRoot}" },
      "dependsOn": ["lint"]
    },
    "lint": {
      "dependsOn": ["^compile"]
    }
  }
}
