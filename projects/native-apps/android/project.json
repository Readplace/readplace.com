{
  "name": "android",
  "projectType": "application",
  "targets": {
    "compile": {
      "command": "make apk",
      "options": {
        "cwd": "{projectRoot}"
      },
      "cache": false
    },
    "compile-dev": {
      "command": "make apk-staging",
      "options": {
        "cwd": "{projectRoot}"
      },
      "cache": false
    },
    "check": {
      "command": "if ./scripts/ax.sh true >/dev/null 2>&1; then make test; else echo 'SKIPPED android:check - no Android SDK on this machine, so nothing was tested here. Run `make test` once the SDK is installed.'; fi",
      "options": {
        "cwd": "{projectRoot}"
      },
      "dependsOn": []
    }
  }
}
