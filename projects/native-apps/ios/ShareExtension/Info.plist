<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Readplace</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsArbitraryLoadsInWebContent</key>
		<true/>
	</dict>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.share-services</string>
		<key>NSExtensionPrincipalClass</key>
		<string>$(PRODUCT_MODULE_NAME).ShareViewController</string>
		<key>NSExtensionAttributes</key>
		<dict>
			<!-- A predicate rather than the dictionary keys: sharing a PDF (Safari's
			     PDF viewer, Files, mail attachments) offers a com.adobe.pdf /
			     public.file-url attachment that no dictionary key short of
			     all-files covers, and the dictionary form hides the extension
			     unless every attachment class is declared. The URL and text
			     branches exclude file URLs so arbitrary non-PDF files stay
			     unmatched, and an item must carry at least one attachment so an
			     all-attachments-match subquery cannot pass vacuously. -->
			<key>NSExtensionActivationRule</key>
			<string>SUBQUERY (
    extensionItems,
    $extensionItem,
    $extensionItem.attachments.@count &gt; 0
    AND SUBQUERY (
        $extensionItem.attachments,
        $attachment,
        (
            ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url"
            AND NOT (ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.file-url")
        )
        OR (
            ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.plain-text"
            AND NOT (ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.file-url")
        )
        OR ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.adobe.pdf"
    ).@count == $extensionItem.attachments.@count
).@count &gt;= 1</string>
		</dict>
	</dict>
</dict>
</plist>
