<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

	<uses-permission android:name="android.permission.INTERNET" />

	<application
		android:allowBackup="false"
		android:icon="@mipmap/ic_launcher"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/Theme.Readplace">

		<!-- The launcher app: sign in (OAuth + PKCE), list the reading list, save a URL. -->
		<activity
			android:name=".app.MainActivity"
			android:exported="true"
			android:label="@string/app_name">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />
				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>

		<!--
			The share target — Android's analogue of the iOS Share Extension. It appears in
			the system share sheet for shared web links (delivered as text/plain), renders
			the page, and saves it. ACTION_SEND of text/plain is how browsers/apps share URLs.
		-->
		<activity
			android:name=".share.ShareActivity"
			android:exported="true"
			android:excludeFromRecents="true"
			android:label="@string/share_target_label"
			android:theme="@style/Theme.Readplace.Translucent">
			<intent-filter>
				<action android:name="android.intent.action.SEND" />
				<category android:name="android.intent.category.DEFAULT" />
				<data android:mimeType="text/plain" />
			</intent-filter>
		</activity>
	</application>
</manifest>
