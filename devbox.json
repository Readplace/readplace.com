{
  "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.14.2/.schema/devbox.schema.json",
  "packages": [
    "nodejs_22",
    "nodePackages.pnpm",
    "pulumi-bin",
    "awscli2",
    "jq@latest",
    "docker@latest"
  ],
  "shell": {
    "init_hook": [
      "echo 'Welcome to readplace monorepo!' > /dev/null",
      "if ! docker info >/dev/null 2>&1; then echo 'Starting Docker daemon (OrbStack)...'; open -ga OrbStack; n=0; until docker info >/dev/null 2>&1 || [ $n -ge 30 ]; do sleep 1; n=$((n+1)); done; fi"
    ],
    "scripts": {
      "test":  ["pnpm test"],
      "check": ["pnpm check"]
    }
  }
}
